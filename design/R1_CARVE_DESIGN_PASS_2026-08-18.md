# R1 / SLAB-CARVE -- design pass, 2026-08-18

**Preserved here because the standing rule on this project is that no work may be lost, and this
brief lived only in a scratchpad.** Ten agents: four read-only grounding sweeps and five adversarial
lenses attacking a candidate design (CARVE-v1), then a synthesis pass that re-verified every claim
at source.

**No code was written. This is the pass BEFORE the increment**, and it changed the design question
rather than answering it.

**THE HEADLINE, and it corrects the architect's own framing.** The crux was stated as: "carve makes
an UNPRIVILEGED instruction write the ODT, which is the single thing this machine's authority model
exists to protect." **That premise is false at source.** `veda_ocl_insts.sail` gates Populate on an
**OR** -- `cur_privilege == Machine | veda_oda_authorized()` -- and `veda_oda_authorized()` carries
no privilege term at all. A shipped RTL test, `rtl/sim/veda_smoke_m11.S`, drops to U-mode and mints
an ODT entry with a software-chosen Object_ID, Base, Length and sealing permissions through the ODA
alone. **The authority CARVE-v1 asks for is strictly narrower than what U-mode can already do.**

**WHAT WAS FIXED IMMEDIATELY OUT OF THIS PASS:** R44 below in DESIGN_07 -- `veda.bind` mode `0b11`,
the exact encoding slot CARVE would occupy, was not unconditionally fail-closed on Sail and the
differential suite reported AGREE while blind to it.

**STILL OPEN FROM THIS PASS:** the aliasing/pin finding (DESIGN_07 R45) and the R1 design itself,
which must now be re-thought on the corrected premise.

---

{
  "summary": "Design and adversarially refute SLAB-CARVE, the flagship R1 finding, before any code is written",
  "agentCount": 10,
  "logs": [],
  "result": {
    "brief": "Verification complete. Every claim below was re-read at source in this pass; nothing was built or run; rva23-core was never touched.\n\n---\n\n# R1 / CARVE-v1 — SYNTHESIS PASS\n\n## 0. Method note\n\nI re-verified every claim I repeat, including the four the architect asked for and the prior sweeps' headline findings. Where a prior sweep's line number was off by one I give the corrected one. Two claims in the incoming brief I found **wrong or incomplete** and say so in §5. One hypothesis of my own I **refuted**, and the refutation produced a new finding (§5, D-7).\n\n---\n\n## 1. ATTACKS: WHAT FIRES, WHAT DOES NOT\n\n### 1A. FIRES TODAY, against the machine as it exists, no carve needed\n\n**A-1. Unprivileged ODT minting already ships. The crux's premise is false at source.**\nThe brief says making an unprivileged instruction write the ODT \"is the single thing this machine authority model exists to protect.\" It is not. The gate is an **OR**:\n\n```\nveda_ocl_insts.sail:636   if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()\nveda_ocl_insts.sail:632-633  function veda_oda_authorized() -> bool =\n                               veda_oda_tag & not(isSealedCap(veda_oda)) & permBit(veda_oda.Perms, PERM_ACCESS_SYSTEM_REGISTERS)\n```\n\n`veda_oda_authorized()` carries no privilege term. And a shipped RTL test exercises exactly that path from U-mode:\n\n```\nrtl/sim/veda_smoke_m11.S:40   VEDA_DROP_TO_USER(x28, x29)   // R36: mstatus.MPP=U then mret\nrtl/sim/veda_smoke_m11.S:42-44  li x1, 41 ; li x2, 0x8001200000400300\n                                .insn r 0x0b, 0x0, 0x03, x5, x1, x2   # veda.odt.populate -- via ODA alone\nrtl/sim/veda_smoke_m11.S:46-49  bind c4, then cgettag == 1, cgetbase == 0x80012000\n```\n\nThat descriptor decodes (veda_ocl_insts.sail:697-699) to Base 0x80012000 / Length 0x40 / **Perms 0x0300 = PERM_SEAL|PERM_UNSEAL** (veda_types.sail:226-227). A U-mode principal already mints an ODT entry with a software-chosen Object_ID, Base, Length and sealing authority. **The authority CARVE-v1 asks for is strictly narrower than what U-mode can already do with an ODA.** Status: not a defect — architecture (Milestone 11) — but it reframes the whole question. Confidence: high, source + shipped test.\n\n**A-2. Aliasing ODT entries are unchecked, and R11(b)'s pin is name-scoped while its guarantee is memory-scoped.**\nNo disjointness check exists anywhere. I re-ran the grep: `overlap|disjoint|intersect` over `model/extensions/Veda/*.sail` returns one unrelated hit (`veda_atomic_insts.sail:42`), over `veda_core.tlv` four unrelated hits (`:4626, :5239, :6414, :6605`), over `VEDA_CORE_SPEC.md` zero. Populate takes Base verbatim:\n\n```\nveda_ocl_insts.sail:697   Base       = zero_extend(descriptor[63..32]),\nveda_ocl_insts.sail:890   Base       = base,\n```\n\nThe pin is an identity test:\n\n```\nveda_regs.sail:990-992  function veda_object_is_executing(object_id : bits(44)) -> bool =\n                          (object_id == veda_pcc_object)\n                          | (veda_trap_depth != zeros() & object_id == veda_mepcc_object)\n```\n\nso an authority holding Machine or a tagged ODA — **R11(b)'s own stated adversary**, veda_ocl_insts.sail:648-652 — can populate a *second* name at the *same* Base as the running compartment's code object and then page it out, because 190 != 180:\n\n```\nli x1, 190 ; la x7, compartment\n.insn r 0x0b, 0x0, 0x04, x5, x1, x7   # populate.fast 190 -- pin at :873 tests 190 == 180 -> NO\nli x1, 190\n.insn i 0x0b, 0x1, ...                # page.out 190 -- pin at :1115 passes; :1116 valid+resident pass;\n                                      #   :1158 cow false; :1160 gen unsaturated -> RETIRES\n```\n\nFetch cannot notice: `ext_fetch_check_pc` compares PC against PCC's cached bounds only —\n`model/postlude/step_ext.sail:85-90`, `if veda_pcc_length != VEDA_PCC_UNBOUNDED & (unsigned(pc) < unsigned(veda_pcc_base) | ...)`.\nThe RTL agrees, by slot compare: `veda_core.tlv:2573-2574 $veda_object_slot_is_pcc = ($veda_object_id[43:24] == $veda_pcc_object[43:24]) && ($veda_object_id[7:0] == $veda_pcc_object[7:0]);` — 190 vs 180 is also false.\n\n**The corpus cannot see it.** `sail_tests/vc_r11b_executing_pin_neg.S:70-72` is PART D, the over-refusal control, and it populates object 183 at a *disjoint* label `victim_pad`. So the suite covers the disjoint case and the same-name case and **nothing in between**. I could not find any test in `sail_tests/`, `rtl/sim/` or `difftest/probes/` that populates a second object at an already-live Base.\n\nHonest severity: nothing is corrupted in-tree — there is no pager to act on the answer, and the eviction only clears object 190's `resident` and bumps its generation. What is wrong is the **architectural statement**, and the RTL sets that standard itself for the converse case (`veda_core.tlv:2600-2603`: a silent refusal \"is worse than either trapping or succeeding, because the pager then reuses memory it does not own\"). Confidence: source-derived, **not measured** — see §6.\n\n**A-3. `veda.bind` mode 0b11 is NOT unconditionally fail-closed on Sail. Real two-layer divergence, on the exact slot R1 wants.**\nSail reaches the reserved arm only after three ODT-state-dependent traps:\n\n```\nveda_bind_insts.sail:211  if not(veda_region_is_resident(region)) then veda_trap(rd, VEDA_CAUSE_REGION_FAULT)\nveda_bind_insts.sail:284  if not(veda_bind_domain_ok(e.owner_domain)) then veda_trap(rd, VEDA_CAUSE_DOMAIN_VIOLATION)\nveda_bind_insts.sail:285  else if e.valid & not(e.resident) then veda_trap(rd, VEDA_CAUSE_RESIDENCY_FAULT)\nveda_bind_insts.sail:289    VEDA_BIND_RESERVED => Illegal_Instruction(),\n```\n\nThe RTL refuses at **decode** — mode 0b11 is absent from `$veda_decoded` (`veda_core.tlv:4833-4835`, comment: \"Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap\"), feeding `$veda_undef_encoding = $veda_op_claimed && !$veda_decoded;` (`:4846`), and the three fault signals are gated on the three real modes only (`:2346`, `:2379`, `:2402`), so mode 0b11 never reaches them.\n\nReproduction on state both layers already seed identically — region 2 is non-resident on both (`veda_regs.sail:1124 region_resident = false`; `veda_core.tlv:291 rt_valid[2] = 1'b1; rt_resident[2] = 1'b0;`):\n\n```\nli x1, 0x02000007\n.insn i 0x0b, 0x5, x2, x1, 3       # Sail: VEDA_CAUSE_REGION_FAULT 0x09 ; RTL: illegal-instruction mcause 0x02\n```\n\n**The existing probe is blind twice over.** `difftest/probes/p5_reserved.S:26` sets `li x1, 1` (region 0, resident, owner_domain ANY — all three gates pass), and its handler at `:48-49` is `trap_handler: addi x29, x29, 1` — it counts traps and never reads mcause. Note also that `p5_reserved.S:6` cites \"veda_bind_insts.sail:276\" and `veda_core.tlv:4728` cites the same — both now stale; the arm is at :289.\nSecondary consequence: a reserved encoding whose refusal cause is a function of another domain's paging state is a small ODT-state oracle for unprivileged code. Confidence: high, source-verified, not run.\n\n**A-4. Saturate-then-retire-forever is reachable today** through privileged Destroy. `veda_ocl_insts.sail:784-785` bump-then-retire; `:671 if old_entry.retired then Illegal_Instruction()` refuses Populate permanently; RTL mirrors at `:2730 $veda_odtpd_new_retired = $veda_odt_retired || ...` with the retired term in `$veda_odt_populate_violation` (`:2610`). Today the counter ticks at object-lifetime cadence, so 2^24 is unreachable in practice; under CARVE-v1 clause 5 it ticks at free() cadence. Fires as a *property*; the DoS is allocator-self-inflicted today, attacker-driven only if carve/free go unprivileged.\n\n**A-5. No instruction in the ISA can clear a permission bit on a live ODT entry.** I enumerated every writer of `Perms`: only Populate (`veda_ocl_insts.sail:699`) and Populate-Fast (`:899`) write it; Destroy (`:790`), set.cow (`:1002`), set.domain (`:1048`), page-out (`:1168`) and page-in (`:1238`) all copy `Perms = old_entry.Perms`. CAndPerm writes the register only (`veda_cap_insts.sail:981`). Revoking any entry-held right therefore requires re-Populate, which bumps the generation (`:683-685`) and kills every outstanding capability. Fires today as a structural property; it is the second, independent kill of any entry-held `PERM_CARVE`.\n\n### 1B. FIRES THE DAY CARVE-v1 LANDS AS SPECIFIED (design refutations, not live exploits)\n\nAll of these are blocked today only because there is no carve instruction: `veda_bind_insts.sail:289` and `veda_core.tlv:4833-4846`.\n\n- **B-1. Re-carve the window.** No overlap check (A-2), so a second child minted over a *live* element gives full access with every arm passing: `veda_ocl_insts.sail:72` reads only the named entry, `:83` matches by construction, `:182` bounds against the child's own Length. Consequence: **\"carve-only, not directly dereferenceable\" is vacuous** — carve authority over `[a,b)` *is* load/store authority over `[a,b)`, one instruction later, at any sub-range. This is a rights-**amplifying** derivation in an ISA whose derivation family is monotone by construction (`veda_cap_insts.sail:981`, `:236`).\n- **B-2. Re-Bind the name.** Clause 2 imports R41's reset — `veda_ocl_insts.sail:742 owner_domain = VEDA_DOMAIN_ANY,` — and `veda_regs.sail:986 if owner_domain == VEDA_DOMAIN_ANY then true`. So every carved child is bindable by every domain that can name it, and Bind hands over the entry's Perms verbatim (`veda_regs.sail:982-983`). Names are free: `VEDA_CAPQUERY`/`CGetObjectID` has no privilege test in its encdec (`veda_cap_insts.sail:139-141`) or its execute body (`:143-166`), and the file states the attack against itself at `:126-132`.\n- **B-3. `PERM_CARVE` is advisory.** `veda_bind_perms(e)` re-derives from the entry; the codebase already adjudicated this for `PERM_STORE` and says so verbatim at `veda_bind_insts.sail:316-320`.\n- **B-4. Clause 2 vs clause 6 is a closed trilemma.** Child Perms can come from (a) the parent — then the child is carve-only too and the mechanism is inert; (b) a GPR — an unprivileged instruction mints arbitrary Perms including bits 7/8/9/10/11; (c) `veda_attr` — Machine-only on **both** layers (Sail generically via `csrPriv(csr) = csr[9..8]`, `sys/sys_control.sail:19`, with 0x7C4[9:8] = 0b11; RTL `veda_core.tlv:4918`).\n- **B-5. Composition breaks four built mechanisms.** R11(b) pin (parent unpinned while child executes, and the mirror), COW (`:256` reads the child's own entry; child born `cow = false` at `:743`), `owner_hart` (Populate resets to `VEDA_OWNER_UNOWNED` at `:706`/`:901`, defeating the only cross-hart exclusion at `veda_bind_insts.sail:222`), and R10 identity (`veda_pcc_object` is installed from `cs1.Object_ID` at `veda_cap_insts.sail:608` and `:831`; its region field *is* the domain, `veda_regs.sail:988`).\n- **B-6. Exhaustion becomes a two-instruction unprivileged permanent DoS**, and the reclaimer is unstarted (`DESIGN_08:203-206`; `DESIGN_07:104-105`).\n- **B-7. Cost fails on the free half.** Clause 5 is ODT-Destroy of the child, whose gate at `veda_ocl_insts.sail:753` CARVE-v1 does not touch.\n\n### 1C. DOES **NOT** FIRE — record these so they are not re-closed\n\n- ODA escalation via a carved `PERM_ACCESS_SYSTEM_REGISTERS` capability: **blocked**, `veda_cap_insts.sail:893-894 function clause execute VEDA_OSPECIALRW(scr, rs1, rd) = { if cur_privilege != Machine then Illegal_Instruction()`.\n- Carving the ODT itself: **blocked**, Sail's ODT is a Sail register (`veda_regs.sail:802`), the RTL's `odt_mem` (`veda_core.tlv:234`, ODT_BASE 0x9000_0000) is physically distinct from `elfmem` (`:942`, 0x8000_0000).\n- OCA walking from element 3 to element 7 through a per-element child: **refused**, `veda_cap_insts.sail:64` / `:77`. This is CARVE-v1's real, verified win.\n- Spill/reload laundering via OCS.C/OCL.C: **refused**, generation is packed and re-checked (`veda_types.sail:191`, `:200`; recheck at `veda_ocl_insts.sail:83`).\n- Same-child-id reuse after free: **refused**, Destroy bumps unconditionally (`:784`).\n- Generation saturation as a UAF: **not a UAF**, only DoS (`:785`, `:671`).\n- A missing COW arm on the second access checker: **refuted by me this pass** — the second checker is `veda_check_nmc_access` (`veda_ocl_insts.sail:417`) and it does carry `else if entry.cow then Err(... COW_FAULT)` at `:514`.\n\n---\n\n## 2. THE SMALLEST DESIGN THAT STILL CLOSES THE CLASS\n\nStated part by part, each marked by what the attacks proved.\n\n**LOAD-BEARING — the attacks could not remove these.**\n\n- **P1. One ODT entry per slab element, with its own generation. FORCED, not chosen.** The dereference recheck is `entry.generation != cap.generation` against `odt_lookup(cap.Object_ID)` (`veda_ocl_insts.sail:72`, `:83`), so a generation is per-Object_ID by construction. Option (b) — a fresh generation without a fresh ID — is refuted by that same line. This half survived every attack in every lens.\n- **P2. Containment against the parent CAPABILITY's window, computed wider than the widest operand.** CSetBounds already has the hardened form: `veda_cap_insts.sail:229 let window_ok : bool = unsigned(cap.Offset) + unsigned(new_length) <= unsigned(cap.Length);`. Validating against `odt_lookup(cap.Object_ID).Length` instead would let a narrowed delegated sub-capability carve the whole arena, because CSetBounds keeps `Object_ID` and `generation` (`:232`, `:238`).\n- **P3. The child inherits `owner_domain`; it is NOT reset to ANY.** Proven load-bearing by B-2.\n- **P4. The child inherits `owner_hart`.** Proven load-bearing by B-5 (multi-hart).\n- **P5. The child inherits `cow`.** Proven load-bearing by B-5 (COW bypass).\n- **P6. The child Object_ID is DERIVED from the parent, never GPR-supplied.** Proven load-bearing three times: RTL slot takeover (the Populate write arm at `veda_core.tlv:6466` is the *only* one with no `id_match`, unlike `:6577`, `:6579`, `:6583`); the oracle-vs-clobber dilemma; and R10 identity forgery (P6 is what makes `child.region == parent.region` structural rather than checked).\n- **P7. Carve consults the parent's residency and generation explicitly.** Carve is not a memory access, so `veda_ocl_insts.sail:225` does not apply by inheritance.\n- **P8. The executing-object pin must cover carve — and its predicate must become a WINDOW test.** Adding carve to the four-instruction consumer set (`:659`, `:776`, `:873`, `:1115`) is necessary and **provably insufficient**: it pins the child and leaves the parent open. This is the single most expensive part of any honest R1, and A-2 shows the defect predates carve.\n\n**NOT LOAD-BEARING — the attacks proved these are decoration or worse.**\n\n- **D1. Bind mode 0b11 as the host encoding.** The encoding has one GPR, one capability register and ten reserved-zero bits (`veda_bind_insts.sail:186`); carve needs a window. Drop it. CSetBounds' shape (`veda_cap_insts.sail:206`) already carries offset (from `cap.Offset`) and length (rs2), and brings P2 pre-hardened.\n- **D2. `PERM_CARVE` as a Perms bit.** Advisory (B-3) *and* irrevocable (A-5). Use `owner_domain`, which exists, is entry-resident and is enforced on both layers — or an entry bit written by a set.domain-family instruction that does not bump the generation (`veda_ocl_insts.sail:1042-1044` is the precedent).\n- **D3. \"A fresh generation\" on carve.** DESIGN_07:67 lists it as load-bearing; it is not. Only *change on free* matters, and Destroy guarantees that unconditionally (`:784`). Worse: read as \"reset to zero\" it specifies an ABA bug — the exact failure `retired` exists to prevent (`:685`, `:786`).\n- **D4. The carve-only parent Permit (clause 6).** DESIGN_07:63-67 declares this the load-bearing part. B-1 and B-4 refute it: it is a rights amplification that reads as a restriction, and it has no source for the child's Perms.\n- **D5. Unprivileged minting.** Not load-bearing for the security claim **at all**. The class closes on per-element Object_IDs — a property of the mechanism, not of the minter. The check at `veda_ocl_insts.sail:83` does not care who wrote the entry.\n\n**MINIMAL SHAPE, both variants, for the architect to choose between.**\n\n*Variant C (carve, privileged first):* P1+P2+P3+P4+P5+P6+P7+P8, one new instruction, gated Machine-or-ODA. Every part above is forced by an attack; nothing else survives.\n*Variant S (deletion-shaped, no new opcode):* add P2-style containment to Populate/Populate-Fast against a range the authority holds, and scope the ODA (today `veda_ocl_insts.sail:632-633` and `veda_core.tlv:4184` read exactly one permission bit and ignore the ODA capability's own Object_ID, Base and Length). This closes Rev-E as a side effect and dissolves the clause-6 trilemma, because the arena stops being an object whose Perms must be split. It does **not** give an unprivileged malloc, and it needs an ODA-scoping design that does not exist.\n\n---\n\n## 3. CONTRADICTIONS BETWEEN LENSES — stated, not averaged\n\n**C-1. Is the crux a crux at all?**\n`unprivileged-odt-write`: the model is *already* inverted; unprivileged ODT minting ships (A-1) and is strictly wider than carve; what must be fixed is grant provenance and revocability, not the privilege boundary.\n`authority`: CARVE-v1 *inverts* the machine's authority model; ship privileged first.\nBoth cite the same OR at `veda_ocl_insts.sail:636`. They disagree on whether holding an ODA counts as \"unprivileged\". **Unresolved. It decides whether R1's crux is a real question.**\n\n**C-2. Is the mechanism sound?**\n`does-it-close-the-class`: \"the mechanism half works and I could not break it… forced, not chosen.\"\n`composition`: the mechanism is precisely what breaks R11(b), COW, `owner_hart` and R10, because all four were established for a world where one byte belongs to at most one live entry, and clause 2 abolishes that world.\nBoth are correct about different objects — the mechanism closes UAF for *held capabilities* and simultaneously invalidates four *composition* invariants. **These must not be averaged into \"mostly fine\".**\n\n**C-3. Derived child IDs: the one candidate, or the worst one?**\n`id-space`: derivation is \"the one candidate that survives the allocator question with DESIGN_07 R1's own cost claim intact.\"\n`minimality`: derivation \"hands every element-holder a capability to every sibling — worse than the arena being dereferenceable\" (`cgetobjectid`, `addi x1,x1,1`, `veda.bind`, all unprivileged).\n**Direct contradiction on the same proposal.** My reading: they are reconcilable *only* under P3. With `owner_domain` inherited and narrowed, sibling names are useless to a foreign domain; with `owner_domain = ANY` as clause 2 specifies, minimality is right and id-space is wrong. Neither lens said this. It makes P3 a **precondition of P6**, not an independent hardening.\n\n**C-4. Cost.**\n`alternatives`: the cost premise is false — `veda_malloc`/`veda_free` are already one instruction each with no trap (`runtime/veda_rt.c:68-95`, `runtime/veda_rt_asm.S:26-34`), so option (a) already costs what carve would cost.\n`exhaustion`: R1 turns the 24-bit temporal-safety counter into a malloc/free-rate consumable with a permanent-retirement floor.\nCompatible in fact, contradictory in verdict. Together — and neither lens says this alone — **R1 pays a finite, non-renewable resource for a cost win that does not exist.**\n\n---\n\n## 4. THE EXACT EDIT SET, IF IT WERE BUILT\n\n### Sail\n\n1. **`veda_types.sail`** — add the carve field(s) to `odt_entry` (struct at `:287-384`) and to `empty_odt_entry` (`:386-405`). This forces edits to **nine** struct literals, and that is the dominant mechanical risk: `veda_ocl_insts.sail:686-745` (Populate), `:787-804` (Destroy), `:888-914` (Populate-Fast), `:998-1012` (set.cow), `:1041-1055` (set.domain), `:1162-1177` (page-out), `:1234-1253` (page-in), `veda_bind_insts.sail:226-245` (`claimed_entry`), plus the 15 reset seeds in `veda_regs.sail:1141-1388`. The RTL states the same hazard for its own layout at `veda_core.tlv:6486-6493`.\n2. **`veda_cap_insts.sail`** — new instruction in the CSetBounds idiom. **Custom-2 (`0b1011011`), funct3 `0b001`, funct7 `0b0011000`** — verified next free: the allocated set is `0001000`, `0001001`, `0001010`, `0010000`..`0010111`.\n3. **New cause code** — `0x0D` is the next free (the list at `veda_bind_insts.sail:17-138` allocates through `0x0C`).\n4. **`veda_ocl_insts.sail`** — add carve to the pin consumer set beside `:659`, `:776`, `:873`, `:1115`.\n5. **`veda_regs.sail:990-992`** — if P8's window pin is adopted, replace the identity test with a range test against `veda_pcc_base`/`veda_pcc_length` (`veda_regs.sail:97-98`). This is the largest single behavioural change in the set.\n\n### RTL (`veda_core.tlv`)\n\n6. **Entry layout.** Bytes **30-31 are the only spare** — `ODT_OFF_RESIDENT = 25` (`:135`), `ODT_OFF_OWNER_DOMAIN = 26` occupying 26-28 (`:148`), `ODT_OFF_COW = 29` (`:160`), `ODT_ENTRY_BYTES = 32` (`:108`). A 24-bit stride does not fit alongside a flag; this forces the 64-byte bump `DESIGN_02:316-317` deferred. Fix the stale comment at `:106` in the same edit.\n7. **Decode** — add the terminal to `$veda_decoded` (`:4827-4846`) or it traps (that inversion is the point of R30).\n8. **A seventh write arm** in the chain at `:6466-6657`, and it **must** carry `CPU_veda_odt_id_match_a0` — copy `:6583` (Destroy), **not** `:6466` (Populate, which deliberately has no id_match for slot takeover, `:2549-2562`).\n9. **Pin** — `$veda_object_is_executing` (`:2577`) consumers; a window pin needs `$veda_pcc_base`/`$veda_pcc_length` comparisons.\n10. **Index truncation** — `$veda_odt_entry_idx = $veda_region_base + {24'b0, $veda_local[7:0]}` (`:2146`) and `$veda_odt_idx[7:0] = $veda_local[7:0]` (`:2154`) cap the RTL at 256 locals per region. A derived-stride scheme must not collide mod 256, or the RTL corpus cannot express more than 256 children per region at all.\n\n### Tests and probes that would prove it\n\n- `sail_tests/vc_carve.S` — positive: carve, dereference through the child, free the child, dereference again → TAG_VIOLATION 0x01.\n- `vc_carve_containment_neg.S` — the **R18 shape**: `length = 0xFFFFFFFFFFFFFFF8` so `offset + length` wraps. Sail may not be able to express it (R18's own recorded lesson) — so this one must also exist as an RTL sim.\n- `vc_carve_rebind_neg.S` — the B-2 attack: `cgetobjectid` the child, free, re-carve for a different owner, `veda.bind` the saved name → must fail. **This is the test that decides P3.**\n- `vc_carve_sibling_neg.S` — `cgetobjectid` child, `addi x1,x1,1`, `veda.bind` → must fail. **This is the test that decides C-3.**\n- `vc_carve_cow_neg.S`, `vc_carve_owner_hart_neg.S` — P5, P4.\n- `vc_carve_pin_neg.S` — page.out the **parent** while executing a **child**, and the mirror. This is the test that would have caught the composition finding, and it is the one no existing test resembles.\n- `difftest/probes/p19_carve.S` — and it must read `mcause`, not count traps (the `p5_reserved.S:48-49` failure mode).\n- `rtl/sim/veda_smoke_carve.S` / `_neg.S`.\n- **Prerequisite, before the 0b11 slot is claimed at all:** fix `p5_reserved.S` to use a region-2 Object_ID and to record `mcause`.\n\n---\n\n## 5. REAL DEFECTS UNRELATED TO R1, WORTH RECORDING SEPARATELY\n\n- **D-1. `veda.bind` mode 0b11 trap-cause divergence.** A-3 above. Two-instruction reproduction, both layers exist, existing probe blind twice. Highest-confidence item in this pass.\n- **D-2. R11(b)'s pin is name/slot-scoped while the authority it refuses is memory-scoped, and Populate's unvalidated Base already breaks the correspondence.** A-2 above. Same family as the recorded-open **Rev-E**; Rev-E narrows it (a legal-backing-region check) but does not close it — two objects may still alias *inside* the legal region. The closing invariant is entry disjointness or a window pin.\n- **D-3. `veda_core.tlv:106` is two increments stale**: \"26 B used, 6 spare after RTL-6\" — actually 30 B used, 2 spare, after RTL-17 (`:148`) and RTL-18 (`:160`). Any entry-field work reads this comment first.\n- **D-4. `runtime/veda_rt.c` models retirement 65536× early**: `static uint8_t g_destroy_count[VEDA_RT_MAX_OBJECTS];` (`:48`) retiring at wrap (`:104-106`) versus hardware's 2^24 (`veda_ocl_insts.sail:784`). Stale in the **safe** direction, so not a security bug — but any R1 cost model that reads the runtime's retirement rate as the hardware rate is off by 65536.\n- **D-5. No instruction clears a permission bit on a live ODT entry.** A-5 above. Not a bug today; a hard blocker for any future entry-held right, and it should be recorded before one is designed.\n- **D-6. (mine, low severity)** Sail's Populate computes `new_retired = old_entry.valid & (old_entry.generation == 0xffffff)` (`veda_ocl_insts.sail:685`) — it does **not** OR in `old_entry.retired`, unlike the RTL (`veda_core.tlv:2730 $veda_odtpd_new_retired = $veda_odt_retired || ...`) and unlike Sail's own Destroy (`:785`). Behaviourally equivalent **only** because `:671` refuses retired slots first. No defence in depth: delete or weaken `:671` and Sail silently *clears* retirement while the RTL does not.\n- **D-7. (mine, new — the product of a refuted hypothesis, and it lands directly on the recommended fix P3.)** I hypothesised that a never-populated slot's `owner_domain` defaults to domain 0, making the domain gate fire on non-existent objects. **Refuted:** `veda_types.sail:404 owner_domain = VEDA_DOMAIN_ANY,` in `empty_odt_entry`. But the comment above it, `veda_types.sail:398-403`, states the consequence itself: *\"A not-found lookup reports OPEN, not 'owned by nobody' … Reporting a foreign domain here would usefully hide whether an object EXISTS -- a real improvement -- but it needs a third state this field cannot express.\"* So under **P3** — which four of the five lenses recommend — a live child narrowed to the carver's domain traps `DOMAIN_VIOLATION` at `veda_bind_insts.sail:284`, while an un-carved slot reads ANY, passes the gate, and returns `OBJECT_NOT_FOUND` (or soft-fails under `.notrap`, `:376-378`). **A foreign domain can then distinguish allocated from free for every element of the arena by trap cause.** This oracle exists today only for objects deliberately narrowed by the privileged `set.domain`; P3 makes it universal at slab-element granularity. The missing third state is already named in the source and unbuilt. **This is a cost of the recommended fix that no prior sweep priced.**\n\n---\n\n## 6. WHERE THE EVIDENCE IS THIN — plainly\n\n- **Nothing was executed.** The Sail model cannot be rebuilt on this machine (recorded as task #21) and this pass was read-only by instruction. **A-2 and A-3 are source-level derivations over every arm of both chains, not measured results.** A-3 is a two-instruction reproduction on state both layers already seed, and I rate it high-confidence; **A-2 requires executing inside a compartment so that `veda_pcc_object == 180`, and I did not confirm that the surrounding test harness reaches that state with an M-mode/ODA actor still able to issue Populate.** Both should be measured before being recorded as findings.\n- **Whether the parent is `rd` or a second capability operand is still unfixed by the design**, and it changes the shape of P2, P6 and the R10 identity obligation. Every sweep hit this wall.\n- **RTL-vs-Sail capacity.** The RTL models 768 entries / 256 locals per region (`veda_core.tlv:87`, `:94`) against Sail's 2^20 modeled locals per region (`veda_regs.sail:830`). **No carve scheme can be differentially tested past 256 children per region on the current RTL**, so any claim about namespace behaviour at scale is architecture-only, unmeasurable in this tree.\n- **The footprint numbers are arithmetic over stated constants, not measurements.** The 4 KiB / 128-entry ODT-granule demand-paging `DESIGN_08:55-57` relies on to bound the per-region ODT **does not exist in either layer** — I re-ran the grep, and `granule` hits only capability tag-store granules (`veda_types.sail:185-188`; `veda_core.tlv:949`, `:1713`). Every footprint bound in this brief therefore rests on a mechanism that is named but unbuilt.\n- **I did not attempt to price P8** (converting the executing-object pin from an identity test to a window test) in cycles or area. It is the largest edit in §4 and the least analysed anywhere in this brief.",
    "refutations": [
      {
        "verdict": "REFUTED",
        "strongest_attack": "CARVE-v1 SPLITS THE MINT FROM THE POLICY. It delegates the half that widens and keeps the half that narrows, and the kept half is privileged, un-pre-stageable, and irrevocable. This survives every repair the other sweeps proposed (derived child IDs, entry-held carve right, wide containment arithmetic), because it is forced by two pre-existing decisions that were each correct for a privileged minter.\n\nWHAT THE POPULATE GATE IS REALLY PROTECTING (my lens): not \"who may write the ODT\" -- the machine already permits unprivileged ODT writes, see does_it_fire. It protects the invariant that THE PRINCIPAL THAT MINTS AN ENTRY IS THE PRINCIPAL THAT SETS ITS POLICY. One authority (Machine-or-ODA) gates Populate (veda_ocl_insts.sail:636), set.domain (:1029) and set.cow (:989) identically, so \"mint open, then narrow\" is a single trusted sequence with no exploitable intermediate state.\n\nTHE CONTRADICTION, at source:\n(1) Clause 2 resets the child's owner_domain \"the way Populate resets them (R41)\" -- veda_ocl_insts.sail:742 `owner_domain = VEDA_DOMAIN_ANY,`; veda_types.sail:285 `let VEDA_DOMAIN_ANY : bits(20) = 0xFFFFF`; veda_types.sail:371-372 calls that \"how every object is created\".\n(2) veda_regs.sail:986 `if owner_domain == VEDA_DOMAIN_ANY then true` -- every carved element is bindable by EVERY domain that can name it.\n(3) veda_regs.sail:982-983 `veda_bind_perms(e) = if e.cow then e.Perms & 0xFFF7 else e.Perms` -- that binder receives the child's ENTRY Perms verbatim. Full authority, not an attenuated view.\n(4) The only narrowing instruction is privileged: veda_ocl_insts.sail:1029 `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`.\n(5) AND IT CANNOT BE PRE-STAGED: veda_ocl_insts.sail:1039 `if not(old_entry.valid) then Illegal_Instruction()`, whose own comment says a policy on a non-existent object is meaningless \"and allowing it would let software pre-stage rules on slots it does not own yet\". RTL mirrors it at veda_core.tlv:2617-2618 (`|| !$veda_odt_valid`).\n\nSo there is a MANDATORY OPEN WINDOW on every carve, and no third horn:\n (a) leave the child open -> intra-region isolation is gone for every heap element. owner_domain is the ONLY intra-region isolation this machine has (region == domain: veda_regs.sail:988 `owner_domain == veda_pcc_object[43 .. 24]`), so CARVE-v1 does not merely fail to help, it converts every slab allocation into a hole in the mechanism DESIGN_07 R17 left as the residue.\n (b) call privileged set.domain after each carve -> a syscall per malloc. The crux exists to avoid exactly that, so the cost argument dies on the carve half too, not just the free half.\n And (b) is racy anyway: Populate resets owner_hart to UNOWNED, and Bind claims it (veda_bind_insts.sail:232 `owner_hart = mhartid[7..0],`), so on multi-hart another hart binds and claims the child inside the window; the rightful carver then gets OWNER_VIOLATION on its own allocation.\n\nSECOND, INDEPENDENT KILL -- PERM_CARVE IS IRREVOCABLE. I enumerated every writer of an entry Perms field: only Populate (veda_ocl_insts.sail:699 `Perms = descriptor[15..0],`) and Populate-Fast (:899 `Perms = veda_attr[15..0],`). Destroy (:790), set.cow (:1002), set.domain (:1048), page-out (:1168) and page-in (:1238) all copy `Perms = old_entry.Perms` verbatim. CAndPerm writes the REGISTER only (veda_cap_insts.sail:981 `let new_perms : bits(16) = cap.Perms & mask[15 .. 0];`). So THERE IS NO INSTRUCTION THAT CLEARS ONE PERMISSION BIT ON A LIVE OBJECT. Revoking carve authority over an arena requires re-Populating it, which bumps the generation (veda_ocl_insts.sail:683-685) and kills every outstanding capability to the arena -- i.e. you can only revoke carve by destroying the thing carve exists to share. Contrast the ODA, the machine's existing unprivileged-mint authority: it is one register, installed by one M-mode instruction, revoked by one more (veda_cap_insts.sail:910-911).\n\nTHIRD -- CARVE MUST INSPECT AN ENTRY IT HAS NO AUTHORITY OVER. Carve reads parent P and writes child C. To be safe it must test C's current occupant (retired, valid, executing, RTL alias). But C is a name the carver holds no capability to. If carve refuses on C's state, unprivileged code gets an ODT-state oracle over its whole region -- precisely what veda_bind_insts.sail:284-287 refuses to leak (\"a hint about an object that is none of its business\"). If it does not refuse, it clobbers another domain's live object. A privileged minter is entitled to the whole table's state, so for Populate this is not an oracle; for an unprivileged carver it is. Only a DERIVED child id (arena_base + index), which CARVE-v1 does not specify, dissolves this.\n\nFinally, on the crux as literally worded: \"the parent capability is the authority\" cannot be true here, and R34's own recorded test says why. A capability's Perms are a cache of the entry's Perms (veda_regs.sail:983), and Bind is unprivileged with the domain gate open by default, so the set of principals that can carve equals the set that can NAME the arena. Names are 44-bit integers, freely readable via ungated CGetObjectID and enumerable for free via veda.bind.notrap, which soft-fails with `wCTag(rdno, false); RETIRE_SUCCESS` instead of trapping (veda_bind_insts.sail:363-366, :368-371). By R34's own standard -- \"an authority gate whose authority no reachable context lacks denies nothing\" (DESIGN_07:838-840) -- PERM_CARVE denies nothing.",
        "does_it_fire": "NO -- my attack CANNOT FIRE against the machine as it exists, and I state that plainly. CARVE-v1 is unbuilt and its encoding slot is fail-closed on both layers: Sail veda_bind_insts.sail:289 `VEDA_BIND_RESERVED => Illegal_Instruction(),`, and the RTL never decodes mode 0b11 -- veda_core.tlv:4833-4835 lists only `$is_veda_bind_plain || $is_veda_bind_notrap || $is_veda_rebind` with the comment \"Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap\", feeding `$veda_undef_encoding = $veda_op_claimed && !$veda_decoded;` (:4846). Every step above is an argument about a design, not a reachable exploit.\n\nBUT ONE THING I WENT LOOKING FOR DOES FIRE, AND IT FALSIFIES THE CRUX'S PREMISE. The brief says making an unprivileged instruction write the ODT \"is the single thing this machine authority model exists to protect\". That is false at source. Unprivileged ODT minting ALREADY EXISTS, is architected, and ships as a passing test on the RTL:\n\n/home/prabhu/veda-core-sindhu/veda-core/rtl/sim/veda_smoke_m11.S:40-44\n    VEDA_DROP_TO_USER(x28, x29)   // R36: the architected drop -- mstatus.MPP=U then mret\n    li   x1, 41\n    li   x2, 0x8001200000400300\n    .insn r 0x0b, 0x0, 0x03, x5, x1, x2  # veda.odt.populate x5, x1, x2  -- must succeed via ODA alone (!$priv now)\n\nand :47-49 then Bind the result and assert cgettag == 1, cgetbase == 0x80012000. That descriptor decodes (per veda_ocl_insts.sail:697-699) to Base 0x80012000, Length 0x0040, Perms 0x0300 = bits 8 and 9 = PERM_SEAL | PERM_UNSEAL (veda_types.sail:226-227). So a U-mode principal already mints an ODT entry with a software-chosen Object_ID, Base, Length and sealing authority. The gate is an OR, not an AND: veda_ocl_insts.sail:636 `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`, with veda_ocl_insts.sail:632-633 `veda_oda_authorized() = veda_oda_tag & not(isSealedCap(veda_oda)) & permBit(veda_oda.Perms, PERM_ACCESS_SYSTEM_REGISTERS)` carrying no privilege term at all. RTL mirrors: veda_core.tlv:2609-2611 `!($priv || $veda_oda_authorized)`.\n\nSo \"a capability may authorise minting an ODT entry without privilege\" is not a proposal to attack -- it is shipped behaviour, and the authority it grants (any id, any Base, any Length, any Perms, slot takeover, policy erase) is strictly WIDER than anything CARVE-v1 asks for. The real defect in CARVE-v1 is therefore not that it crosses the privilege line; it is that the ODA is granted once, explicitly, by M-mode, and revoked the same way, whereas PERM_CARVE is acquired by guessing an integer and cannot be revoked at all.\n\nTwo further sub-mechanisms of my attack fire today, independent of carve: (i) veda.bind.notrap is a free namespace-enumeration oracle (veda_bind_insts.sail:363-371, soft-fail with RETIRE_SUCCESS, no trap); (ii) the ODA is never saved, cleared or narrowed on any boundary -- grepping veda_oda across all six Veda sail files gives hits only in veda_regs.sail (declaration/reset), veda_cap_insts.sail:906-911 (OSpecialRW) and veda_ocl_insts.sail (the gate), and zero in veda_bind_insts.sail, so a U-mode compartment entered via OCInvoke inherits it unsheddably. That is DESIGN_07 R34 item 3 (DESIGN_07:851-853), already recorded and accepted.",
        "conditions": [
          "Child inherits the parent's owner_domain instead of resetting to VEDA_DOMAIN_ANY -- this is the one-line repair for the primary kill, and it works only because narrowing the arena is then one privileged act per ARENA, not per element",
          "Child Object_ID is DERIVED arithmetically (arena_id_base + element index) rather than taken from a GPR, so carve can only ever touch slots the arena already owns -- this is what dissolves the oracle-vs-clobber dilemma and is the only variant preserving DESIGN_07 R1's own one-ODT-write cost claim",
          "Carve authority lives in the ODT ENTRY as a non-inherited bit, not as a Perms bit, since Bind re-mints Perms from the entry verbatim (veda_regs.sail:982-983)",
          "PERM_CARVE / the entry carve bit is stripped from the child, or the carve tree is unbounded and recursive",
          "free() must not be privileged, or the cost argument fails on the free half -- ODT-Destroy's gate at veda_ocl_insts.sail:753 is untouched by CARVE-v1",
          "A revocation path for carve authority exists that does not bump the arena's generation -- today no instruction can clear a permission bit on a live entry",
          "Rev-B sweeping reclamation lands first: DESIGN_08:203-206 makes it a hard prerequisite, and an unprivileged carve/free loop otherwise saturates the 24-bit generation and permanently retires slots (veda_ocl_insts.sail:784-786, :670-671)",
          "Ship it PRIVILEGED first -- the intra-slab UAF class closes by virtue of per-element Object_IDs, which is a property of the MECHANISM, not of who mints them, so the whole security win is available without touching the privilege boundary at all"
        ],
        "notes": "Read-only throughout. Nothing modified, built or run; /home/prabhu/makerchip/rva23-core never touched.\n\nFiles read: /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/{veda_bind_insts,veda_ocl_insts,veda_cap_insts,veda_regs,veda_types}.sail; /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv; /home/prabhu/veda-core-sindhu/veda-core/rtl/sim/veda_smoke_m11.S; /home/prabhu/veda-core-sindhu/veda-core/sail_tests/{vc_umode_compartment_basic,vc_scheduler_cooperative_yield}.S; /home/prabhu/veda-core/design/DESIGN_07_ROBUSTNESS_AND_SECURITY_HARDENING.md.\n\nTHE ONE FINDING THE OTHER SWEEPS MISSED, and the reason my verdict differs in kind from theirs: all three prior sweeps treated \"an unprivileged instruction writes the ODT\" as the novel danger CARVE-v1 introduces. It is not novel and it is not introduced -- rtl/sim/veda_smoke_m11.S:40-49 is a shipped test that drops to U-mode and performs a full ODT-Populate through the ODA, minting seal/unseal authority. The authority sweep came closest (it censused the gates and saw the OR) but still concluded that CARVE-v1 \"inverts\" the model; in fact the model was already inverted by design in Milestone 11, and CARVE-v1's authority is strictly narrower than what U-mode can already do with an ODA. The crux is therefore misframed, and reframing it changes what must be fixed: not the privilege boundary, but GRANT PROVENANCE and REVOCABILITY.\n\nI also re-verified the architect's four stated facts and they all hold (veda_cap_insts.sail:232/236/238 for CSetBounds temporal inertness; veda_ocl_insts.sail:784-786 for the destroy bump/saturate/retire; veda_bind_insts.sail:181,289 plus veda_core.tlv:4833-4846 for the free-and-fail-closed 0b11 slot; veda_types.sail:229-231 for PERM_SET_CID = 11 and bits 13-15 spare).\n\nI deliberately did NOT re-report the mode-0b11 Sail/RTL trap-ordering divergence the authority sweep found. I re-read the path and it is real -- Sail's Illegal_Instruction arm at veda_bind_insts.sail:289 sits after the region gate at :211, the domain gate at :284 and the residency gate at :285 -- but it is that sweep's finding, not mine, and it is independent of R1's merits.\n\nOne thing I could not settle, same as the other readers: CARVE-v1 does not fix whether the parent is rd or a second operand. My primary attack is insensitive to that choice; the oracle-vs-clobber dilemma is not, and it collapses entirely under a derived child id."
      },
      {
        "verdict": "REFUTED",
        "strongest_attack": "HOT-SLAB-SLOT GENERATION EXHAUSTION -> PERMANENT, UNPRIVILEGED SLOT DEATH.\n\nCARVE-v1 wires free() = ODT-Destroy of the child (DESIGN_07:57-59). Destroy ALWAYS bumps the child's 24-bit generation: veda_ocl_insts.sail:784 \"let new_generation : bits(24) = if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1;\". On the cycle it leaves saturated, the next Destroy retires the slot: :785 \"let new_retired : bool = old_entry.retired | (old_entry.generation == 0xffffff);\". \"retired\" is monotone -- written false ONLY in the empty-entry template (veda_types.sail:393) and reset seeds (veda_regs.sail:1154..1386); NO instruction clears it (every runtime writer ORs new_retired or carries old_entry.retired verbatim: veda_ocl_insts.sail:707,793,902,1008,1051,1173,1249). Once retired, carve/Populate on the slot is Illegal_Instruction FOREVER: veda_ocl_insts.sail:671 \"if old_entry.retired then Illegal_Instruction()\", and its backing memory dies with it (runtime states the coupling verbatim: veda_rt.h:13-19, \"once a slot's generation counter is exhausted ... that slot's backing memory is permanently unusable too, not just its Object_ID\"). RTL mirrors exactly: veda_core.tlv:2730-2731 ($veda_odtpd_new_gen / $veda_odtpd_new_retired), refusal folded into $veda_odt_populate_violation at :2610, retired read at :2264.\n\nTHE CATEGORY ERROR (this is the refutation): the generation counter is the machine's TEMPORAL-SAFETY primitive (veda_types.sail:314-333 -- each Destroy must mint a distinct generation so a stale cap cannot ABA-match), and it was sized for OBJECT lifetimes, where destroying one object 16.7M times is absurd. R1 conscripts that SAME counter as the per-ELEMENT free() primitive, so it now ticks at malloc/free frequency. R1's temporal-safety mechanism and its allocation rate become the SAME finite resource, consumed at element-free rate.\n\nQUANTIFIED with the real constant (generation = bits(24); saturates 0xffffff): 2^24 = 16,777,216 alloc/free cycles on ONE hot slot exhaust it. The exact class DESIGN_07 names as the target (msg_msg / dirty-cred / cross-cache heap-spray) churns a single hot slab slot millions of times. A kmalloc fast path on one CPU sustains ~1-10M allocs/s: one hot slot self-retires in ~1.7 s at 10M/s, ~17 s at 1M/s, ~168 s at 100k/s -- from ordinary, non-adversarial load. An ADVERSARY does it on purpose: a carve/free loop is two unprivileged instructions burning one generation each, permanently retiring any slot it can name -- unprivileged permanent DoS. This is strictly WORSE than the object-per-allocation status quo, which already concedes the 1:1 limit (veda_rt.h:13-19) but ticks generations at object-lifetime rate; CARVE multiplies it, turning one arena into thousands of independently-retireable, individually-DoSable slots.\n\nSECOND RESOURCE FACE (bump variant): if the child local is bump-allocated instead of slot-reused, the direct-mapped ODT (idx = base + local: veda_regs.sail:1075; veda_core.tlv:2146 region_base + local[7:0]) makes resident footprint track HIGH-WATER local, not the live set, so 16.7M lifetime allocations walk local monotonically to the 2^24 wall = 512 MiB ODT ceiling (2^24 x 32 B, DESIGN_08:56-57) AND local-space exhaustion, after which no child carves at all. The 4 KiB/128-entry ODT-granule demand-paging DESIGN_08:56-58 leans on to bound this DOES NOT EXIST in either layer (no granule residency state/fault/cause). Either variant hits the same 2^24 ceiling at malloc/free rate, and the reclaimer that would reset generations or recycle locals (Rev-B) is UNSTARTED: DESIGN_08:203-206, DESIGN_07:104-105.",
        "does_it_fire": "HONEST SPLIT -- the design fails, but the instruction-level attack does NOT fire against the machine as it exists, and I separate the two.\n\nVERIFIED-REACHABLE TODAY (fires on existing machinery, no carve needed): the saturate-then-retire-FOREVER behavior is real and reachable right now through the existing privileged ODT-Destroy. veda_ocl_insts.sail:784 bumps unconditionally, :785 retires on the saturated bump, :671 refuses Populate on a retired slot permanently, and \"retired\" is cleared by nothing (only veda_types.sail:393 + veda_regs.sail reset seeds). RTL identical (veda_core.tlv:2730-2731, :2610, :2264). The Sail suite already drives the true 24-bit boundary by seed injection (vc_gen_retire_neg.S, cited by veda_rt.h:13-19). So \"a slot destroyed to saturation retires permanently and its backing memory is lost\" is a CONFIRMED property of the machine, not a hypothesis.\n\nDOES NOT FIRE against current source (the rate + reachability the attack needs are unbuilt): (1) CARVE-v1 is UNBUILT -- there is no carve instruction; bind mode 0b11 is refused on both layers (veda_bind_insts.sail:289 Illegal_Instruction; veda_core.tlv:4833-4835 decode-refuse), so I cannot emit a carve/free loop. (2) Destroy is PRIVILEGED today -- veda_ocl_insts.sail:753 \"if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()\"; RTL :2621-2623 -- so unprivileged code cannot burn generations at will. (3) The shipped allocator is object-per-allocation (veda_rt.h:6-13), so the counter ticks at object-lifetime frequency where 2^24 is unreachable in practice.\n\nCONCLUSION: the resource attack REFUTES CARVE-v1 AS SPECIFIED -- it turns the 24-bit temporal-safety generation counter into a malloc/free-rate consumable with a hard 2^24 permanent-retirement floor and no reclaimer, self-retiring hot slab slots within seconds-to-minutes of ordinary churn and offering a two-instruction unprivileged permanent-DoS to an adversary. But it CANNOT FIRE as an instruction sequence against the machine as it exists, because carve is unbuilt and Destroy is privileged; only its underlying saturate-retire machinery is verified reachable, and the malloc/free-rate, unprivileged trigger that makes it a live exploit is exactly the part CARVE-v1 would add. Reported as: design-refuting, not currently firing.",
        "conditions": [
          "The refutation holds for CARVE-v1 AS SPECIFIED: free() = ODT-Destroy of the child (DESIGN_07:57-59), which reuses the existing always-bump Destroy path (veda_ocl_insts.sail:784-785).",
          "The permanent-DoS severity requires carve/free to be UNPRIVILEGED as the crux proposes; today Destroy is Machine-or-ODA gated (veda_ocl_insts.sail:753), so a privileged-first CARVE (the authority-lens recommended shape) downgrades this from unprivileged-DoS to allocator-self-inflicted exhaustion -- still a permanent slot-death correctness problem, but not attacker-triggerable.",
          "Both the generation-exhaustion face and the local/footprint face require that no reclaimer runs; Rev-B sweeping revocation that would reset generations or recycle locals is recorded UNSTARTED (DESIGN_08:203-206, DESIGN_07:104-105). If Rev-B lands first and can clear retired / recycle locals, this attack is neutralized -- which is exactly why DESIGN_08:203 calls reclamation a HARD PREREQUISITE for R1, not a co-design partner.",
          "The concrete instruction-level firing is blocked today by two unbuilt facts (no carve instruction; privileged Destroy). The saturate-retire machinery itself IS verified reachable at source via existing privileged Destroy (veda_ocl_insts.sail:671,784-785; veda_core.tlv:2730-2731)."
        ],
        "notes": "Resource lens, read-only; nothing built or run; rva23-core untouched.\n\nConstants used, all re-verified at source: generation = bits(24) saturating at 0xffffff (veda_types.sail generation field + veda_ocl_insts.sail:784); 2^24 = 16,777,216 destroy cycles to permanent retirement; ODT entry = 32 bytes (veda_core.tlv:108, DESIGN_08:56-57); per-region ceiling 2^24 x 32 B = 512 MiB; local = 24 bits, ~16.7M objects/domain (DESIGN_08:42); direct-mapped index idx = base + local (veda_regs.sail:1075; veda_core.tlv:2146).\n\nThis finding is consistent with, and sharpens, the authority-lens JUDGMENT and the id-space grounding already in the brief (both flag exhaustion) -- my contribution is the QUANTIFIED rate argument and the category error: R1 makes the temporal-safety counter and the allocation clock the same finite resource, so the very mechanism that closes intra-slab UAF is consumed at the frequency of the workload it is meant to protect. The 16.7M ceiling that is comfortable at object-lifetime cadence becomes a seconds-to-minutes permanent-death wall at element-free cadence.\n\nOne correction to a number that appears in the brief's own text: the id-space section quotes generation retirement as needing 2^24 reuses (correct) but the runtime SOFTWARE model retires 65536x early -- g_destroy_count is uint8_t wrapping at 256 (veda_rt.c:104-106), i.e. software gives up at 2^8, hardware at 2^24. Stale in the SAFE direction (software over-conservative), so not a security bug, but any R1 cost model that reads veda_rt.c's retirement rate as the hardware rate is off by 65536.\n\nRecommended mitigation if R1 proceeds: (a) ship privileged-first so free-rate exhaustion is allocator-self-inflicted, not attacker-driven; (b) make Rev-B reclamation a landed prerequisite, not a follow-on, per DESIGN_08:203; (c) decouple slot generation-death from backing-memory-death (the veda_rt.h:13-19 coupling) so an exhausted Object_ID does not permanently forfeit its DRAM."
      },
      {
        "verdict": "REFUTED",
        "strongest_attack": "RE-CARVE THE WINDOW. The attacker never touches a stale capability, so the generation mechanism is irrelevant by construction.\n\nCARVE-v1's crux is that the parent capability IS the authority -- holding a PERM_CARVE capability to an arena authorises minting children inside it, unprivileged. Nothing stops minting a child over a window someone else is already using:\n\n    ; attacker holds (or is one instruction from holding) a carve cap to arena P\n    veda.bind  c2, x20             ; x20 = P; Perms = veda_bind_perms(entry_P), incl PERM_CARVE\n    li         x21, 0xC0           ; byte offset of element 3 -- the VICTIM's LIVE element\n    li         x22, 0x40\n    veda.bind  c3, ..., mode=0b11  ; SLAB-CARVE: fresh child Object_ID over the victim's window\n    ocl.d      x11, c3, x0         ; reads the victim's element -- every check passes\n\nThe arms that would have to refuse, quoted:\n- generation, veda_ocl_insts.sail:83 -- \"else if not(entry.valid) | entry.generation != cap.generation then Err(veda_trap(vcapno_to_vcapidx(capidx), VEDA_CAUSE_TAG_VIOLATION))\". The generation was minted by this very carve, so it matches by construction.\n- bounds, veda_ocl_insts.sail:182 -- \"else if unsigned(offset) + width > unsigned(cap.Length) then Err(... VEDA_CAUSE_BOUNDS_VIOLATION)\". cap.Length is the CHILD's own Length; clause 3 guarantees the child sits inside the parent, so it passes.\n- overlap: NO OVERLAP CHECK EXISTS IN EITHER LAYER. grep for overlap/disjoint/intersect over all six files in /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/ returns one hit, veda_atomic_insts.sail:42, an unrelated comment about spec text. The RTL's only hits (/home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:6414, :6605) are comments about always_ff block structure.\n- cross-entry relation: none. veda_ocl_insts.sail:72 -- \"let entry = odt_lookup(cap.Object_ID);\" reads exactly one entry, the one the capability names.\n\nTHE THEOREM: an unprivileged carve right over an arena is definitionally identical to load/store authority over every byte of that arena, at all times. Clause 6's \"carve-only, not directly dereferenceable\" is vacuous -- carve-only IS dereferenceable, one instruction later, over any sub-window you choose. CARVE-v1 does not partition the arena into elements; it hands every carve-holder the whole arena and relabels the access path.\n\nAnd PERM_CARVE cannot restrict who holds it, for the reason this codebase already recorded verbatim at veda_bind_insts.sail:316-320: \"a capability handed out with store stripped is only ADVISORY, because the holder can re-Bind the name and get a fresh, fully-permissioned one.\" Bind re-derives Perms from the ENTRY -- veda_regs.sail:982-983, \"function veda_bind_perms(e : odt_entry) -> bits(16) = if e.cow then e.Perms & 0xFFF7 else e.Perms\" -- and the arena entry's owner_domain is ANY, so veda_regs.sail:986 \"if owner_domain == VEDA_DOMAIN_ANY then true\" lets anyone re-Bind the arena and re-derive PERM_CARVE.\n\nAN OVERLAP CHECK CANNOT RESCUE IT. (a) Cost: an overlap test is a search across ODT entries. DESIGN_08 Section 6 commits to \"a fixed-shape O(1) operation: one read intra-domain\", DESIGN_08:96-97 names the one-read fast path, and the RTL instruments it with a probe asserting intra-domain bind reads the RT exactly 0 times (veda_core.tlv:2118-2133). A search destroys all of it. (b) It inverts the attack rather than closing it: after free(element 3) the window is unclaimed, so the attacker carves it in the gap before the allocator re-carves it for the victim; the allocator's carve then fails and the attacker owns the element. Hardware has no notion of who SHOULD own a window -- the parent capability is the only authority CARVE-v1 offers, and both parties hold it.\n\nNote this makes free() optional to the attack: the victim's element can be carved over while LIVE. So CARVE-v1 does not merely fail to close temporal safety within the slab, it removes the spatial isolation between slab elements that CSetBounds delegation provides today.",
        "does_it_fire": "HONEST ANSWER, SPLIT THREE WAYS.\n\n(1) THE STRONGEST ATTACK CANNOT FIRE AGAINST THE MACHINE AS IT EXISTS, and I state that plainly. There is no carve instruction to issue: bind mode 0b11 is refused on both layers -- veda_bind_insts.sail:289 \"VEDA_BIND_RESERVED => Illegal_Instruction(),\" and veda_core.tlv:4833-4835 \"custom-0 Bind: the three real modes. Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap\". It is a refutation of the candidate design, not a bug report against the model or RTL. Every premise it rests on IS verified true at source today (no overlap check; per-entry-only dereference checking at veda_ocl_insts.sail:72; Perms re-derived at Bind, veda_regs.sail:982-983; owner_domain born ANY, veda_ocl_insts.sail:742), so it would fire on the day carve lands as specified. Nothing was built or run; this is source-read only.\n\n(2) THE SECOND ATTACK -- RE-BIND THE NAME -- FIRES TODAY, in every component, and defeats CARVE-v1's closure in one unprivileged instruction without any carve:\n\n    cgetobjectid  x10, c1     ; x10 = child Object_ID X. No tag check, no generation check.\n    ; ... victim frees element 3, allocator re-carves it for the victim ...\n    veda.bind     c1, x10     ; unprivileged\n    ocl.d         x11, c1, x0 ; reads the VICTIM's element\n\nEach gate, verified: veda_bind_insts.sail:284 domain gate passes because CARVE-v1 clause 2 borns owner_domain = VEDA_DOMAIN_ANY (veda_ocl_insts.sail:742; veda_types.sail:285) and veda_regs.sail:986 returns true for ANY; :285 residency passes; :222 \"let owner_ok : bool = (e.owner_hart == VEDA_OWNER_UNOWNED) | (e.owner_hart == mhartid[7..0])\" passes on one hart. Then :342-359 installs \"Perms = veda_bind_perms(e)\" (:354), \"generation = e.generation\" (:356) -- the entry's LIVE value -- and \"wCTag(rdno, true)\" (:359). The stale generation is gone; veda_ocl_insts.sail:83 now matches. Bind carries no privilege test at all: veda_bind_insts.sail:190-396 contains no cur_privilege reference, and the RTL mirrors it with no $priv term (veda_core.tlv:2343-2345), installing the same live generation at veda_core.tlv:3234.\n\nCGetObjectID is a free oracle -- veda_cap_insts.sail:139-160, the execute clause is \"let cap = C(capidx);\" then returns cap.Object_ID with NO tag and NO generation check. The file states the attack against itself at :125-132: \"A STALE capability therefore returns a real, currently allocated name belonging to a DIFFERENT incarnation of that slot.\"\n\nBUT I MUST NOT OVERCLAIM THIS AS NEW: the name-is-authority hole is pre-existing at object granularity (task #13, closed by the owner_domain/set.domain gate). What is genuinely NEW and specific to CARVE-v1 is that the only fix -- narrowing owner_domain away from ANY -- is veda.odt.set.domain, gated Machine-or-ODA at veda_ocl_insts.sail:1028. So every carve needs a privileged set.domain per element. CARVE-v1's cost claim was \"carve = one ODT write\"; the real cost is one ODT write plus one privileged set.domain per malloc, plus the still-privileged Destroy per free. The design fails on its own cost argument.\n\n(3) ATTACKS I EXPECTED TO WORK THAT DO NOT FIRE -- reported as not firing, because CARVE-v1's mechanism half is genuinely sound and deserves the credit:\n- Spill with OCS.C, reload later with OCL.C: DOES NOT LAUNDER. veda_types.sail:191 packs \"c.Object_ID @ c.Base @ c.Length @ c.Offset @ c.Perms @ c.otype @ c.generation @ c.flags\" and :200 unpacks \"generation = v[43..20]\" verbatim; the reloaded capability is re-checked at veda_ocl_insts.sail:83 and takes Tag Violation.\n- OCA re-pointing Offset into a freed sibling: REFUSED. veda_cap_insts.sail:64 \"let out_of_range : bool = sum_signed < 0 | sum_signed >= unsigned(cap.Length);\" and :77 \"wCTag(rdno, CTag(capidx) & not(out_of_range) & not(isSealedCap(cap)))\". With the child's Length = element size, OCA genuinely cannot walk from element 3 to element 7. This is CARVE-v1's real, verified win over today's whole-arena capability.\n- Allocator reusing the SAME child Object_ID: REFUSED. Destroy bumps unconditionally, veda_ocl_insts.sail:784 \"let new_generation : bits(24) = if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1;\"; a re-carve of the now-invalid slot preserves that bumped value, :683-685 \"if old_entry.valid then (...) else old_entry.generation\".\n- Generation saturation as a UAF: NOT A UAF, only DoS. Destroy sets retired at :785, and Populate refuses forever at :671 \"if old_entry.retired then Illegal_Instruction()\", so an exhausted slot is dead rather than reallocatable to a new owner.",
        "conditions": [
          "Attack 2 (re-carve) requires bind mode 0b11 to be implemented as CARVE-v1 specifies; today it is fail-closed on both layers (veda_bind_insts.sail:289, veda_core.tlv:4833-4835), so it cannot fire against the current machine.",
          "Attack 2 requires the attacker to hold or obtain a PERM_CARVE capability to the arena. This is not an assumption: Bind re-derives Perms from the entry (veda_regs.sail:982-983) and owner_domain is born ANY (veda_ocl_insts.sail:742), so anyone who can name the arena Object_ID gets one. R43 (task #41, open) makes it cheaper still -- Rebind installs the operand's Object_ID/Base/Length while preserving the destination register's Offset (veda_bind_insts.sail:312-315) with no check that the register named the same object, so any legitimate child capability plus knowledge of the arena name yields the arena capability.",
          "Attack 1 (re-Bind the name) requires the child's owner_domain to remain VEDA_DOMAIN_ANY. CARVE-v1 clause 2 specifies exactly that by importing the R41 Populate reset. Narrowing it closes the attack but costs a privileged veda.odt.set.domain (veda_ocl_insts.sail:1028) per carved element.",
          "Attack 1 assumes a single hart, so owner_ok passes at veda_bind_insts.sail:222. On multi-hart the victim's Bind claims owner_hart first and the attacker's plain Bind takes OWNER_VIOLATION 0x06 -- but the attacker can win the race by binding before the victim, since a carved child is born owner_hart = VEDA_OWNER_UNOWNED.",
          "Nothing was built, run, or modified. Every claim is source-read against the Sail model and the TL-Verilog RTL. rva23-core was never touched."
        ],
        "notes": "WHAT CARVE-v1 GETS RIGHT, STATED FIRST BECAUSE IT IS REAL. The mechanism half works and I could not break it. Walk the canonical attack: attacker holds c1 = {Object_ID X, Base 0x8010_00C0, Length 0x40, generation G, Tag 1} to element 3; free is veda.odt.destroy X, which sets generation G+1 (veda_ocl_insts.sail:784) and valid=false (:786); the allocator re-carves slot X for a different owner; the attacker issues ocl.d. THE CHECK THAT REFUSES IS veda_ocl_insts.sail:83 -- \"else if not(entry.valid) | entry.generation != cap.generation then Err(veda_trap(vcapno_to_vcapidx(capidx), VEDA_CAUSE_TAG_VIOLATION))\" -- reading the entry freshly at :72. G+1 != G, Tag Violation 0x01. That closure holds against all four re-acquisition-free variants I was asked to test (OCA, spill/reload, same-id reuse, saturation), each verified above. Per-element Object_IDs genuinely do close intra-slab UAF FOR HELD CAPABILITIES, and the mechanism is forced rather than chosen, because the recheck compares against odt_lookup(cap.Object_ID) and a generation is therefore per-Object_ID by construction.\n\nWHERE IT DIES IS AUTHORITY, NOT MECHANISM. Both attacks share one shape: the attacker does not use the stale capability. CARVE-v1 staleifies HELD CAPABILITIES and does nothing about RE-ACQUISITION, and on this machine re-acquisition is unprivileged, one instruction, and needs only a 44-bit integer. Making the mint unprivileged converts the arena capability from a naming convenience into ambient byte authority over the whole arena.\n\nCONSEQUENCE FOR THE DESIGN. Ship carve PRIVILEGED first. The security win R1 wants -- per-element temporal safety -- is a property of per-element Object_IDs, not of who mints them, and it is fully available with the privilege boundary untouched: the check at veda_ocl_insts.sail:83 does not care who wrote the entry. That separates a provable win from an unproven cost argument, and the cost argument is separately weak, because veda_oda_authorized() carries no privilege term (veda_ocl_insts.sail:632-633) and the shipped runtime already does one-instruction malloc/free with no trap (/home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt.c:69-95).\n\nIF UNPRIVILEGED CARVE IS EVER REVISITED, three things must exist first, none of which do: (a) carve authority as a non-inherited ENTRY property, not a Perms bit, because veda_bind_perms(e) re-derives Perms from the entry and makes any Perms-bit answer advisory; (b) a child born owner_domain = the carver's domain rather than ANY, which needs the region-grant authority DESIGN_08 Section 9 names and leaves open (\"a bug letting domain A mint into region B is a compartment-escape of the Milestone-19 class\"); (c) an answer to window ownership that does not require a cross-entry search, since the direct-mapped ODT and DESIGN_08's fixed-read-count guarantee forbid one.\n\nONE THING TO FIX INDEPENDENTLY OF R1, on the exact slot R1 wants. I confirm the divergence an earlier reader found on bind mode 0b11: the RTL refuses at decode (mcause 0x02, veda_core.tlv:4846), but Sail reaches \"VEDA_BIND_RESERVED => Illegal_Instruction()\" only at veda_bind_insts.sail:289, AFTER the region gate at :211, the domain gate at :284 and the residency gate at :285. On a non-resident region Sail gives REGION_FAULT instead. A reserved encoding whose refusal cause depends on another domain's paging state is an ODT-state oracle for unprivileged code, and R1 must settle it before claiming the slot.\n\nAlso confirmed in passing while reading entry layout for a possible carve field: the RTL entry-sizing comment at veda_core.tlv:102-108 claims \"26 B used, 6 spare after RTL-6\", but ODT_OFF_OWNER_DOMAIN=26 (:148) and ODT_OFF_COW=29 (:160) leave only bytes 30-31 -- 2 spare, not 6. Any R1 entry-field work will read that comment and be misled."
      },
      {
        "verdict": "REFUTED",
        "strongest_attack": "THE PAIR THAT CONTRADICTS: R11(b)'s executing-object pin vs CARVE-v1 clause 2.\n\nThe pin's PREDICATE is an identity comparison. Sail: `veda_object_is_executing(object_id) = (object_id == veda_pcc_object) | (veda_trap_depth != zeros() & object_id == veda_mepcc_object)` (/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_regs.sail:990-992). RTL: a region + local[7:0] SLOT comparison, `$veda_object_slot_is_pcc = ($veda_object_id[43:24] == $veda_pcc_object[43:24]) && ($veda_object_id[7:0] == $veda_pcc_object[7:0]);` (/home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:2573-2574). The GUARANTEE it was written to deliver is a MEMORY property -- its own test names it in the title line: \"R11(b) ... -- YOU MAY NOT EVICT THE CODE YOU ARE RUNNING\" (/home/prabhu/veda-core-sindhu/veda-core/sail_tests/vc_r11b_executing_pin_neg.S:4). Identity and memory coincide only under an invariant this machine never states and never checks: that no two ODT entries describe overlapping memory. CARVE-v1 is DEFINED as the instruction that violates that invariant, once per allocation. Grep confirms the invariant is absent: `overlap|disjoint|intersect` over model/extensions/Veda/*.sail returns only veda_atomic_insts.sail:42 (unrelated prose) and over veda_core.tlv only :4626, :5239, :6414, :6605 (all about decode/always_ff blocks, none about Base ranges).\n\nINSTRUCTION-LEVEL SCENARIO -- a JIT/executable slab, exactly the shape R1 exists to enable. Arena P = Object_ID 180, Base = &jit_arena, Length 0x1000, Perms = EXECUTE|INVOKE|CARVE, populated once by the privileged allocator.\n\n  1. veda.bind c1, x1   (x1 = 180)   -> c1 carries P's ENTRY Perms verbatim, incl. PERM_EXECUTE\n                                        (veda_regs.sail:982-983 `veda_bind_perms`)\n  2. veda.carve c2, c1, off=0x200, len=0x100\n                                     -> child C (a distinct local, say 0x100): entry {Base\n                                        &jit_arena+0x200, Length 0x100, Perms inherited,\n                                        fresh generation, resident=true, owner_hart=UNOWNED,\n                                        owner_domain=ANY, cow=false}; c2 names C\n  3. csealentry c3, c2               -> NO authority operand at all; veda_cap_insts.sail:386-388\n                                        calls sentry-minting \"an ambient monotonic action,\n                                        requiring no additional permission than to have a\n                                        capability bearing Permit_Execute\" (body at :406-427)\n  4. ocreturn c3                     -> passes tag / sentry-otype / PERM_EXECUTE / RT-residency /\n                                        veda_code_object_check, then commits\n                                        `veda_pcc_base = cs1.Base; veda_pcc_length = cs1.Length;`\n                                        (veda_cap_insts.sail:823-824) and\n                                        `veda_pcc_object = cs1.Object_ID;` (:831)\n\nSteps 3 and 4 are both UNPRIVILEGED. The core now fetches bytes that belong to BOTH P and C, while PCC names only C.\n\n  5. Any principal holding Machine or a tagged ODA -- R11(b)'s OWN stated adversary, verbatim:\n     \"An authority refused a Destroy on the running compartment's code object could simply\n     re-populate it instead\" (veda_ocl_insts.sail:648-652) -- executes:\n\n         veda.odt.page.out rd, xP        (xP = 180)\n\n     - pin, veda_ocl_insts.sail:1115 `if veda_object_is_executing(object_id)`:\n       180 == veda_pcc_object (0x100)? NO. PIN PASSES.\n     - `not(old_entry.valid) | not(old_entry.resident)` -> passes\n     - R38(b) `else if old_entry.cow then Illegal_Instruction()` (:1158) -> P is not cow, passes\n     - saturation guard -> passes\n     RETIRES. The machine has just told the pager that the frame the core is fetching from\n     is free to reclaim.\n     RTL identical: `$veda_odt_page_out_refusal` (veda_core.tlv:2652) takes\n     `$veda_object_is_executing`, whose slot compare is 180 vs local[7:0]=0x00 -> false.\n\n  6. Fetch never notices. `ext_fetch_check_pc` compares PC against veda_pcc_base/length ONLY --\n     /home/prabhu/veda-core-sail-riscv/model/postlude/step_ext.sail:85-90 -- and R11's own\n     Base-consumer table records \"instruction fetch (PC vs PCC)   NO\"\n     (veda_cap_insts.sail:493). Execute-after-free, by the exact route the RTL comment says\n     this increment exists to close (veda_core.tlv:2564-2566).\n\n  The same pin-pass admits `veda.odt.destroy 180` (:776) and `veda.odt.populate(.fast) 180`\n  (:659 / :873) -- i.e. ALL THREE eviction paths R11(b) enumerated.\n\nWHY EXTENDING THE PIN'S CONSUMER SET DOES NOT FIX IT. R11(b) defines its own completeness as INSTRUCTION completeness: \"The complete consumer set of this predicate is answerable exactly -- every instruction that can change an entry's identity or backing -- and it is Populate, Populate-Fast, Destroy and page-out\" (veda_ocl_insts.sail:653-658). Both prior readers concluded \"carve belongs in that set\". That is necessary and INSUFFICIENT: adding carve pins the CHILD, and the break above is the PARENT unpinned while the child executes (with the mirror-image break -- child evictable while the parent executes -- equally open). The defect is in the PREDICATE. A sound pin under carve must ask \"does this entry's [Base, Base+Length) intersect [veda_pcc_base, +veda_pcc_length)?\" -- a range query over an object-indexed table with no reverse index, no overlap invariant, and (DESIGN_02) a standing rule that \"there is exactly one indirection structure in the machine, and it is object-indexed.\"\n\nTHREE FURTHER CONTRADICTING PAIRS, each verified:\n\n(a) R38 + R38(b) copy-on-write. `veda.odt.set.cow` writes EXACTLY ONE entry and deliberately does not bump the generation (veda_ocl_insts.sail:998-1012, and its own comment at :1042-1044: \"the generation is NOT bumped -- that is the entire purpose of this instruction\"). The COW arm reads the DEREFERENCED capability's own entry: `else if need_store & entry.cow` (:256) with `let entry = odt_lookup(cap.Object_ID);` (:72). A child born cow=false therefore writes the shared parent bytes with no 0x0C and no copy -- fork() over a carved arena silently shares. Worse for R38(b): its refusal tests the PARENT's cow bit (:1158), so page-out of any CHILD of a cow arena is permitted, and the child is where the split entitlement now lives. R38(b)'s own successor note already concedes the shape of the fix -- the entitlement \"must live in the ENTRY ... A per-(domain, object) entitlement needs a table this design does not have\" (veda_ocl_insts.sail:1152-1156) -- and carve multiplies the rows that missing table would need by the slab element count.\n\n(b) owner_hart (Milestone 12), untouched by every prior reader. The machine's ONLY cross-hart exclusion is `let owner_ok : bool = (e.owner_hart == VEDA_OWNER_UNOWNED) | (e.owner_hart == mhartid[7..0]);` (veda_bind_insts.sail:222), enforced as VEDA_CAUSE_OWNER_VIOLATION. Populate/Populate-Fast reset `owner_hart = VEDA_OWNER_UNOWNED` (veda_ocl_insts.sail:706, :901; sentinel 0xFF at veda_types.sail:275), and CARVE-v1 clause 2 says the child resets the Populate way. So hart 1, refused Bind(P) as a foreign owner, Binds child C ⊂ P and receives read/write into memory hart 0 exclusively owns. This is precisely the hole page-in was written to avoid -- \"Populate hardcodes unowned, so using it as page-in would silently drop hart ownership across every paging cycle -- invisible today with one hart, a real hole under multi-hart\" (veda_ocl_insts.sail:1245-1248) -- reintroduced once per allocation. Repairable by inheriting owner_hart; the design does not say so.\n\n(c) R10 domain identity. OCInvoke's soundness sentence is \"the domain being ENTERED is the region field of the code capability's own Object_ID -- unforgeable, because only a residency-gated Bind ever mints a capability with a given Object_ID\" (veda_cap_insts.sail:554-556). CARVE-v1 clause 4 is a SECOND Object_ID-minting path that is not Bind and carries none of Bind's four gates (region residency veda_bind_insts.sail:211; domain :284; object residency :285; owner_hart :222). Since `veda_bind_domain_ok` measures the caller's domain as `veda_pcc_object[43..24]` (veda_regs.sail:988), and steps 3-4 above install a carve-minted name into veda_pcc_object, carve becomes an input to compartment identity. HARD OBLIGATION: child.region must equal parent.region structurally, in the ENCODING, not by a check -- every existing ODT writer takes the 44-bit id verbatim from a GPR (veda_ocl_insts.sail:641-642), and a GPR-supplied child id makes the region forgeable, which is DESIGN_08 Section 9(b)'s compartment escape.",
        "does_it_fire": "TWO ANSWERS, and the honest one first.\n\nTHE HEADLINE ATTACK CANNOT FIRE TODAY. There is no carve instruction. Bind mode 0b11 is `VEDA_BIND_RESERVED => Illegal_Instruction(),` (/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_bind_insts.sail:289) and is absent from the RTL decode (/home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:4833-4846). Nothing in either layer creates a second Object_ID over a sub-range of a live object at unprivileged level. So the R11(b) refutation above is a refutation of an UNBUILT design, not a live exploit, and I report it as such.\n\nBUT A WEAKENED FORM OF THE SAME PAIR FIRES ON THE MACHINE AS IT EXISTS, because the aliasing invariant the pin silently depends on is ALREADY unenforced. Populate and Populate-Fast take Base verbatim, with no range check and no disjointness check: `Base = zero_extend(descriptor[63..32]),` (/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_ocl_insts.sail:697) and `Base = base,` (:890). The RTL mirrors it: `$veda_odtpd_new_base[55:0] = $is_veda_odt_populate ? {24'b0, $rs2_data[63:32]} : ...` (/home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:2750), written into the entry at :6496-6502; the ONLY in-range test in the whole file is `$veda_baseisa_store_in_range` (:5924), an unrelated base-ISA tag-store path.\n\nConcrete reproduction, in this repo's own test idiom (vc_r11b_executing_pin_neg.S's encodings), executed INSIDE a compartment entered on object 180 so that veda_pcc_object == 180:\n\n    li   x6, 0xFFFFFFFFFF\n    slli x6, x6, 16\n    ori  x6, x6, 0x0402                    # Permit_Execute|Permit_Invoke\n    csrw 0x7C4, x6\n    la   x7, compartment                   # the SAME Base object 180 already holds\n    li   x1, 190                           # a fresh, UNRELATED name\n    .insn r 0x0b, 0x0, 0x04, x5, x1, x7    # populate.fast 190  -- pin at\n                                           #   veda_ocl_insts.sail:873 tests 190 == 180 -> NO\n                                           #   RETIRES. Object 190 now describes the running\n                                           #   compartment's exact code bytes.\n    li   x1, 190\n    .insn r 0x0b, 0x1, 0x05, x5, x1, x0    # page.out 190 -- pin at :1115 passes,\n                                           #   valid+resident pass, cow false, gen unsaturated\n                                           #   RETIRES.\n\nBoth retire on both layers (RTL slot compare 190 vs 180, veda_core.tlv:2573-2574, is likewise false). R11(b) has just permitted the eviction of a descriptor for the code the core is executing.\n\nTHE EXISTING TEST CANNOT SEE IT, on the specific point it was built to control for. vc_r11b_executing_pin_neg.S PART D is the over-refusal control -- \"an UNRELATED live object is still evictable\" -- and its object 183 is populated at `victim_pad` (:70-72), a disjoint label; the page-out that must succeed is at :158-160. So the corpus tests the disjoint case and the same-name case, and nothing in between. No test in sail_tests/, rtl/sim/ or difftest/probes/ populates a second object at an already-live Base.\n\nHONEST LIMITS ON SEVERITY, stated rather than glossed:\n- The in-tree page-out only clears object 190's `resident` bit and bumps its generation. Object 180 is untouched and execution continues correctly. There is no pager in this repository to act on the answer, so nothing is corrupted today. What is wrong today is the ARCHITECTURAL STATEMENT -- and the RTL sets that standard itself, for the converse case: \"A silent refusal tells a pager the eviction happened when it did not -- which is worse than either trapping or succeeding, because the pager then reuses memory it does not own\" (veda_core.tlv:2600-2603). Here the machine tells a pager an eviction is permitted on memory the core owns; the same argument applies with the sign flipped.\n- The actor holds Machine or a tagged ODA. That is not an escape hatch: it is exactly R11(b)'s stated adversary (veda_ocl_insts.sail:648-652), and R11(b) set its own bar at \"Refusing Destroy while leaving Populate open would not be a partial fix, it would be a BYPASSABLE one\" (veda_core.tlv:2545-2547). An aliasing Populate is one instruction, and it restores the bypass at a different name.\n- I did NOT run this. The Sail model cannot be rebuilt on this machine (recorded as task #21) and this pass was read-only by instruction. The claim is a source-level derivation over every arm of both chains, not a measured result, and it should be measured before it is recorded as a finding.",
        "conditions": [
          "CARVE-v1 must be encoded so that child.region == parent.region STRUCTURALLY -- derived in the datapath, never taken from a GPR. Every existing ODT writer reads the 44-bit id verbatim from a GPR (veda_ocl_insts.sail:641-642, :758-759, :855-856, :991-992, :1031-1032); a GPR-supplied child id makes veda_pcc_object's region field -- the whole of compartment identity, veda_regs.sail:988 -- forgeable by an unprivileged instruction.",
          "The executing-object pin must be re-specified as a WINDOW test against [veda_pcc_base, veda_pcc_base+veda_pcc_length) rather than an identity test, on both layers, before any carve lands. Adding carve to the pin's four-instruction consumer set (veda_ocl_insts.sail:653-658) is necessary and provably insufficient -- it pins the child and leaves the parent open.",
          "A carve child must inherit owner_hart from the parent, not reset to VEDA_OWNER_UNOWNED the way Populate does (veda_ocl_insts.sail:706, :901). Otherwise one carve voids the arena's hart exclusivity (veda_bind_insts.sail:222) -- the machine's only cross-hart exclusion.",
          "A carve child must inherit cow from the parent AND set.cow must reach children, or copy-on-write is bypassed per allocation (veda_ocl_insts.sail:256 reads the child's own entry via :72; set.cow writes exactly one entry, :998-1012). Since no parent->child link exists in the 32-byte entry and DESIGN_02:307-311 refuses bit-packing, this needs the per-(domain,object) table R38(b) already named as unbuilt (veda_ocl_insts.sail:1152-1156).",
          "Carve must consult the parent's residency (veda_ocl_insts.sail:225 does not apply -- carve is not a memory access) and the parent's generation, or it mints children from a stale descriptor.",
          "The two layers' pin identity notions differ by construction -- Sail by NAME (veda_regs.sail:990-992), RTL by SLOT (veda_core.tlv:2573-2574, deliberately, per :2548-2566). Any carve-era pin must be specified in terms both layers can express, or the difftest will agree while the property differs.",
          "Ship carve PRIVILEGED first if at all. Every finding above is about what carve creates, not about who creates it -- the intra-slab UAF class closes on per-element Object_IDs regardless of the minter -- and the privileged version leaves the R11(b), R38, owner_hart and R10 pairs as the only open work rather than compounding them with an authority change."
        ],
        "notes": "SCOPE AND METHOD. Read-only throughout: only Read/sed/grep. Nothing modified, nothing built, nothing run. /home/prabhu/makerchip/rva23-core was never touched.\n\nWHAT I RE-VERIFIED FROM THE PRIOR READERS AND FOUND TRUE, so it is not re-litigated above: Bind mints Perms from the ENTRY (veda_regs.sail:982-983), so PERM_CARVE is advisory exactly as PERM_STORE was; R41 borns every object owner_domain = VEDA_DOMAIN_ANY (veda_ocl_insts.sail:742, predicate veda_regs.sail:985-988); ODT-Destroy stays Machine-or-ODA gated (veda_ocl_insts.sail:753), so CARVE-v1's free() half is still a privileged act; bind mode 0b11 is free (veda_bind_insts.sail:181, :289).\n\nWHAT I COULD NOT ESTABLISH. CARVE-v1 does not fix whether the parent is rd (Rebind-style) or a second capability operand. Finding (c) above changes shape depending on that choice, and so does the containment check: if the parent is a capability operand, containment must be tested against the parent CAPABILITY's window, since CSetBounds narrows spatially while keeping Object_ID and generation (veda_cap_insts.sail:232, :238) -- validating against odt_lookup(cap.Object_ID).Length instead would let a narrowed delegated sub-capability carve anywhere in the full arena.\n\nONE THING THE HEADLINE FINDING IMPLIES THAT IS WORTH SEPARATING FROM R1. Even with carve abandoned, R11(b)'s refusal is name-scoped while the authority it refuses is memory-scoped, and Populate's unvalidated Base already breaks the correspondence (see the reproduction in the fire-check). That is an independent item, in the same family as the recorded-but-open Rev-E (\"ODT-Populate writes a software-supplied raw Base with zero range validation\"), and it is the reason CARVE-v1 cannot be built on this pin as it stands. If Rev-E lands as a legal-backing-region check, it narrows but does not close this: two objects may still alias inside the legal region. The closing invariant is entry disjointness, or a window-based pin.\n\nON THE VERDICT. I default to REFUTED as instructed, and I want to be precise about what is refuted. CARVE-v1's MECHANISM (a per-element Object_ID, so the existing per-dereference generation re-check does the temporal work) survives every attack in this pass -- it is forced, not chosen, because the check compares entry.generation against cap.generation on odt_lookup(cap.Object_ID). What is refuted is CARVE-v1 AS SPECIFIED, and specifically its composition with the machine's existing mechanisms: the executing-object pin, copy-on-write, owner_hart, and the unforgeability premise under R10. Those four were all established for a world in which one byte of memory belongs to at most one live ODT entry, and clause 2 abolishes that world without replacing any of them."
      },
      {
        "verdict": "REFUTED",
        "strongest_attack": "PERM_CARVE OVER A RANGE IS A STRICT SUPERSET OF LOAD|STORE OVER THAT RANGE, SO CLAUSE 6 -- R1's OWN SELF-DECLARED LOAD-BEARING PART -- IS AN AMPLIFICATION, AND IT RECONSTRUCTS THE EXACT UAF IT EXISTS TO CLOSE.\n\nDESIGN_07_ROBUSTNESS_AND_SECURITY_HARDENING.md:63-65 states R1's whole security claim and pins it on clause 6: \"Closes the class? Yes, by construction, given the carve-only parent Permit ... The load-bearing new parts are (a) minting a fresh Object_ID + generation on carve and (b) the carve-only Permit.\" Clause 6 removes PERM_LOAD/PERM_STORE from the arena's ENTRY so \"every actual byte access must go through a child ID\". But carve is a rights-CREATING operation: it writes a fresh ODT entry whose Perms must contain load/store or the mechanism is inert (clause 2 + clause 4). Therefore holding PERM_CARVE over [a,b) confers, in ONE instruction, load/store over any sub-range of [a,b) -- plus FRESH POLICY, since clause 2 imports R41's resets (veda_ocl_insts.sail:742-743 `owner_domain = VEDA_DOMAIN_ANY,` / `cow = false,`; mirrored at :802-803 and :912-913). \"Carve-only, not directly dereferenceable\" reads as a restriction and functions as a strict escalation.\n\nINSTRUCTION-LEVEL SCENARIO. kmalloc-64 arena A, entry Perms = PERM_CARVE only. Allocator carves child C3 over element 3 (Base = A.Base+192, Len 64) and hands it to a victim.\n  1. Attacker learns A's name. `cgetobjectid x1, cA` is ungated -- VEDA_CAPQUERY has no privilege test in its encdec (veda_cap_insts.sail:139-141) or its execute clause (:143-166), and the file's own comment at :117-123 records the decision: \"DISCLOSING THE NAME IS SAFE ... Isolation here rests on capability non-possession, not on name secrecy.\" If the attacker holds no arena capability at all, he enumerates instead: `veda.bind.notrap` soft-fails without trapping on a missing object (veda_bind_insts.sail:376-378 `} else if mode == VEDA_BIND_NOTRAP then { wC(rdno, zero_capability); wCTag(rdno, false);`), the domain gate passes unconditionally at the default (veda_regs.sail:986 `if owner_domain == VEDA_DOMAIN_ANY then true`), and the region gate never fires in your own region (veda_regs.sail:1055-1057 `if region == veda_current_region then true`). That is a trap-free scan of the entire local namespace.\n  2. `veda.bind cA2, x1` re-mints the arena capability with Perms taken VERBATIM from the entry -- veda_bind_insts.sail:354 `Perms = veda_bind_perms(e),` into veda_regs.sail:982-983 `function veda_bind_perms(e : odt_entry) -> bits(16) = if e.cow then e.Perms & 0xFFF7 else e.Perms`. PERM_CARVE lives in the entry (it must, per clause 1), so every Bind of the arena name hands out carve authority.\n  3. `carve cX, cA2, offset=192, len=64` -- a SECOND child over element 3's bytes, different Object_ID, own entry, own generation, cow=false, owner_domain=ANY.\n  4. Victim frees C3. ODT-Destroy bumps ONLY C3's counter (veda_ocl_insts.sail:784 `let new_generation : bits(24) = if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1;`). cX is untouched.\n  5. `veda.ocs.d cX, ...` writes the freed element. Every arm of veda_check_access passes, because every arm reads cX's OWN entry: `let entry = odt_lookup(cap.Object_ID);` (veda_ocl_insts.sail:72), valid+generation :83, bounds against the capability's cached Length :181, residency :225, and cow :256 `else if need_store & entry.cow then Err(... VEDA_CAUSE_COW_FAULT)` -- cX's entry has cow=false, so the copy-on-write fault never fires either. NOTHING relates two entries: `grep -n \"overlap\\|disjoint\"` over model/extensions/Veda/*.sail, veda_core.tlv and VEDA_CORE_SPEC.md returns zero bounds-disjointness checks (the four RTL hits at :4626, :5239, :6414, :6605 are about always_ff block overlap; the SPEC has none).\n\nThe attacker needs no stale pointer, no race and no groom -- step 3 can precede the victim's allocation entirely. And the machine's own already-adopted doctrine names this failure verbatim: veda_cap_insts.sail:955-959, the reason CAndPerm exists, says a read-only view \"forces a SEPARATE Object_ID aliasing the same Base -- which breaks temporal safety (destroying one ID does not revoke the alias). CAndPerm derives a weaker capability to the SAME live object (no new Object_ID, no aliasing), so a later ODT-Destroy still revokes every view.\" CARVE-v1 mints one such alias per allocation and makes minting them unprivileged.\n\nThe trilemma has no fourth branch, and I checked by enumerating every rights source in the ISA. Rights are CREATED in exactly one place -- the ODT entry, via Bind (veda_regs.sail:982-983) -- and every other derivation is monotone-clear-only: CAndPerm is `cap.Perms & mask[15..0]` (veda_cap_insts.sail:981), CSetBounds copies Perms verbatim and narrows only spatially (veda_cap_insts.sail:236 `Perms = cap.Perms,` beside :232 `Object_ID = cap.Object_ID,` and :238 `generation = cap.generation,`). CARVE-v1 adds a SECOND rights-creation site and puts it in unprivileged hands. That, not the allocator and not the footprint, is the whole design.",
        "does_it_fire": "PARTLY, AND I WILL NOT OVERSTATE IT. Steps 3 and 4 have no instruction on this machine: mode 0b11 is fail-closed (veda_bind_insts.sail:289 `VEDA_BIND_RESERVED => Illegal_Instruction(),`; veda_core.tlv:4833-4835 \"Mode 0b11 is deliberately absent\"), so the carve-specific steps CANNOT execute today. Every OTHER step fires today exactly as cited: the ungated name disclosure (veda_cap_insts.sail:143-166), the trap-free enumeration (veda_bind_insts.sail:376-378 with veda_regs.sail:986 and :1055-1057), the verbatim Perms re-mint (veda_regs.sail:982-983 via veda_bind_insts.sail:321 and :354), the per-entry-only dereference chain (veda_ocl_insts.sail:72, 83, 181, 225, 256), and the total absence of any overlap check in both layers and the spec.\n\nWHERE IT DOES FIRE END-TO-END TODAY, AND THIS IS THE FINDING THAT MATTERS: substitute `veda.odt.populate.fast` for `carve` and the whole attack runs on the machine as it exists, at M-mode or with the ODA. Populate accepts a software-supplied Base with zero range validation -- veda_ocl_insts.sail:895 `let base : bits(56) = base_gpr64[55..0];` written straight through at :890, and the compact form at :719-720 -- so an authority can mint a second Object_ID whose Base lands inside a live object's window, born `cow = false` and `owner_domain = VEDA_DOMAIN_ANY` (:912-913). No check anywhere refuses it. The ONLY thing standing between today's machine and the aliasing attack is the privilege gate at veda_ocl_insts.sail:636 and :850 (`if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`), mirrored in the RTL at veda_core.tlv:2610-2612.\n\nSo the honest statement is sharper than \"my attack does not fire\". CARVE-v1's crux -- \"THE PARENT CAPABILITY IS THE AUTHORITY\", make the mint unprivileged -- is a proposal to remove the sole remaining defence against an attack this machine is otherwise already fully open to. The overlap-alias primitive is built, unchecked on both layers, and gated only by privilege.\n\nTWO CLAIMS IN THE BRIEF I COULD NOT CONFIRM AS STATED. (1) \"veda_bind_mode 0b11 maps to Illegal_Instruction() on both layers\" is true only for objects that clear three earlier gates: on Sail the reserved arm is reached at veda_bind_insts.sail:289, AFTER the region gate (:211), the domain gate (:284) and the residency gate (:285), while the RTL refuses at decode. The prior authority sweep already reported this; I confirmed the line ordering independently. (2) Clause 2's \"a fresh generation\" is not a real requirement -- see notes.",
        "conditions": [
          "The attack as written needs the carve instruction to exist. Today it runs only in its Populate-substituted form, and only at Machine privilege or with a tagged ODA (veda_ocl_insts.sail:636, :850; veda_core.tlv:2610-2612).",
          "Step 1's enumeration variant needs the attacker to execute in the same region as the arena, or to be in no compartment at all -- veda_bind_domain_ok returns true when veda_pcc_object == VEDA_OBJECT_NONE (veda_regs.sail:987), which is boot code and every trap handler. Cross-region scanning stops at the region gate (veda_bind_insts.sail:211).",
          "Step 2 needs the arena's owner_domain still at VEDA_DOMAIN_ANY. Narrowing it closes step 2 -- but narrowing requires veda.odt.set.domain, which is privileged (veda_ocl_insts.sail:1029), so this defence costs exactly the privileged per-object operation R1 exists to eliminate, now once per carved child rather than once per arena.",
          "The COW-bypass corollary needs a pre-cow parent capability carrying carve authority, which R38's fork flow is precisely the moment that exists.",
          "My whole-part-3 argument assumes the child's Perms must include load/store. If they do not, the mechanism is inert and no byte is ever reachable through a child -- which is the other horn, not an escape."
        ],
        "notes": "MY LENS -- WHICH PARTS ARE LOAD-BEARING. The design is much bigger than it needs to be, and four of its five moving parts are decoration propping each other up.\n\n(1) NEW BIND MODE 0b11 vs A NEW INSTRUCTION -- NOT LOAD-BEARING, AND ATTACHED TO THE WRONG PARENT. Bind's encoding is one GPR and one capability register with no immediate: veda_bind_insts.sail:186 `0b0000000000 @ mode @ encdec_reg(rs1) @ 0b101 @ 0b0 @ encdec_vcap(rd) @ 0b0001011`. Carve needs {parent cap, offset, length, dest cap, child id}. It does not fit. But CSetBounds's shape already carries the whole child window: veda_cap_insts.sail:206 `0b0001000 @ encdec_reg(rs2) @ 0b0 @ encdec_vcap(rs1) @ 0b001 @ 0b0 @ encdec_vcap(rd) @ 0b1011011` -- rs2 = length, rs1 = parent, rd = child -- with the offset supplied for free by cap.Offset (veda_cap_insts.sail:234 `Base = cap.Base + zero_extend(cap.Offset),`) and a containment check ALREADY WRITTEN AND ALREADY HARDENED against R18's wraparound: :229 `let window_ok : bool = unsigned(cap.Offset) + unsigned(new_length) <= unsigned(cap.Length);`. R1 attached carve to Bind because Bind is the instruction that touches the ODT; the operand shape it actually needs is CSetBounds's. DROP IT: nothing breaks, and clause 3's containment arrives pre-verified rather than re-derived (and re-derived is where R18's `offset = 0xFFFFFFFFFFFFFFF8` wraparound bug would come back).\n\n(2) PERM_CARVE AS A NEW PERMS BIT -- NOT LOAD-BEARING, AND VACUOUS IN EXACTLY THE CONFIGURATION R1 NEEDS. To carve you must hold a parent capability; the only mint is Bind; Bind's gate is owner_domain (veda_regs.sail:985-988) and it then hands out entry Perms verbatim (:982-983). So for every principal that can Bind the arena -- at the default owner_domain = VEDA_DOMAIN_ANY, everyone -- the PERM_CARVE bit-test never changes the answer, because owner_domain already decided it. PERM_CARVE becomes non-vacuous only for a delegatee across a NARROWED owner_domain, and narrowing costs a privileged veda.odt.set.domain per object (veda_ocl_insts.sail:1029). Vacuous where R1 needs it, useful only where R1 cannot afford it. DROP IT: use owner_domain, which exists, is entry-resident, and is already enforced on both layers.\n\n(3) THE CARVE-ONLY PARENT -- LOAD-BEARING BY R1'S OWN TEXT, AND INVERTED IN EFFECT. This is the scissors and it is the refutation: see strongest_attack. It is also worse than a plain load/store parent, not better. A plain parent with STORE over a cow entry gets bit 3 masked at Bind (veda_regs.sail:982-983) and faults 0x0C on write (veda_ocl_insts.sail:256). A carve-only parent bypasses both, because the child is a different entry born cow=false (clause 2's R41 import, veda_ocl_insts.sail:743). So clause 6 makes the arena strictly MORE dangerous while reading as a restriction. It cannot be dropped (dropping it means an attacker holding the arena reads freed elements directly and the class is not closed at all) and it cannot be kept (keeping it requires the first rights-AMPLIFYING derivation in an ISA whose derivation family is monotone by construction). That is a contradiction in the specification, not a gap in it.\n\n(4) HARDWARE-ALLOCATED vs SOFTWARE-SUPPLIED CHILD ID -- GENUINELY LOAD-BEARING AND GENUINELY UNSOLVED. Prior sweeps established there is no allocator anywhere. One consequence I did not see recorded: under the arithmetic-derivation proposal (child local = {arena tag, element index}), THE CHILD'S NAME LEAKS ITS SIBLINGS' NAMES. `cgetobjectid x1, cChild` is ungated (veda_cap_insts.sail:143-166), `addi x1, x1, 1` names the neighbour, and `veda.bind` mints it with full entry Perms (veda_bind_insts.sail:354 / veda_regs.sail:982-983) because it is born owner_domain = ANY. Derivation therefore hands every element-holder a capability to every sibling -- worse than the arena being dereferenceable. So the ID question is a trilemma too: derived -> sibling forgery; software-supplied -> RTL slot takeover (veda_core.tlv:2549-2562, \"Object_ID 436 and Object_ID 180 land on the same 32 bytes ... neither ODT write arm looks at it\"); hardware-allocated -> no allocator exists and searching breaks DESIGN_08's fixed-shape one-read guarantee.\n\n(5) A FRESH GENERATION -- NOT LOAD-BEARING, AND R1 DOUBLE-COUNTS IT. R1 lists it as load-bearing part (a) alongside the fresh Object_ID, but the deref check only compares entry vs capability -- veda_ocl_insts.sail:83 `entry.generation != cap.generation`, with the capability's copy taken FROM the entry at Bind (veda_bind_insts.sail:356 `generation = e.generation,`). The VALUE is arbitrary; all that matters is that it changes on free, which Destroy already guarantees unconditionally (veda_ocl_insts.sail:784). DROP IT: nothing breaks, provided the child slot keeps its own persistent counter. WORSE, if \"fresh\" is read as \"reset to zero\", it specifies an ABA bug -- a re-carve resetting a slot's counter revives stale capabilities from that slot's first incarnation, the exact failure the `retired` flag exists to prevent (veda_ocl_insts.sail:684-685, :786). One word in clause 2 is either redundant or a live bug depending on how an implementer reads it.\n\nTHE DELETION-SHAPED INCREMENT. Strip the four decorations and what remains of R1 is: \"one Object_ID per slab element, minted cheaply, with the mint confined to a range the minter actually holds.\" The machine ALREADY mints one Object_ID per allocation in one untrapped instruction -- runtime/veda_rt.c:69-95 calls veda_odt_populate_fast_asm / veda_odt_destroy_asm, no ecall in the file. What is missing is not an instruction, it is a CONSTRAINT on the instruction that exists: Populate takes an unvalidated raw Base (veda_ocl_insts.sail:719-720, :890-895 -- Rev-E, still open) and nothing in either layer or the spec forbids overlapping entries. Add the containment test to Populate against a range the authority holds, and scope the ODA (which today reads only one permission bit and ignores the ODA capability's own Object_ID, Base and Length -- veda_ocl_insts.sail:632-633, veda_core.tlv:4184), and you have carve: no new opcode, no new Perms bit, no new bind mode, no carve-only parent, and the SAME check closes Rev-E. Crucially the clause-6 trilemma evaporates, because the arena stops being an ODT object whose Perms must be split -- it becomes a range the authority holds. That is the deletion this project's best increments look like.\n\nONE PROCESS NOTE. Ship any version of this PRIVILEGED first. The intra-slab UAF class is a property of per-element Object_IDs, not of who mints them, so the entire security win is available without touching the authority boundary at all -- and it is testable, which the unprivileged question is not until the six invariants Populate currently gets free from privilege are re-established as explicit checks.\n\nRead-only throughout. Nothing was modified, built or run; /home/prabhu/makerchip/rva23-core was never touched."
      }
    ],
    "ground": [
      {
        "summary": "R1/CARVE-v1 sweep on \"where do child Object_IDs come from?\", from a complete read of DESIGN_08 plus the ODT indexing on both layers. Headline: **no free-ID allocator exists anywhere in this machine, in hardware or in architecture.** Every ODT-mutating instruction on both layers takes the 44-bit Object_ID verbatim from a GPR; the only allocator in the tree is an 8-slot linear scan over a static bool array in the C runtime. The Region Table -- the one per-domain structure that could hold a bump cursor -- is written only by reset seeds on both layers, has no write instruction, and R10's CRBR-restore soundness is explicitly premised on that immutability. So CARVE-v1 must either invent a per-region cursor plus an RT write path (costing an RT read+write on every carve, destroying the DESIGN_08 Section 4 one-read intra-domain fast path that the RTL has a probe asserting), or thread a hardware free-list through dead entries (a second dependent DRAM read on the allocate path, with no storage: the Sail struct has no spare field and the RTL entry has 2 spare bytes, not the 6 its own comment claims), or derive the child ID deterministically instead of allocating it. Only the third keeps DESIGN_07 R1's own \"carve = one ODT write\" claim alive, and it is not in any source -- it is my proposal.\n\nOn footprint: the architectural entry is 32 bytes, so one entry per live slab element is a 100% metadata tax on a 32-byte kmalloc element, 50% at 64 B, 25% at 128 B, 12.5% at 256 B; per-region ceiling is 2^24 x 32 B = 512 MiB, plus a fixed ~20-24 MiB resident RT. But the deeper cost is structural, not ratio: the per-region ODT is a DIRECT-MAPPED array indexed by local, so resident footprint tracks the HIGH-WATER local index, not the live set -- a bump pointer that never reuses drives a hot domain monotonically to its full 512 MiB and 16.7M-ID ceiling regardless of how few children are live. And the 4 KiB / 128-entry granule paging DESIGN_08 leans on to bound this DOES NOT EXIST in either layer; DESIGN_08 attributes it to \"the existing DESIGN_02 residency mechanism\", but that mechanism is per-OBJECT residency, and there is no ODT-granule residency anywhere in Sail or RTL.\n\nTwo findings kill CARVE-v1 as specified independently of the allocator question. (1) The design makes carve unprivileged but leaves FREE privileged -- step 5 says free() = ODT-Destroy of the child, and ODT-Destroy's Machine-or-ODA gate is untouched, so half of every malloc/free pair is still a syscall and the cost argument fails on free. (2) The \"carve-only parent Permit\" cannot be a capability-register attenuation, because re-Binding the arena's Object_ID re-mints Perms straight from the entry -- the exact bypass DESIGN_02 already found and fixed for copy-on-write. It must therefore be an ENTRY property, meaning the arena's entry carries no PERM_LOAD/PERM_STORE -- and then there is no source in this machine for the child's load/store permission, because Perms flow entry -> capability and every derivation instruction is monotonic-clear-only. The veda_attr template Populate-Fast uses is unavailable too: after R39 it is M-mode-only, and veda_regs.sail records that leaving it ungated at the compartment level was justified specifically because \"its sole consumer, VEDA_ODT_POPULATE_FAST, already has its own independent capability-authority gate\" -- the premise CARVE-v1 removes.\n\nAll four of the architect's stated facts re-verified true at source, with one extra consequence: the Bind instruction word has no immediate field left to carry a child window, so mode 0b11 is probably the wrong host encoding. My judgement: the design dies first on the child-Perms question, second on the allocator; the footprint is survivable only under an ID-reuse allocator that does not exist.",
        "findings": [
          {
            "claim": "No free-ID allocator exists anywhere in hardware on either layer. Every ODT-mutating instruction receives the 44-bit Object_ID verbatim from a GPR -- the machine has no notion of \"give me a fresh ID\".",
            "evidence": "Sail, identical two-line idiom in all five ODT writers: `let object_id_gpr64 : bits(64) = zero_extend(X(rs1)); let object_id : bits(44) = object_id_gpr64[43..0];` at /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_ocl_insts.sail:641-642 (VEDA_ODT_POPULATE), :855-856 (POPULATE_FAST), :758-759 (DESTROY), :991-992 (SET_COW), :1031-1032 (SET_DOMAIN). RTL mirrors it: the ODT address derives from $veda_object_id alone -- /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:2090-2091 `$veda_region[19:0] = $veda_object_id[43:24];` / `$veda_local[23:0]  = $veda_object_id[23:0];`. Greps for next_id|next_object|free_list|freelist|allocator|alloc_id|id_alloc|next_local across model/extensions/Veda/*.sail and veda_core.tlv return zero allocator state -- only prose about generation \"bump\" and one passing reference to \"real allocator traces\" (veda_types.sail:328).",
            "bearing": "This is the load-bearing gap. CARVE-v1 step 2 says hardware \"mints a FRESH child Object_ID inside the parent region\" -- there is no mechanism, no state, and no precedent for hardware choosing an ID. Everything else in the design is downstream of solving this."
          },
          {
            "claim": "The only allocator that exists in the entire tree is software, and it is 8 slots of linear scan over a static bool array in the C runtime.",
            "evidence": "/home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt.c:68-84: `veda_obj_t veda_malloc(void) { for (int i = 0; i < VEDA_RT_MAX_OBJECTS; i++) { if (!g_in_use[i] && !g_retired[i]) { uint64_t object_id = VEDA_RT_FIRST_OBJECT_ID + (uint64_t)i; ... } } return VEDA_OBJ_INVALID; }`, backed by `static bool g_in_use[VEDA_RT_MAX_OBJECTS];` (veda_rt.c:34), `#define VEDA_RT_MAX_OBJECTS 8` (/home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt.h:49) and `#define VEDA_RT_FIRST_OBJECT_ID 1000u` (veda_rt.h:57). The header calls itself \"Deliberately a SINGLE-SLAB-SIZE pool allocator, not a general variable-size malloc\" (veda_rt.h:6).",
            "bearing": "There is no software allocator to copy either. CARVE-v1 is designing an allocator from scratch -- the project never had to answer the ID-supply question because 1000..1007 was hardcoded."
          },
          {
            "claim": "The Region Table -- the only per-domain structure that could hold a per-region bump cursor -- is immutable after reset on BOTH layers, and R10's CRBR-restore soundness argument is explicitly premised on that immutability, with the future RT-write instruction already carrying a recorded hard obligation.",
            "evidence": "Sail: the only writes to veda_region_table[..] are four reset seeds inside veda_test_seed_odt at veda_regs.sail:1122, :1123, :1124, :1133. veda_regs.sail:1010-1012 states why it matters: \"The restore is INFALLIBLE by design -- it re-installs the saved pair verbatim, with no RT re-validation -- and that is sound TODAY for a checked reason, not an assumed one: the Region Table is immutable after reset (no RT-write instruction exists).\" Then :1018-1021: \"HARD OBLIGATION recorded for the future RT-write instruction (DESIGN_07 R10, DESIGN_08 Section 9): it must refuse to clear residency on the CURRENT region and on any SAVED region, or this restore stops being sound the day that instruction lands.\" RTL: rt_valid[]/rt_resident[]/rt_odt_base[]/rt_backing[]/rt_generation[] are written only inside the `initial begin` block at veda_core.tlv:269-301; every other occurrence (2140, 2363-2364, 3367, 3394-3395, 5445-5448) is a read.",
            "bearing": "A per-region bump cursor requires the first RT write path this machine has ever had, created by an UNPRIVILEGED instruction. Even if the cursor field is disjoint from region_odt_base/region_resident, the literal premise \"no RT-write instruction exists\" becomes false and the R10 restore argument must be re-established field-by-field rather than inherited."
          },
          {
            "claim": "A per-region bump cursor in the RT would cost an RT read AND an RT write on every carve, destroying the DESIGN_08 Section 4 fast path -- because the CRBR holds only the base, not a cursor, and the RTL has a probe that asserts intra-domain operations read the RT exactly zero times.",
            "evidence": "The CRBR is exactly two registers, neither a cursor: veda_regs.sail:860-861 `register veda_current_region   : bits(20) = zeros()` / `register veda_current_odt_base : bits(56) = zeros()`. /home/prabhu/veda-core/design/DESIGN_08_OBJECT_NAMESPACE_SCALE.md:96-97 states the property a cursor breaks: \"Intra-domain bind (the common case): the object's region field equals the current region, so the ODT base is already in the CRBR. One DRAM read (the ODT read) -- identical to today, no regression.\" The RTL instruments it -- veda_core.tlv:2118-2123: \"The observable that proves the fixed-shape one-read property... a testbench probes it to assert that an intra-domain bind reads the RT exactly 0 times and a cross-domain bind exactly 1 -- the difference between a single architectural register and a cache\", implemented as `$veda_rt_read_en = (...) && !$veda_intra_region;` at :2124-2133.",
            "bearing": "Raises carve's real cost from DESIGN_07 R1's claimed \"one ODT write\" to {RT read, RT write, ODT write} on the hottest allocation path, and a live probe would catch the regression. Cheapest repair is a third CRBR-class register (a current-region allocation cursor, saved/restored like veda_saved_region/_base at veda_regs.sail:872-873) with writeback at domain exit -- a genuinely new architectural register plus a new RT write path, not a free extension."
          },
          {
            "claim": "A hardware free-list threaded through dead ODT entries has nowhere to live and adds a second dependent DRAM read to the allocate path -- the exact serial-dependency cost DESIGN_08 Section 4 spent a whole section removing from Bind.",
            "evidence": "Sail's odt_entry (veda_types.sail:287-384) is ten fields with no spare capacity: valid(1)+Base(56)+Length(40)+Perms(16)+generation(24)+owner_hart(8)+retired(1)+resident(1)+owner_domain(20)+cow(1) = 168 bits. Destroy PRESERVES Base and Length rather than freeing them as link storage: veda_ocl_insts.sail:788-789 `Base = old_entry.Base,` / `Length = old_entry.Length,`. DESIGN_08:84-87 records the cost this reintroduces: \"a naive scheme adds +1 dependent DRAM read to every hot-path Object-Bind (RT read, then ODT read), doubling the serial memory dependency on the machine's hottest path, with no measured cost because the repo models zero real DRAM latency.\"",
            "bearing": "Free-list-in-dead-entries is the textbook answer and the one this machine can least afford: the head pointer needs RT storage (previous finding) and each pop needs a dependent read of the dead entry's link. It also collides with the entry-sizing finding below."
          },
          {
            "claim": "The RTL's own entry-sizing comment is two increments stale: it claims 6 spare bytes when only 2 remain. Any new per-entry field CARVE-v1 needs (child-Perms template, parent back-pointer, free-list link) forces the 64-byte entry bump DESIGN_02 says was deferred rather than avoided.",
            "evidence": "veda_core.tlv:102-108 says \"RTL-3: 32 bytes. This is arithmetic, not preference... Every field is byte-aligned (26 B used, 6 spare after RTL-6)\" with `localparam int ODT_ENTRY_BYTES = 32;`. But RTL-17 then added `localparam int ODT_OFF_OWNER_DOMAIN = 26;` (:148, bytes 26-28) and RTL-18 added `localparam int ODT_OFF_COW = 29;` (:160). Enumerating every odt_mem[...+N] literal in the file gives offsets 0 through 24 continuously; with ODT_OFF_RESIDENT=25 (:135) and the two named fields above, bytes 0..29 are used and only 30-31 are spare. /home/prabhu/veda-core/design/DESIGN_02_ODT_UNIFIED_MM.md:316-317 already flagged the ceiling: \"This defers the 64-byte bump rather than avoiding it, and that is the honest framing. `resident` + a future `cow` leaves 40 bits for `backing`.\" That budget is now spent.",
            "bearing": "Prices any CARVE variant needing new per-entry state. Two bytes cannot hold a 16-bit child-Perms template plus anything else, and DESIGN_02's own next field (`backing`, still unbuilt) wants the same bytes -- CARVE and backing are now in direct competition. Worth fixing the stale comment regardless of R1."
          },
          {
            "claim": "ODT footprint of one entry per live slab element, at the real entry sizes: 100% metadata overhead on a 32-byte kmalloc element, 50% at 64 B, 25% at 128 B, 12.5% at 256 B. Per-domain ceiling is 512 MiB of ODT, plus a fixed ~20-24 MiB always-resident Region Table.",
            "evidence": "Entry size is 32 bytes in the design arithmetic -- DESIGN_08:26 \"a flat table of 2^44 entries x 32 bytes is ~512 TiB\"; DESIGN_08:56-57 \"a flat, direct-mapped array of up to 2^24 entries at 32 bytes (512 MiB logical ceiling)\" -- and 32 bytes in the RTL, `localparam int ODT_ENTRY_BYTES = 32;` (veda_core.tlv:108). Bit-packed, the Sail fields total 168 bits = 21 bytes (66%/33%/16%/8% instead), but DESIGN_02:307-311 refuses bit-packing on auditability grounds: \"Byte-aligned, that arm is eight enumerated writes auditable by eye; bit-packed, it becomes a read-modify-write of a shared byte where a preservation bug is invisible.\" RT cost: DESIGN_08:51-54 \"A single Region Table (RT), flat and DRAM-resident, `2^20` entries. Each entry is packed tight (~20-24 bytes...) Fixed resident cost **~20-24 MiB**.\" Concrete: 1M live children in one domain = 32 MiB ODT; 4M = 128 MiB; the 2^24 ceiling = 16.7M children = 512 MiB.",
            "bearing": "For the small kmalloc caches that ARE the dominant exploitation target R1 names (the cross-cache / msg_msg family lives in kmalloc-32/64/96), the descriptor is as large as or larger than the object it describes. Linux SLUB's per-object metadata is zero in the common case (the freelist pointer lives inside the free object), so this is not marginal -- it is a new 25-100% tax on the kernel heap. Defensible for closing the class, but must be stated as a doubling of small-object heap cost, not as DESIGN_07:71-74's vague \"pressures the Object_ID ceiling and ODT footprint\"."
          },
          {
            "claim": "The footprint is worse than \"one entry per LIVE element\", and this is where I think the design most likely dies on cost. Because the per-region ODT is a DIRECT-MAPPED array indexed by local, resident footprint tracks the HIGH-WATER local index, not the live set -- a bump pointer that never reuses drives a hot domain monotonically to its full 512 MiB and 16.7M-ID ceiling no matter how few children are live at any instant.",
            "evidence": "The index is a direct sum, not a hash or compacting map. Sail: `let idx = unsigned(veda_odt_base_of(region)) + lu;` (veda_regs.sail:1075, with `let local  = object_id[23 .. 0]; ... let lu = unsigned(local);` at :1069-1071). RTL: `$veda_odt_entry_idx[31:0] = $veda_region_base + {24'b0, $veda_local[7:0]};` (veda_core.tlv:2146). DESIGN_08:203-206 names the consequence without pricing it: \"Reclamation is now a hard prerequisite, not a someday. Under R1 SLAB-CARVE the 24-bit local space and the 24-bit generation fill under churn; without Cornucopia-style sweeping revocation (DESIGN_06, DESIGN_07 Rev-B, unstarted) a hot domain exhausts its local namespace.\" Rev-B is recorded unstarted at /home/prabhu/veda-core/design/DESIGN_07_ROBUSTNESS_AND_SECURITY_HARDENING.md:104-105.",
            "bearing": "CARVE-v1 is not merely blocked on an allocator; it is blocked on a REUSING allocator. Reuse is architecturally clean -- reusing a local means re-Populating that slot, and Populate already bumps generation (veda_ocl_insts.sail:682-685), which is exactly the temporal property R1 wants -- but reuse is precisely what needs the free-ID state that does not exist. A non-reusing bump pointer converts a bounded live-set cost into an unbounded churn cost."
          },
          {
            "claim": "The demand-paging granularity DESIGN_08 relies on to bound the per-region ODT does not exist in either layer, and DESIGN_08 attributes it to a mechanism that is about something else.",
            "evidence": "DESIGN_08:56-58: \"Each domain's per-region ODT is a flat, direct-mapped array of up to `2^24` entries at 32 bytes (512 MiB logical ceiling), **demand-paged** in 4 KiB / 128-entry granules via the existing DESIGN_02 residency mechanism.\" But that mechanism is per-OBJECT residency, and veda_types.sail:341-346 draws the distinction itself: \"Deliberately DISTINCT from region_entry's `region_resident`, and the distinction is why this must be a separate field rather than a reuse: region residency governs whether a domain's ODT *table* is paged in; object residency governs whether this object's *contents* are.\" Grepping `granule` across model/extensions/Veda/*.sail and veda_core.tlv returns only capability tag-store granules (veda_types.sail:185-188; veda_core.tlv:3540-3599) -- no ODT-granule residency state, no granule fault, no granule cause. Region residency is whole-region (one rt_resident bit per domain, veda_core.tlv:260); object residency is whole-object.",
            "bearing": "\"Only the touched granules of active domains consume DRAM\" (DESIGN_08:58) is unimplemented design prose. As built, a domain's ODT is resident or not as a UNIT, so binding one carved child makes that domain's entire ODT window resident. R1's footprint cannot be bounded by granularity that has not been designed; a third residency mechanism (granule, alongside region and object, each with its own cause per DESIGN_02:54-59) is a prerequisite the R1 doc does not list."
          },
          {
            "claim": "CARVE-v1 makes carve unprivileged but leaves FREE privileged. ODT-Destroy's Machine-or-ODA gate is untouched by the design, so half of every malloc/free pair is still a syscall and the cost argument the design exists to satisfy fails on the free side.",
            "evidence": "The candidate's step 5 is \"free() = ODT-Destroy of the CHILD id\". That instruction is gated: veda_ocl_insts.sail:753 `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`. The RTL mirrors it: veda_core.tlv:2617-2619 gate the destroy/populate refusals on `(!($priv || $veda_oda_authorized) || !$veda_odt_valid)`. Nothing in CARVE-v1 changes either. The crux as stated (\"If carve and free stay privileged, every malloc and free is a syscall and the design is dead on cost\") identifies the problem and then solves only the carve half.",
            "bearing": "Structural gap in the candidate design, independent of the allocator. Either a symmetric unprivileged carve-free instruction is needed -- authorised by the same parent capability, so PERM_CARVE on the arena confers the right to destroy any child in it -- or the cost argument does not close. The symmetric version has its own consequence: PERM_CARVE then becomes the authority to revoke every element of an arena, a far larger grant than \"may allocate\"."
          },
          {
            "claim": "The \"carve-only parent Permit\" cannot be a capability-register attenuation -- re-Binding the arena's Object_ID re-mints Perms straight from the entry. This is the exact bypass DESIGN_02 already found and fixed for copy-on-write, one layer down.",
            "evidence": "Bind mints Perms from the ENTRY, not from any held capability: veda_regs.sail:982-983 `function veda_bind_perms(e : odt_entry) -> bits(16) =` / `  if e.cow then e.Perms & 0xFFF7 else e.Perms`. DESIGN_02:135-141 states the general lesson in its own correction: \"Bind mints the capability's `Perms` straight from the ODT entry, verbatim, with no attenuation, on all three bind modes; and CAndPerm writes only the capability register, never the table. So a domain that can name a `cow` object's Object_ID can simply **re-Bind it and receive a fully writable capability**, never touching the attenuated one it was given. The register-local attenuation the scheme below relies on is not a security boundary.\" The required shape is at DESIGN_02:143-145: \"The fix must be hardware... so the ODT entry is the sole authority and no re-derivation can escape it.\"",
            "bearing": "Forces the carve-only property to be an ENTRY property: the arena's ODT entry must itself carry PERM_CARVE and NOT PERM_LOAD/PERM_STORE. Achievable -- and it sets up the next finding, which I think is what actually kills CARVE-v1 as specified."
          },
          {
            "claim": "With the arena's ENTRY stripped of load/store, there is no source in this machine for the CHILD's load/store permission. Perms flow entry -> capability, and every derivation instruction is monotonic-clear-only, so a parent holding no load/store authority cannot confer it.",
            "evidence": "Entry -> capability is the only inbound path (veda_regs.sail:982-983, above). Derivation is clear-only by construction: /home/prabhu/veda-core/design/DESIGN_01_CAPABILITY_FORMAT_RESPEC.md:102-117 on CAndPerm -- \"`cd = cs1` with `Perms &= rs2` (monotonic clear-only)... Monotonicity is structural, not a check: bitwise AND can only clear, never grant.\" CSetBounds copies Perms verbatim and narrows only spatially: veda_cap_insts.sail:236 `Perms     = cap.Perms,` (alongside :232 `Object_ID = cap.Object_ID,` and :238 `generation = cap.generation,` -- the architect's stated fact, confirmed).",
            "bearing": "CARVE-v1 has no answer to \"what Perms does the child get?\". The options are all new architecture: a second Perms field in the arena entry acting as a child-grant template (new per-entry state, and only 2 spare bytes remain), or a GPR operand supplying child Perms -- which makes an unprivileged instruction mint arbitrary permissions with nothing to bound it against, since the parent deliberately holds none. I would answer this before the allocator question."
          },
          {
            "claim": "veda_attr, the existing Perms template Populate-Fast uses, is unavailable to an unprivileged carve -- and the recorded justification for leaving it ungated at the compartment level is exactly the premise CARVE-v1 removes.",
            "evidence": "Populate-Fast takes Perms from the CSR: veda_ocl_insts.sail:898-899 `Length     = veda_attr[55..16],` / `Perms      = veda_attr[15..0],`. veda_regs.sail:564-569 records why that was safe: \"`veda_attr` (0x7C4) is deliberately NOT gated here -- its sole consumer, VEDA_ODT_POPULATE_FAST, already has its own independent capability-authority gate (`veda_oda_authorized()` OR M-mode, Milestone 11), so a compartment rewriting `veda_attr` alone cannot use it for anything without separately satisfying that unrelated check -- considered and correctly excluded, not overlooked.\" Separately, after R39 the generic CSR privilege rule makes 0x7C4 M-mode-only: `csrPriv(csr) = csr[9..8]` transcribed at veda_core.tlv:4888-4891, enforced as `$csr_priv_violation = $is_csr_access && ($csr_priv_bits < $csr_addr[9:8]);` (:4918), and 0x7C4[9:8] = 0b11.",
            "bearing": "Two-sided. Unprivileged code cannot write veda_attr, so it cannot be the child-Perms source. And if a future increment relaxed 0x7C4 to make it usable, the veda_regs.sail:564-569 exclusion would silently become wrong -- an unprivileged minting instruction reading an unprivileged template is the whole gate gone. Whatever CARVE does, that comment must be revisited in the same increment."
          },
          {
            "claim": "There IS an existing unprivileged ODT WRITE on both layers -- Bind's owner_hart claim -- and it is the right precedent shape for the crux argument, but it is also the yardstick CARVE-v1 fails: it writes one non-identity field on an entry the caller already resolved, idempotently. Every other ODT-write authority in the machine is either global or M-mode.",
            "evidence": "Sail: unprivileged Bind performs `odt_write(object_id, claimed_entry);` on both success paths, veda_bind_insts.sail:305 (Rebind) and :342 (fresh Bind), where claimed_entry carries every field over unchanged except owner_hart (:226-245, e.g. :232 `owner_hart = mhartid[7..0],`, :241 `resident   = e.resident,`, :243-244 `owner_domain = e.owner_domain,` / `cow = e.cow,`). RTL writes exactly one byte -- veda_core.tlv:6679-6683 `always_ff @(posedge clk) begin` / `   if (act4_mode && CPU_veda_owner_claim_en_a0) begin` / `      odt_mem[CPU_veda_odt_addr_a0+18] <= MHARTID;` -- with :6666-6671 stating why nothing else may be written: \"This block writes exactly ONE byte, so residency carries over by construction and NO corresponding line belongs here. Adding one would be the bug: writing 8'h01 would make any Bind mark any object resident, which is the precise defeat of the gate that Bind is supposed to be subject to.\" By contrast the only capability-native authority for real ODT mutation is the ODA, and it is GLOBAL and unscoped -- veda_regs.sail:632-633 `function veda_oda_authorized() -> bool =` / `  veda_oda_tag & not(isSealedCap(veda_oda)) & permBit(veda_oda.Perms, PERM_ACCESS_SYSTEM_REGISTERS)` names no object and no region -- and it can only be installed by an M-mode instruction: veda_cap_insts.sail:893-894 `function clause execute VEDA_OSPECIALRW(scr, rs1, rd) = { if cur_privilege != Machine then Illegal_Instruction()`.",
            "bearing": "The crux's capability-native answer has no precedent at the authority level: every ODT-write authority today is global (ODA) or M-mode. CARVE-v1 would be the first SCOPED ODT-write authority in the machine. The Bind precedent supports \"unprivileged code may touch the ODT\" but explicitly not \"unprivileged code may write identity\" -- and carve writes valid, Base, Length and generation, the exact identity set DESIGN_02:281-285 draws a hard line around: \"identity changes cost a generation bump, policy changes do not... Base, Length, generation, valid and retired stay unreachable from this instruction and it must never become a general field-write -- that would turn the ODT into an ordinary table with a security story bolted on.\""
          },
          {
            "claim": "Sail-layer ODT indexing established: two-step region+local resolution into a single bounded flat array of 2^23 entries, partitioned 8 regions x 2^20 -- so the modeled per-region local window is 2^20, one sixteenth of the architectural 2^24.",
            "evidence": "veda_regs.sail:802 `register veda_odt : vector(8388608, odt_entry) = vector_init(empty_odt_entry)`; :818 `let VEDA_ODT_MODELED_ENTRIES = 8388608`; :829-830 `let VEDA_REGION_MODELED = 8` and `let VEDA_LOCAL_MODELED  = 1048576  // 2^20 modeled objects per region; 8 x this = 2^23`; :833 `register veda_region_table : vector(8, region_entry) = vector_init(empty_region_entry)`. Resolution is veda_regs.sail:1067-1078: region = `object_id[43 .. 24]`, local = `object_id[23 .. 0]`, `if (region != veda_current_region & ru >= VEDA_REGION_MODELED) | lu >= VEDA_LOCAL_MODELED then None()` else `let idx = unsigned(veda_odt_base_of(region)) + lu; if idx < VEDA_ODT_MODELED_ENTRIES then Some(idx) else None()`. The bound is deliberate -- veda_regs.sail:805-809: \"a vector(2^44) would be roughly 300 TiB and simply will not build. Ids at or above the modeled size read as 'not found'.\"",
            "bearing": "A Sail carve experiment can mint at most 2^20 children per region and 2^23 system-wide before falling off the modeled window into a silent OBJECT_NOT_FOUND. Fine for a property-test experiment, but it cannot reach the 2^24 exhaustion boundary DESIGN_08 Section 9 says is R1's real wall -- that would need the same direct-state-injection technique DESIGN_01:170-177 used for the 16.7M generation ceiling."
          },
          {
            "claim": "RTL-layer ODT indexing established: 768 entries of 32 bytes = 24 KiB total, three windows of 256, with the local index HARDWIRED to 8 bits and a 36-bit hi-tag doing the disambiguation. Carve on the RTL as it stands caps at 256 live children per region.",
            "evidence": "veda_core.tlv:87 `localparam int ODT_ENTRIES = 768;`; :94 `localparam int ODT_REGION_ENTRIES = 256;`; :101 `localparam int RT_ENTRIES = 8;`; :108 `localparam int ODT_ENTRY_BYTES = 32;`; :234 `logic [7:0] odt_mem [ODT_BASE : ODT_BASE + (ODT_ENTRIES * ODT_ENTRY_BYTES) - 1];` (= 24,576 bytes). Index truncation is explicit: :2146 `$veda_odt_entry_idx[31:0] = $veda_region_base + {24'b0, $veda_local[7:0]};` and :2154 `$veda_odt_idx[7:0]    = $veda_local[7:0];`. Aliasing is caught rather than allowed by a 36-bit tag: :2226-2227 `$veda_odt_id_hi[35:0] = {odt_mem[$veda_odt_addr+24], ... odt_mem[$veda_odt_addr+20]};` / `$veda_odt_id_match    = ($veda_odt_id_hi == $veda_object_id[43:8]);`, folded into `$veda_odt_valid` at :2232. :2203-2213 proves index and tag bits are \"exactly complementary and jointly total over all 44 bits.\"",
            "bearing": "A 257th carved child in a region reads not-found -- fail-closed, but the table cannot hold a realistic slab. Growing ODT_REGION_ENTRIES is not a parameter change alone: `$veda_local[7:0]` is a literal 8-bit slice at both the bind site (:2146, :2154) and the dereference re-check site (:3368, :3396), and veda_core.tlv:64-75 records that a previous width mismatch in exactly this area would have produced \"a live confused-deputy bug with ZERO test signal.\" Any RTL carve increment must widen both sites together and re-derive the tag-complementarity proof at :2203-2213."
          },
          {
            "claim": "All four facts the architect asked me to re-verify are true at source -- and the bind encoding carries an extra consequence: mode 0b11 has no immediate field left to carry a child window, so it is probably the wrong host encoding for carve.",
            "evidence": "(1) CSetBounds is temporally inert: veda_cap_insts.sail:232 `Object_ID = cap.Object_ID,` and :238 `generation = cap.generation,` inside veda_setbounds, called by both VEDA_CSETBOUNDS and VEDA_CSETBOUNDSEXACT (:246-253). (2) Destroy bumps, saturates, then retires: veda_ocl_insts.sail:784-785 `let new_generation : bits(24) = if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1;` / `let new_retired : bool = old_entry.retired | (old_entry.generation == 0xffffff);`. (3) Bind mode 0b11 is free and fail-closed on both layers: veda_bind_insts.sail:181 `VEDA_BIND_RESERVED <-> 0b11,` and :289 `VEDA_BIND_RESERVED => Illegal_Instruction(),`; the RTL decodes only three modes (:1649, :1662, :1669) and the fail-closed catch-all lists exactly those three -- veda_core.tlv:4833-4835 \"custom-0 Bind: the three real modes. Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap\", feeding `$veda_undef_encoding = $veda_op_claimed && !$veda_decoded;` (:4846). (4) Perms bits: veda_types.sail:229-231 `let PERM_SET_CID                       : range(0,15) = 11` / `let PERM_NMC_COMPUTE                   : range(0,15) = 12` / `// bits 13-15 spare`. The encoding consequence: veda_bind_insts.sail:186-187 pins `0b0000000000 @ mode @ encdec_reg(rs1) @ 0b101 @ 0b0 @ encdec_vcap(rd) @ 0b0001011`, and the RTL enforces it as `($instr[31:22] == 10'b0)` (veda_core.tlv:1620).",
            "bearing": "Confirms the encoding slot and permission bits are genuinely available, but CARVE-v1's \"rs1 supplies the child window (offset, length)\" does not fit: one 64-bit GPR must carry a 56-bit-capable offset and a 40-bit-capable length, the same squeeze veda_ocl_insts.sail:688-696 already records for plain Populate (\"112 bits of Base(56)+Length(40)+Perms(16) simply cannot fit one GPR\"). Bind's format has one GPR source; the parent-in-rd reading Rebind uses (veda_bind_insts.sail:300 `let cur = C(rdno);`) is available but a second GPR is not. The ODT family's three-operand Custom-0 funct7 shape (e.g. VEDA_ODT_SET_COW at veda_ocl_insts.sail:984-985) is the better host."
          },
          {
            "claim": "The one candidate that survives the allocator question with DESIGN_07 R1's own cost claim intact: DERIVE the child local deterministically from the parent and the element index rather than allocating it. This is my proposal and appears in no source document -- I state that plainly rather than dressing it as sourced.",
            "evidence": "Nothing in the sources partitions the 24-bit local field. DESIGN_08:37 defines it flat: \"`  Object_ID[43:0]  =  { region [43:24] (20 bits) , local [23:0] (24 bits) }`\", with :42 \"**local (24 bits) = an object within that domain.** ~16.7M objects per domain.\" Sub-splitting local into {arena tag, element index} would make the child's slot a pure function of (parent, element), needing zero allocator state and exactly one ODT write -- which is what DESIGN_07:70-71 already claims for carve: \"stays cache-less single-transaction (carve = one ODT write, free = one generation bump)\". Under derivation the high-water-mark problem disappears (footprint = one entry per slab SLOT, not per allocation-ever), and R1's \"fresh generation\" requirement is met by machinery already built: re-Populating a recycled slot bumps generation (veda_ocl_insts.sail:682-685) and Destroy always bumps (:784-785).",
            "bearing": "Worth attacking before the RT-cursor and free-list variants, because it is the only one needing no new mutable hardware state. Honest costs: an arena of N elements permanently consumes ceil(log2 N) bits of its domain's local namespace whether or not elements are live; variable-size arenas need a per-arena split, which the arena's entry must record (new per-entry state, and only 2 spare bytes remain); and it does nothing for the child-Perms problem. Unsourced -- DESIGN_08 treats local as flat and nothing in either layer sub-divides it."
          }
        ]
      },
      {
        "findings": [
          {
            "claim": "COMPLETE ODT WRITE CENSUS, SAIL. Every ODT write on the Sail layer funnels through one chokepoint, `odt_write` (veda_regs.sail:1091-1094), over `register veda_odt : vector(8388608, odt_entry)` (veda_regs.sail:802). It has exactly nine call sites in instruction code, belonging to eight instructions. SEVEN ARE PRIVILEGED, all carrying the identical gate `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`: ODT-Populate (veda_ocl_insts.sail:635 gate, write at :686), ODT-Destroy (:752 gate, write :786), ODT-Populate-Fast (:849 gate, write :888), veda.odt.set.cow (:988 gate, write :998), veda.odt.set.domain (:1028 gate, write :1041), veda.odt.page.out (:1101 gate, write :1162), veda.odt.page.in (:1224 gate, write :1234). ONE IS UNPRIVILEGED: Object-Bind, whose execute clause (veda_bind_insts.sail:190) contains no privilege test at all and writes at :305 (Rebind) and :342 (Bind / Bind-NoTrap). The second authority arm is `veda_oda_authorized() = veda_oda_tag & not(isSealedCap(veda_oda)) & permBit(veda_oda.Perms, PERM_ACCESS_SYSTEM_REGISTERS)` (veda_ocl_insts.sail:632-633). Reset scaffolding writes fifteen further entries via veda_test_seed_odt (veda_regs.sail:1141-1388).",
            "evidence": "veda_regs.sail:1091 `function odt_write(object_id : bits(44), e : odt_entry) -> unit =`; the seven gates are textually identical, e.g. veda_ocl_insts.sail:636 `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`. veda_bind_insts.sail:190-199 opens `function clause execute VEDA_BINDINST(mode_bits, rs1, rd) = { let mode = encdec_veda_bind_mode(mode_bits); ... }` with no `cur_privilege` reference anywhere in the 196-line body.",
            "bearing": "Establishes the baseline the crux asks about: an unprivileged ODT writer already exists and already ships. R1 is not introducing an unprecedented category -- it is proposing to widen one that exists."
          },
          {
            "claim": "COMPLETE ODT WRITE CENSUS, RTL, AND THE SAFETY TEMPLATE BIND ALREADY OBEYS. The RTL has two runtime writers of `odt_mem` (veda_core.tlv:234). (1) A six-arm mutually-exclusive if/else-if chain (veda_core.tlv:6466-6657): populate|populate_fast (:6466), set_cow (:6577), set_domain (:6579), destroy (:6583), page_out (:6610), page_in (:6621) -- each gated on its violation signal, and each violation signal carries `!($priv || $veda_oda_authorized)` (:2611, :2618, :2621, :2623, :2652). (2) A SEPARATE always_ff (:6680-6682) `if (act4_mode && CPU_veda_owner_claim_en_a0) odt_mem[CPU_veda_odt_addr_a0+18] <= MHARTID;` -- the unprivileged Bind claim, with no `$priv` term at :2460-2462. WHAT MAKES THAT ONE SAFE is a four-part template, and it is worth naming because it is the only worked example this machine has of a safe unprivileged ODT write: it writes exactly ONE field; the VALUE IS HARDWARE-SUPPLIED (MHARTID, a constant -- software contributes no data to the write); it touches no identity field (Base/Length/generation/valid/retired) and no policy field (owner_domain/cow); and its slot is reached only after the name's authority was independently checked.",
            "evidence": "veda_core.tlv:6680-6682 is the whole block: `always_ff @(posedge clk) begin / if (act4_mode && CPU_veda_owner_claim_en_a0) begin / odt_mem[CPU_veda_odt_addr_a0+18] <= MHARTID;`. Contrast the privileged arm at :6466 `if (act4_mode && (CPU_is_veda_odt_populate_a0 || CPU_is_veda_odt_populate_fast_a0) && !CPU_veda_odt_populate_violation_a0 && CPU_veda_odt_idx_ok_a0)` which then writes 25 bytes (:6496-6537) from `CPU_veda_odtpd_new_base/length/perms`, all software-supplied. The file's own header at :6486-6493 counts the hand-written entry layouts: \"There is no shared macro and no struct. A field added to five of six compiles clean and produces wrong values with no diagnostic.\"",
            "bearing": "CARVE-v1 violates all four clauses of the template simultaneously: it writes a whole entry, from software-supplied Base/Length/Perms, creating identity, at a software-named slot. It is not an extension of Bind's precedent -- it is its inverse."
          },
          {
            "claim": "THE CRUX PREMISE IS REFUTED AT SOURCE: THE PARENT CAPABILITY CANNOT BE THE AUTHORITY, BECAUSE ON THIS MACHINE THE NAME IS THE AUTHORITY AND CAPABILITY PERMISSIONS ARE ADVISORY. R1 proposes that holding a PERM_CARVE capability authorises minting. But Bind re-derives Perms from the ODT ENTRY, not from any held capability: `function veda_bind_perms(e : odt_entry) -> bits(16) = if e.cow then e.Perms & 0xFFF7 else e.Perms` (veda_regs.sail:982-983), consumed at veda_bind_insts.sail:321 and :354. So a holder whose PERM_CARVE was stripped by CAndPerm simply re-Binds the arena's Object_ID and gets it back. The design already knows this and says so verbatim in the Bind source. Consequently PERM_CARVE gates nothing: carve authority would in fact be held by anyone who can pass `veda_bind_domain_ok` for the arena, and Object_IDs are ordinary integers freely readable into GPRs by the unprivileged CGetObjectID (veda_cap_insts.sail:133, :160).",
            "evidence": "veda_bind_insts.sail:316-320, verbatim: \"DESIGN_02's own correction: a capability handed out with store stripped is only ADVISORY, because the holder can re-Bind the name and get a fresh, fully-permissioned one. So the attenuation has to happen HERE, where the entry is the authority and no re-derivation can escape it.\" This is the project's own already-adjudicated finding, applied to PERM_STORE; PERM_CARVE inherits it unchanged.",
            "bearing": "BLOCKS CARVE-v1 as specified. Any carve authority must live in the ODT ENTRY (a new entry field), never in the capability's Perms. This also matches the standing conclusion already in memory that attenuation is advisory by default."
          },
          {
            "claim": "CARVE-v1 POINTS 2 AND 6 ARE MUTUALLY CONTRADICTORY, AND THE CHILD'S Perms HAS NO SAFE SOURCE -- A CLOSED TRILEMMA. Point 6 requires the parent arena to carry PERM_CARVE but NOT load/store (\"carve-only, not directly dereferenceable\"). Point 2 leaves the child's Perms unspecified. There are only three sources and each is closed. (a) INHERIT FROM PARENT: the child is then also carve-only and not dereferenceable, and the mechanism is inert -- no byte is ever reachable. (b) FROM A GPR: an unprivileged instruction then mints arbitrary Perms, including PERM_ACCESS_SYSTEM_REGISTERS (bit 7), PERM_SEAL/PERM_UNSEAL (8/9), PERM_INVOKE (10), PERM_SET_CID (11). (c) FROM veda_attr, the natural choice since carve has the same operand-budget problem Populate-Fast was invented to solve -- but veda_attr is Machine-only on BOTH layers, so unprivileged code cannot set it, and un-gating it to make carve usable directly reverses R35/R27.",
            "evidence": "Perms allocation verified: veda_types.sail:218-230 allocate bits 0-12 (PERM_GLOBAL 0 .. PERM_NMC_COMPUTE 12), with PERM_SET_CID = 11; 13/14/15 are free, as the architect stated. veda_core.tlv:5616-5626 states the (c) hazard at its real size: \"a principal running below Machine -- holding neither privilege nor a tagged ODA -- could CHOOSE THE LENGTH AND PERMS OF AN OBJECT A LATER PRIVILEGED POPULATE-FAST WILL MINT. Control over the contents of someone else's mint, which on a machine whose thesis is derived authority is exactly the wrong direction.\"",
            "bearing": "BLOCKS CARVE-v1 as specified. The resolution is that carve-only must be a NON-INHERITED ODT entry bit (a `carve_only` flag that suppresses dereference of THIS entry), while child Perms are inherited monotonically from the parent entry. That separates the two roles Perms is currently being asked to play at once."
          },
          {
            "claim": "REFUTED, AND THE REFUTATION MATTERS. I hypothesised a live Sail/RTL divergence on veda_attr (CSR 0x7C4): Sail's write clause carries no privilege test (veda_regs.sail:619 `function clause write_CSR(0x7C4, value) = { veda_attr = zero_extend(value); Ok(veda_to_xlen(veda_attr)) }`) while the RTL gates it on `$priv` (veda_core.tlv:5655). That is NOT a divergence. Sail enforces Machine-only generically before any Veda clause runs: `check_CSR` conjoins `check_CSR_priv` (sys/sys_control.sail:54-59), and `check_CSR_priv(csr,p) = privLevel_to_CSR_privbits(p) >=_u csrPriv(csr)` with `csrPriv(csr) = csr[9..8]` (sys/sys_control.sail:19, :40-41). 0x7C4 = 0b0111_1100_0100, so bits[9:8] = 0b11 = Machine. Both layers agree. The RTL's own comment asserting exactly this (veda_core.tlv:5605-5610) is correct.",
            "evidence": "sys/sys_control.sail:19 `function csrPriv(csr : csreg) -> nom_priv_bits = csr[9..8]` and :54-59 `function check_CSR(csr, p, access_type) -> bool = check_CSR_priv(csr, p) & check_CSR_access(...) & is_CSR_accessible(...) & ...`. The Veda clause veda_regs.sail:511 `function clause is_CSR_accessible(0x7C4, _, _) = currentlyEnabled(Ext_Veda)` is one conjunct of that, not a replacement for it.",
            "bearing": "Closes option (c) of the Perms trilemma by construction rather than by argument, and removes a false finding I would otherwise have reported. Also flags a real documentation hazard: veda_regs.sail:564-569 explains 0x7C4's ungatedness by \"its sole consumer, VEDA_ODT_POPULATE_FAST, already has its own independent capability-authority gate\" -- a load-bearing sole-consumer invariant that any second consumer, carve included, silently falsifies."
          },
          {
            "claim": "CARVE-v1 POINT 2 IMPORTS R41 AND THEREBY BORNS EVERY SLAB ELEMENT OPEN TO EVERY DOMAIN. The candidate says owner_domain and cow \"reset the way Populate resets them (R41)\". R41 resets owner_domain to VEDA_DOMAIN_ANY = 0xFFFFF (veda_ocl_insts.sail:742, veda_types.sail:285), and `veda_bind_domain_ok` returns true unconditionally for ANY (veda_regs.sail:985-988). That is correct for Populate, which is privileged and mints for a trusted allocator. Applied to an unprivileged carve it means every carved child is bindable by every domain that can name it -- and Bind then hands out the child's FULL entry Perms. R1's claimed win is temporal (free() staleifies holders); this would pay for it with a spatial/confidentiality loss across compartments, re-opening the class the per-object bind gate was built to close.",
            "evidence": "veda_ocl_insts.sail:722-742, R41's own comment: \"A Populate MINTS A NEW OBJECT; the previous occupant's policy has no claim on it\" ... and the odt_entry field comment at veda_types.sail:371-372: \"VEDA_DOMAIN_ANY means 'any domain may bind this'. Every object is created that way.\" veda_regs.sail:986 `if owner_domain == VEDA_DOMAIN_ANY then true`.",
            "bearing": "BLOCKS point 2 as written. A carved child must INHERIT the parent's owner_domain (or be stamped with the carver's domain from veda_pcc_object[43:24]), never reset to ANY. R41's reasoning is sound for a privileged mint and unsound for an unprivileged one -- the difference is exactly the privilege the crux proposes to remove."
          },
          {
            "claim": "AN UNPRIVILEGED CARVE MAKES GENERATION EXHAUSTION A PERMANENT, UNPRIVILEGED DENIAL OF SERVICE. `retired` is monotone and no path ever clears it: Destroy computes `new_retired = old_entry.retired | (old_entry.generation == 0xffffff)` (veda_ocl_insts.sail:765), Populate refuses outright on a retired slot (veda_ocl_insts.sail:670-671 `if old_entry.retired then Illegal_Instruction()`), and page-out/page-in/set.domain/set.cow all carry it across. Today that is safe because every generation bump requires Machine or ODA authority. Under CARVE-v1 clause 5, free() = Destroy of the child, so an unprivileged carve/free loop burns generations at will and permanently retires the targeted slots. DESIGN_08 already names the pressure and records the reclaimer as unstarted.",
            "evidence": "DESIGN_08_OBJECT_NAMESPACE_SCALE.md:203-206: \"Reclamation is now a hard prerequisite, not a someday. Under R1 SLAB-CARVE the 24-bit local space and the 24-bit generation fill under churn; without Cornucopia-style sweeping revocation (DESIGN_06, DESIGN_07 Rev-B, unstarted) a hot domain exhausts its local namespace. Widening the counter (increment 4) raised the ceiling; it did not remove the need to reclaim.\" Generation is 24 bits (veda_types.sail:314-322, ~16.7M reuses per slot).",
            "bearing": "CONSTRAINS. R1 cannot ship unprivileged before a reclaimer exists, or it must meter carve (per-domain quota in the RT) so exhaustion is bounded and self-inflicted rather than inflictable on a neighbour."
          },
          {
            "claim": "ON THE RTL LAYER A CARVE BUILT ON THE POPULATE WRITE PATH IS AN EVICTION ENGINE, AND THE TWO LAYERS WOULD DIVERGE BY CONSTRUCTION. The Populate arm is the ONLY ODT write arm with no `id_match` term -- set_cow, set_domain and destroy all carry `CPU_veda_odt_id_match_a0` (veda_core.tlv:6577, :6579, :6583); populate does not (:6466). That is deliberate (\"Populate keeps its slot TAKEOVER\", veda_core.tlv:6552-6555), and tolerable while Populate is privileged. The RTL resolves entries by `region_base + local[7:0]` over ODT_REGION_ENTRIES = 256 (veda_core.tlv:94, :2146), so one slot in 256 collides; Sail resolves over the full VEDA_LOCAL_MODELED = 2^20 (veda_regs.sail:830) and cannot express the collision at all. An unprivileged carve inheriting takeover semantics lets any compartment evict a chosen live object by picking a colliding child name.",
            "evidence": "veda_core.tlv:2549-2560 states the asymmetry plainly: \"This file models 256 locals per region and resolves with local[7:0] only ... so MANY names share one slot -- Object_ID 436 and Object_ID 180 land on the same 32 bytes. The id_hi tag exists to detect exactly that, but it is consulted only on the two READ paths ($veda_odt_valid, $veda_check_odt_valid); neither ODT write arm looks at it.\" This is the RTL-10/R13 class, already fixed once for Destroy.",
            "bearing": "CONSTRAINS the mirror. Carve must carry `id_match` from its first line on the RTL -- it may take a FREE slot but never a slot another name occupies -- and the refusal must be architectural (a trap), not a dropped write, or the two layers disagree silently."
          },
          {
            "claim": "THE EXECUTING-OBJECT PIN MUST EXTEND TO CARVE, AND THE TWO LAYERS IMPLEMENT THE PIN ON DIFFERENT IDENTITY NOTIONS. Sail pins by NAME: `veda_object_is_executing(object_id) = (object_id == veda_pcc_object) | (veda_trap_depth != zeros() & object_id == veda_mepcc_object)` (veda_regs.sail:990-992). The RTL pins by SLOT: `$veda_object_slot_is_pcc = ($veda_object_id[43:24] == $veda_pcc_object[43:24]) && ($veda_object_id[7:0] == $veda_pcc_object[7:0])` (veda_core.tlv:2573-2578). R11(b)'s complete consumer set is Populate, Populate-Fast, Destroy and page-out (veda_ocl_insts.sail:653-658). Carve creates an entry, so it belongs in that set on the same reasoning Populate does -- and clause 5 makes free()=Destroy(child) a second entry point that must respect it.",
            "evidence": "veda_core.tlv:2555-2560 explains why the RTL diverges deliberately: \"a name compare here would have been bypassable in one instruction: destroy or repopulate Object_ID 436 while the core executes object 180, pass the pin (436 != 180), and clobber the descriptor of the code being fetched. Execute-after-free, by the exact route this increment exists to close.\" veda_ocl_insts.sail:656-658 enumerates the set: \"it is Populate, Populate-Fast, Destroy and page-out. Page-in is deliberately NOT in it.\"",
            "bearing": "CONSTRAINS. Carve is a fifth member of the R11(b) consumer set on both layers, with the RTL's slot-compare (which strictly subsumes the name compare) rather than a fresh name compare."
          },
          {
            "claim": "LIVE BUG FOUND IN PASSING, INDEPENDENT OF R1, ON THE EXACT ENCODING SLOT R1 WANTS. veda.bind mode 0b11 is NOT unconditionally fail-closed on Sail. The RTL refuses it at DECODE -- mode 0b11 is absent from `$veda_decoded` (veda_core.tlv:4835-4836, with the explicit comment \"Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap\"), so `$veda_undef_encoding` fires (:4846) and mcause is 0x02 (:5235). Sail reaches `VEDA_BIND_RESERVED => Illegal_Instruction()` only at line 289, AFTER three ODT-state-dependent traps: the region-residency gate at :211, the domain gate at :284, and the residency gate at :285. So on a non-resident region, Sail raises E_Extension (mcause 24) with cause REGION_FAULT 0x09 while the RTL raises illegal-instruction (mcause 0x02). Concrete reproduction using state both layers already seed identically: `li x1, 0x02000007` (region 2, local 7 -- seeded at veda_regs.sail:1379 and veda_core.tlv:435-454, with region 2 non-resident on both, veda_regs.sail:1105 and veda_core.tlv:291) then `.insn i 0x0b, 0x5, x2, x1, 3`. A reserved encoding whose refusal cause is a function of another domain's paging state is also a small ODT-state oracle for unprivileged code.",
            "evidence": "The existing probe cannot see this on two independent counts. difftest/probes/p5_reserved.S:27-29 sets `li x1, 1` then `.insn i 0x0b, 0x5, x2, x1, 3` -- Object_ID 1 is region 0, resident, owner_domain ANY, so all three Sail gates pass and both layers land on illegal. And its handler only counts: `trap_handler: addi x29, x29, 1` (p5_reserved.S:47-48), never reading mcause, so even the divergent case would report AGREE. That is precisely the failure mode veda_core.tlv:4736-4739 warns about from this project's own receipt: \"BOTH layers did the same no-op ... and the probe still reported AGREE.\"",
            "bearing": "MUST BE FIXED BEFORE R1 TOUCHES THE SLOT. Either hoist the reserved-mode check above the three gates in Sail (matching the RTL's decode-time refusal), or accept the RTL's ordering deliberately -- but the choice has to be made, because R1 is about to give mode 0b11 real semantics and would inherit whichever ordering is left in place. Extend p5_reserved.S to assert mcause, not just trap count."
          },
          {
            "claim": "THE COST PREMISE IS CONFIRMED, AND IT IS WORSE THAN 'ONE SYSCALL PER MALLOC'. The shipping runtime allocator calls the privileged mint directly on every allocation -- `veda_malloc` issues `veda_odt_populate_fast_asm(object_id, base)` (runtime/veda_rt.c) and `veda_free` issues `veda_odt_destroy_asm(obj)`. Both instructions require Machine or ODA authority, so the entire allocator today runs at Machine privilege. Separately, the runtime's software model of hardware retirement is stale by a factor of 65536: `static uint8_t g_destroy_count[VEDA_RT_MAX_OBJECTS]` (veda_rt.c:48) retires a slot when that byte wraps at 256 (:104-106), while hardware retires at 2^24. This is stale in the SAFE direction (software gives up early) so it is not a security bug, and it is not among the 71 items already recorded in DOC_STALENESS_SWEEP_2026-08-18.md, which cites veda_rt.h/.c only for suite counts.",
            "evidence": "runtime/veda_rt.h:13-18 states the coupling R1 exists to break: \"Object IDs and backing-memory slots are 1:1 (slot i always backs Object_ID VEDA_RT_FIRST_OBJECT_ID+i) -- a real, stated scope limit: once a slot's generation counter is exhausted ... that slot's backing memory is permanently unusable too, not just its Object_ID -- a production allocator would decouple the two.\" veda_rt.c:104-106 `g_destroy_count[i]++; if (g_destroy_count[i] == 0) { g_retired[i] = true; }`.",
            "bearing": "Confirms the crux's economic framing is real, and identifies the smallest honest first increment: keep carve PRIVILEGED, and measure. A privileged carve already closes the entire intra-slab UAF class -- the class is a property of per-element Object_IDs, not of who mints them. Unprivileged minting is a separate, later, throughput question that should be paid for with measured numbers rather than assumed."
          },
          {
            "claim": "JUDGMENT ON THE CRUX. An unprivileged instruction COULD mint an ODT entry safely, but not under the authority model as it stands, and not in one step. The invariants Populate currently gets for free from being privileged, each verified above, are: (1) the Object_ID operand is unconstrained -- `let object_id : bits(44) = object_id_gpr64[43..0]` accepts any 44-bit value including any region (veda_ocl_insts.sail:641-642), and nothing in the design says which domain may mint into which region; (2) Base, Length and Perms are software-supplied and unchecked; (3) slot takeover is permitted on the RTL; (4) generation and `retired` are consumable resources; (5) owner_hart resets to UNOWNED, so on multi-hart a carved child is claimable by whichever hart Binds first; (6) owner_domain resets to open. A capability-authorised carve must re-establish all six as explicit checks. Crucially, invariant (1) has NO existing mechanism at all -- DESIGN_08 named it and left it open, and it is the load-bearing gap.",
            "evidence": "DESIGN_08_OBJECT_NAMESPACE_SCALE.md:195-198, verbatim: \"New security surface, three parts, all must be closed: (a) the RT and region_odt_base must be strictly MSA-private ... (b) region-grant authority -- which domain may mint into which region -- is a new authority question, and a bug letting domain A mint into region B is a compartment-escape of the Milestone-19 class; (c) a stale/corrupt region_odt_base has region-wide blast radius.\" Item (b) is exactly the authority an unprivileged carve requires and the design does not have.",
            "bearing": "THE SHAPE THAT SURVIVES THIS PASS. Carve authority belongs in the ODT ENTRY, not in a capability's Perms: a new non-inherited `carve_only` bit plus an entry-held carve right, written by a privileged instruction in the veda.odt.set.domain family (policy, no generation bump). The child then inherits owner_domain and a monotone subset of Perms from the parent, is pinned inside the parent's Base/Length window, is refused on retired/executing/aliased slots, and is metered per domain. Build it PRIVILEGED first, prove the intra-slab UAF class closes on both layers, and only then argue the unprivileged variant with measurements -- because every clause of CARVE-v1's crux depends on a capability-is-authority premise that veda_bind_perms refutes."
          }
        ],
        "summary": "SWEEP RESULT. Sail: every ODT write goes through one chokepoint, odt_write (veda_regs.sail:1091). Eight instructions write it -- seven privileged behind the identical `cur_privilege == Machine | veda_oda_authorized()` gate (Populate, Populate-Fast, Destroy, set.cow, set.domain, page.out, page.in), and ONE unprivileged: Object-Bind (veda_bind_insts.sail:305, :342). RTL: a six-arm privileged if/else-if chain (veda_core.tlv:6466-6657) plus one separate unprivileged always_ff writing owner_hart (:6680-6682). So the precedent the crux needs already exists -- but it is safe for four specific reasons that CARVE-v1 inverts one by one: Bind writes ONE field, with a HARDWARE-SUPPLIED value (MHARTID -- software contributes no data), touching no identity and no policy field, at a slot whose name was already authorised.\n\nJUDGMENT: CARVE-v1 as written cannot be built, and the reason is not cost -- it is that its central premise is false on this machine. THE PARENT CAPABILITY IS NOT THE AUTHORITY. Bind re-derives Perms from the ODT entry (veda_bind_perms, veda_regs.sail:982-983), so PERM_CARVE is advisory exactly as PERM_STORE was: strip it and re-Bind the arena name to get it back. The design already adjudicated this and says so verbatim at veda_bind_insts.sail:316-320. Three further blockers, each verified at source: clause 2's R41 reset borns every slab element owner_domain = ANY, i.e. bindable by every domain -- correct for a privileged mint, a compartment hole for an unprivileged one; clauses 2 and 6 are mutually contradictory, since a carve-only parent cannot pass load/store to a child that inherits Perms, and the two non-inheriting sources are both closed (a GPR mints arbitrary authority; veda_attr is Machine-only on BOTH layers); and clause 5 turns generation exhaustion into a permanent unprivileged DoS, with DESIGN_08:203 already recording the reclaimer as unstarted.\n\nONE HYPOTHESIS REFUTED RATHER THAN REPORTED: I suspected veda_attr (0x7C4) was ungated on Sail and gated on RTL. It is not a divergence -- Sail enforces Machine-only generically via csrPriv(csr) = csr[9..8] = 0b11 (sys/sys_control.sail:19, :54). The refutation is what closes the third Perms option by construction.\n\nONE LIVE BUG FOUND IN PASSING, ON THE EXACT ENCODING SLOT R1 WANTS. The architect's brief states mode 0b11 \"maps to Illegal_Instruction() on both layers\". It does not. The RTL refuses it at decode (mcause 0x02); Sail reaches the Illegal_Instruction arm only at line 289, after the region (:211), domain (:284) and residency (:285) traps. On the region-2 object both layers already seed identically, Sail gives mcause 24 / REGION_FAULT 0x09 and the RTL gives mcause 0x02. The existing probe cannot see it on two counts: p5_reserved.S:27 uses Object_ID 1, where all three gates pass, and its handler only counts traps without reading mcause. This must be settled before R1 claims the slot.\n\nRECOMMENDED SHAPE: put carve authority in the ODT entry (a non-inherited `carve_only` bit plus an entry-held carve right, written by a privileged policy instruction in the set.domain family, no generation bump); have the child inherit owner_domain and a monotone subset of the parent's Perms; pin it inside the parent window; refuse on retired, executing (R11(b) fifth consumer) and aliased slots, with id_match from the first RTL line; meter it per domain. Ship it PRIVILEGED first and prove the intra-slab UAF class closes -- the class is a property of per-element Object_IDs, not of who mints them, so the whole security win is available without touching the privilege boundary at all. Unprivileged minting is a separate, later, measurable question.\n\nNote: read-only throughout; nothing was modified, built or run, and rva23-core was never touched."
      },
      {
        "findings": [
          {
            "claim": "CARVE-v1 does not close intra-slab use-after-free -- it converts it into arena-wide use-after-free. A child's ODT entry is a SEPARATE entry with its own Base, Length, generation and valid bit. Destroying the parent touches only the parent's entry, so every child capability stays tagged, generation-current, valid, in-bounds and resident, and keeps read/write access to the freed arena. This is exactly the failure mode DESIGN_01 invented CAndPerm to avoid, now minted once per allocation.",
            "evidence": "DESIGN_01_CAPABILITY_FORMAT_RESPEC.md:98-100: \"giving a domain a read-only view of a live object forces a *separate Object_ID aliasing the same Base* -- which breaks temporal safety (destroying one ID does not revoke the alias).\" The destroy path writes ONE entry: veda_ocl_insts.sail:788 `odt_write(object_id, struct { valid = false, ... generation = new_generation, ...})`. The dereference re-check reads only the entry the capability names: veda_ocl_insts.sail:72 `let entry = odt_lookup(cap.Object_ID);` and veda_ocl_insts.sail:83 `else if not(entry.valid) | entry.generation != cap.generation then Err(... VEDA_CAUSE_TAG_VIOLATION)`. No existing check relates two entries. NO existing check stops this.",
            "bearing": "Fatal to the headline claim in DESIGN_07:64 (\"Closes the class? Yes, by construction\"). CARVE must either record parentage in the entry and make Destroy(parent) transitively revoke children (unbounded work, breaks the flat single-transaction pillar), or forbid destroying a parent that has live children (needs a child counter per entry)."
          },
          {
            "claim": "Carving breaks the DESIGN_02 cached-Base paging contract. PAGE_OUT bumps only the parent's generation and PAGE_IN rewrites only the parent's Base, so children are neither invalidated nor relocated. A child capability survives the parent's eviction pointing at a physical frame the pager has already reassigned -- the precise use-after-free the page-out/page-in pair was built to prevent.",
            "evidence": "veda_ocl_insts.sail:1257-1262 PAGE_IN writes one entry: `Base = base, // CHANGED: the new frame` with `generation = old_entry.generation`; its own header at :1188-1191 says it \"Changes EXACTLY TWO fields -- Base and resident.\" PAGE_OUT likewise writes one entry (veda_ocl_insts.sail:1167 `generation = old_entry.generation + 1`). The residency arm that would catch it reads the CHILD's entry, which is still resident: veda_ocl_insts.sail:225 `else if not(entry.resident) then Err(... VEDA_CAUSE_RESIDENCY_FAULT)`. DESIGN_02_ODT_UNIFIED_MM.md states the invariant this violates: \"a cached `Base` **must** be invalidated on page-in.\" NO existing check stops this.",
            "bearing": "Composes with R38(b), which already made copy-on-write objects unpageable (veda_ocl_insts.sail:1141 `else if old_entry.cow then Illegal_Instruction()`). Under CARVE, carved arenas become unpageable too, which removes paging exactly where allocation churn is highest -- the opposite of DESIGN_02's ODT-as-mm thesis."
          },
          {
            "claim": "Nothing anywhere in either layer forbids two ODT entries from overlapping in [Base, Base+Length). Two overlapping children reconstruct intra-slab UAF in full: free(A) staleifies A, the allocator re-carves the slot as C for a victim, and the attacker's still-live overlapping capability B reads and writes the victim's element with every check passing. Worse, ONE carve of a child spanning the entire parent window converts a \"carve-only, not directly dereferenceable\" arena into a fully load/store-capable arena capability.",
            "evidence": "`grep -rn overlap` over model/extensions/Veda/*.sail, veda_core.tlv and VEDA_CORE_SPEC.md returns no bounds-disjointness check anywhere -- the only hits are unrelated (veda_atomic_insts.sail:42, veda_core.tlv:6414, :6605). The bounds arm is strictly per-object: veda_ocl_insts.sail:182 `else if unsigned(offset) + width > unsigned(cap.Length) then Err(... VEDA_CAUSE_BOUNDS_VIOLATION)`. NO existing check stops this.",
            "bearing": "Overlap-freedom is the load-bearing precondition CARVE-v1 never states. Enforcing it needs a per-arena occupancy structure consulted at carve time -- a slab bitmap in hardware -- which is a second indirection structure and directly contradicts DESIGN_02's \"exactly one indirection structure in the machine\" commitment."
          },
          {
            "claim": "CARVE is a complete copy-on-write bypass. The COW fault reads the ENTRY of the object being dereferenced; a carved child is a different entry with cow = false, so a store through the child writes the shared parent bytes in place with no 0x0C fault and no copy. fork() isolation is gone for any arena a principal can carve.",
            "evidence": "veda_ocl_insts.sail:256 `else if need_store & entry.cow then Err(... VEDA_CAUSE_COW_FAULT)` -- `entry` is `odt_lookup(cap.Object_ID)` (:72), i.e. the child's entry. Populate resets the field on every new entry (veda_ocl_insts.sail:747 `cow = false`, per R41), so a carve modelled on Populate births children non-COW. The Bind-time mask that is supposed to be inescapable only masks the parent's own re-derivation: veda_regs.sail:982-983 `function veda_bind_perms(e : odt_entry) -> bits(16) = if e.cow then e.Perms & 0xFFF7 else e.Perms`. NO existing check stops this. Note the attacker needs a pre-cow parent capability carrying carve authority, which R38 says is exactly what the parent holds at fork.",
            "bearing": "R38 placed the split right in \"whoever held PERM_STORE at the moment the object became copy-on-write.\" CARVE gives that same principal a way to write the shared object without splitting it. A carve must inherit the parent entry's `cow` into the child, or refuse on a cow parent entirely (matching the PAGE_OUT precedent)."
          },
          {
            "claim": "An unprivileged carve that takes the child's Object_ID from an operand is an unprivileged arbitrary ODT-slot overwrite. In the RTL the slot is resolved by local[7:0] alone and NEITHER ODT write arm consults the 36-bit anti-alias tag, so a \"fresh\" child id anywhere in the 24-bit local space collides modulo 256 with a live descriptor and clobbers it -- the exact bypass R11(b)/RTL-11 closed for Populate by comparing SLOTS rather than names.",
            "evidence": "veda_core.tlv:2554-2562: \"This file models 256 locals per region and resolves with local[7:0] only ($veda_odt_entry_idx, above), so MANY names share one slot -- Object_ID 436 and Object_ID 180 land on the same 32 bytes. The id_hi tag exists to detect exactly that, but it is consulted only on the two READ paths ($veda_odt_valid, $veda_check_odt_valid); neither ODT write arm looks at it.\" The existing protection is privilege, and it would be removed by construction: veda_ocl_insts.sail:636 `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`.",
            "bearing": "If CARVE instead has hardware pick the id, hardware must find a free slot -- and there is no free-list, no next-id register, and no instruction that can even read an ODT entry (DESIGN_02: \"There is no way to read an ODT entry field at all\"). A hardware scan breaks the fixed-read-count guarantee DESIGN_08 Section 6 commits to. This operand question is the one CARVE-v1 leaves open and it is the whole design."
          },
          {
            "claim": "PERM_CARVE cannot be confined to intended holders, because Bind re-mints Perms verbatim from the ODT entry and per-object bind authority defaults to open. Anyone who can name the arena Object_ID gets a fresh PERM_CARVE capability, and no CAndPerm attenuation by a delegator survives a re-Bind.",
            "evidence": "veda_regs.sail:982-983 `veda_bind_perms(e) = if e.cow then e.Perms & 0xFFF7 else e.Perms` -- the only attenuation Bind applies is the COW store mask. veda_regs.sail:985-988 `function veda_bind_domain_ok(owner_domain) = if owner_domain == VEDA_DOMAIN_ANY then true else if veda_pcc_object == VEDA_OBJECT_NONE then true else owner_domain == veda_pcc_object[43 .. 24]`, and veda_types.sail's odt_entry comment states VEDA_DOMAIN_ANY is \"how every object is created\". DESIGN_07's R17 records the residue in its own words: \"Intra-region isolation. Many objects share a region, and a compartment may still bind any of them.\" Names are freely readable -- CGetObjectID is ungated (veda_cap_insts.sail:118-135).",
            "bearing": "The \"carve-only Permit\" is load-bearing in DESIGN_07:66 and it is not enforceable through the capability. It is only enforceable through the arena's ODT ENTRY Perms plus a narrowed owner_domain -- i.e. CARVE hard-requires veda.odt.set.domain to be used on every arena, which is privileged. That silently reintroduces a privileged step into the malloc path the design was trying to de-privilege."
          },
          {
            "claim": "If CARVE borrows ODT-Populate-Fast's operand shape (Length/Perms from veda_attr), the child's permissions are chosen by an entirely UNGATED CSR, and the recorded safety argument for leaving that CSR ungated is voided by the same change.",
            "evidence": "veda_regs.sail:619 `function clause write_CSR(0x7C4, value) = { veda_attr = zero_extend(value); Ok(veda_to_xlen(veda_attr)) }` -- no privilege term, and veda_regs.sail:511 `function clause is_CSR_accessible(0x7C4, _, _) = currentlyEnabled(Ext_Veda)`. The recorded justification, veda_regs.sail:565-569: \"`veda_attr` (0x7C4) is deliberately NOT gated here -- its sole consumer, VEDA_ODT_POPULATE_FAST, already has its own independent capability-authority gate ... so a compartment rewriting `veda_attr` alone cannot use it for anything without separately satisfying that unrelated check.\" ODT_POPULATE_FAST reads it at veda_ocl_insts.sail:963-964 `Length = veda_attr[55..16], Perms = veda_attr[15..0]`.",
            "bearing": "Child Perms must be derived by monotonic AND from the PARENT capability's Perms, never from an operand or a CSR. Otherwise carve is a permission-amplification instruction -- including minting PERM_EXECUTE over attacker-written bytes, which no existing check prevents once the entry says so."
          },
          {
            "claim": "Making free() a capability-authorized ODT-Destroy contradicts a researched, recorded decision in the spec, not merely a convention. Possession of a capability is deliberately NOT destroy authority in this design.",
            "evidence": "VEDA_CORE_SPEC.md:330: \"**`ODT-Destroy` is deliberately, permanently NOT owner-gated** ... real CHERI's own architectural precedent for object-capability revocation (CHERI ISA spec §2.3.16 ...) places revocation authority with *\\\"a trustworthy software handler\\\"* / *\\\"a trusted userspace supervisor,\\\"* entirely independent of which capabilities currently reference the object being revoked -- the opposite of an ownership-based gate.\" Both layers enforce it: veda_ocl_insts.sail:753 and veda_core.tlv:2621-2623 `$veda_odt_destroy_violation = $is_veda_odt_destroy && (!($priv || $veda_oda_authorized) || $veda_object_is_executing)`.",
            "bearing": "If carve is unprivileged and free stays privileged, the attacker gets an allocation primitive with no matching deallocation -- a pure resource-exhaustion asymmetry. If free becomes unprivileged on possession, then because Bind mints capabilities to any owner_domain=ANY object, anyone who can NAME an object can destroy it. Neither half is acceptable as stated; carve needs a recorded parent-child link so \"you may destroy what you carved\" is expressible."
          },
          {
            "claim": "Unprivileged carve turns namespace and generation exhaustion from an accounting concern into a one-instruction-per-unit denial of service with domain-wide blast radius, and retirement makes it permanent. Reclamation (Rev-B) is unstarted, and the ODT-granule residency fault that DESIGN_08 relies on to bound the DRAM cost is not built in either layer.",
            "evidence": "Retirement is irreversible: veda_ocl_insts.sail:786 `let new_retired : bool = old_entry.retired | (old_entry.generation == 0xffffff);` and Populate then refuses forever at veda_ocl_insts.sail:684 `if old_entry.retired then Illegal_Instruction()`. DESIGN_08 Section 9 already concedes the pressure -- \"Under R1 SLAB-CARVE the 24-bit local space and the 24-bit generation fill under churn; without Cornucopia-style sweeping revocation (DESIGN_06, DESIGN_07 Rev-B, unstarted) a hot domain exhausts its local namespace\" -- but assumes an allocator is in the loop. There is no granule-residency cause: the full cause list at veda_bind_insts.sail:17-138 has REGION_FAULT 0x09 and RESIDENCY_FAULT 0x0A and nothing at ODT-granule scope; grep for \"granule\" in veda_regs.sail returns only the unrelated tag-store comment at :1084.",
            "bearing": "An unprivileged carve is an unprivileged request for kernel DRAM with no arbiter. The R38 comment states the current, correct posture explicitly (veda_ocl_insts.sail:157-163): allocation \"is a request to something that holds that authority ... rather than an allocation the machine performs on demand for anyone who stores.\" CARVE inverts exactly that sentence."
          },
          {
            "claim": "The ODT-covering-carve attack FAILS today, in both layers, but for an implementation reason rather than an architectural invariant -- and the surrounding range-gate hole (Rev-E) is still open on the capability paths.",
            "evidence": "Sail's ODT is not memory at all: veda_regs.sail:802 `register veda_odt : vector(8388608, odt_entry) = vector_init(empty_odt_entry)`, reached only through odt_lookup/odt_write, never through read_ram/write_ram. In RTL it is a physically separate array from the one capabilities index: veda_core.tlv:234 `logic [7:0] odt_mem [ODT_BASE : ODT_BASE + (ODT_ENTRIES * ODT_ENTRY_BYTES) - 1];` at `ODT_BASE = 32'h9000_0000` (:57) versus veda_core.tlv:942 `logic [7:0] elfmem [ELFMEM_BASE : ELFMEM_BASE + ELFMEM_SIZE - 1];` at 0x8000_0000/0x0008_0000, and every capability store writes elfmem only (veda_core.tlv:6369ff). BUT that OCS.D write has no window test, unlike the base-ISA path's `if (CPU_veda_baseisa_store_in_range_a0)` at :6353 -- Rev-E's \"validate ODT-Populate's new Base\" is also still absent (veda_ocl_insts.sail:720 `Base = zero_extend(descriptor[63..32])`, :955 `Base = base` -- no range check on either).",
            "bearing": "Carve is strictly SAFER than Populate on this one axis, because it derives Base = parent.Base + offset from a checked capability instead of accepting a raw GPR value, and that is a genuine argument in its favour. But the ODT's unreachability should be written down as an invariant (\"no ODT entry Base may fall inside the ODT region\") before an unprivileged instruction starts writing entries, since in silicon both arrays are DRAM."
          },
          {
            "claim": "Two containment-check traps that will silently produce escapes if written the obvious way: the arithmetic must be done wider than the widest operand, and it must be checked against the parent CAPABILITY's window, not the parent ENTRY's.",
            "evidence": "R18 is the recorded precedent for the first (DESIGN_07:939-947): \"The bounds addition was **64 bits wide on both sides**, and the offset is a full, attacker-chosen 64-bit GPR. **`offset = 0xFFFFFFFFFFFFFFF8` makes `offset + 8` wrap to 0** ... **SAIL COULD NOT EXPRESS THE BUG.**\" For the second: CSetBounds keeps the same name, so the entry and the capability disagree about the window -- veda_cap_insts.sail:229 `let window_ok : bool = unsigned(cap.Offset) + unsigned(new_length) <= unsigned(cap.Length);` with veda_cap_insts.sail:232-238 carrying `Object_ID = cap.Object_ID` and `generation = cap.generation` unchanged. A carve that validates containment against `odt_lookup(cap.Object_ID).Length` would let a narrowed delegated sub-capability carve anywhere in the full arena.",
            "bearing": "Both are cheap to get right and invisible when got wrong; the second in particular silently voids CSetBounds-based delegation, which is the only spatial narrowing the machine has."
          },
          {
            "claim": "Several attack routes I expected to work are already blocked by existing checks, and they should be recorded as such so the design does not spend effort re-closing them.",
            "evidence": "(a) Carve a child bearing PERM_ACCESS_SYSTEM_REGISTERS and install it as the ODA for full Populate/Destroy authority -- BLOCKED: veda_cap_insts.sail:894 `if cur_privilege != Machine then Illegal_Instruction()` is the first line of VEDA_OSPECIALRW, and the RTL mirrors the ODA predicate at veda_core.tlv:4184. (b) Carve from a stale/destroyed parent -- BLOCKED if carve reuses the existing chain: veda_ocl_insts.sail:83 `not(entry.valid) | entry.generation != cap.generation`. (c) Carve while the parent is paged out -- BLOCKED by veda_ocl_insts.sail:225 `not(entry.resident)`, PROVIDED carve consults it; it is not a memory access, so this is an explicit obligation, not an inheritance. (d) The encoding slot is genuinely free and fail-closed on both layers: veda_bind_insts.sail:289 `VEDA_BIND_RESERVED => Illegal_Instruction(),` and veda_core.tlv:4833-4835 \"Mode 0b11 is deliberately absent -- it is VEDA_BIND_RESERVED and must trap.\" (e) Perms bits 13/14/15 are free (veda_types.sail: `// bits 13-15 spare`) and bit 11 is PERM_SET_CID, as stated. Causes 0x0D/0x0E/0x0F are unallocated across the full list at veda_bind_insts.sail:17-138.",
            "bearing": "The privilege-escalation-to-ODA route being closed is the single best piece of news for CARVE: it means an unprivileged carve, even with attacker-chosen child Perms, cannot bootstrap itself into Populate authority. The damage stays inside the object model rather than escaping to the machine authority model."
          },
          {
            "claim": "Carve authority is recursive by default and multiplies an already-open finding. A child inheriting the parent's Perms inherits PERM_CARVE, giving an unbounded carve tree from one arena capability; and every carve is a new name that R43 -- still open -- will let Rebind install into any register.",
            "evidence": "Task list item #41 records R43 as pending: \"Rebind does not check that the register it refreshes names the same object.\" The Rebind path installs the operand's name and keeps the destination register's old Offset: veda_bind_insts.sail:312-315 `Object_ID = object_id, ... Offset = cur.Offset, // preserved across relocation, Section 4`. There is no monotonicity rule on Perms at Bind at all -- veda_regs.sail:983 mints `e.Perms` verbatim.",
            "bearing": "Minor next to the first four findings, but it means CARVE must explicitly decide whether PERM_CARVE is inherited, and it raises the priority of R43 from a tidy-up to a prerequisite, because carve multiplies live names by orders of magnitude."
          }
        ],
        "summary": "CARVE-v1 as specified does not close the intra-slab use-after-free class -- it relocates it. A carved child is a SEPARATE ODT entry with its own Base/Length/generation, and every check in the machine is per-entry, so (1) Destroy(parent) leaves children live over freed arena memory, (2) page-out/page-in of the parent leaves children pointing at a reassigned frame, (3) nothing anywhere forbids overlapping entries, so two overlapping children rebuild the exact UAF -- and one whole-window carve turns a \\\"carve-only\\\" arena into a fully dereferenceable one, and (4) a child entry is born cow=false, so carving is a complete copy-on-write bypass. All four are unstopped by any existing check; I quote the specific arms that fail to fire. DESIGN_01:98-100 already names the root cause in its own words: \\\"a separate Object_ID aliasing the same Base -- which breaks temporal safety (destroying one ID does not revoke the alias).\\\" CARVE mints one such alias per allocation. On the crux question -- unprivileged ODT writes -- the news is mixed. The escalation route I most expected (carve a child with Permit_Access_System_Registers, install it as the ODA, obtain Populate/Destroy authority) is already closed by OSpecialRW's Machine-only gate, so the blast radius stays inside the object model. But PERM_CARVE cannot be confined: Bind re-mints Perms verbatim from the entry and owner_domain defaults to VEDA_DOMAIN_ANY, so carve authority is bearer-authority-by-name unless every arena is narrowed with a privileged set.domain -- putting a privileged step back into malloc. If the child id comes from an operand, an unprivileged carve is an arbitrary ODT-slot overwrite in the RTL, whose write arms never consult the anti-alias tag; if hardware picks the id, there is no free-list, no next-id register, and no instruction that can even read an entry, so the search breaks DESIGN_08's fixed-read-count guarantee. That operand question is the one CARVE-v1 leaves open and it is the whole design. Two attacks fail: the ODT is not reachable from any capability in either layer (Sail's is a Sail register; RTL's odt_mem at 0x9000_0000 is a physically distinct array from elfmem), and carve is strictly safer than Populate on Base, since it derives Base from a checked capability rather than accepting a raw GPR -- Rev-E's Populate-Base validation is still absent. Two implementation landmines to record now: the containment arithmetic must be one bit wider than the widest operand (R18's exact shape), and containment must be checked against the parent CAPABILITY's window, not the entry's, or CSetBounds delegation is void. What I could not establish from the sources: whether the parent is rd or a second operand -- the design does not fix it, and several findings above change shape depending on that choice. I did not build or run anything; every claim is source-read."
      },
      {
        "summary": "CARVE-v1 is half right and its stated motivation is factually wrong. The MECHANISM half is not merely right, it is forced: the dereference recheck compares `entry.generation != cap.generation` against `odt_lookup(cap.Object_ID)`, so a generation is per-Object_ID by construction, and per-element temporal safety therefore requires a per-element ODT index, i.e. a fresh child Object_ID. Option (b) is refuted by that same line. Rev-A cannot subsume R1 because it partitions by delegatee while R1 must partition by sub-range inside one object held by one delegatee. But the CRUX AS STATED IS FALSE. \"If carve and free stay privileged, every malloc and free is a syscall\" is not true of this machine: `veda_oda_authorized()` carries no privilege term, and this repo already ships an object-per-allocation malloc where `veda_malloc` is ONE `veda.odt.populate.fast` and `veda_free` is ONE `veda.odt.destroy`, no trap, no privilege transition. Option (a) already costs exactly what carve would cost, and the class is already closed by that allocator's shape (option (d), non-vacuously). What is actually wrong with (a) is not cost but that the ODA is unscoped, globally ambient, and unsheddable across OCInvoke — a delegation-granularity problem. Reframed that way, CARVE-v1's authority design fails in three specific ways: it cannot be encoded as bind mode 0b11 (operand budget short by ~100 bits, and the veda_attr staging route is Machine-only); PERM_CARVE as a Perms bit is advisory because Bind re-derives Perms from the entry; and it never says where the child Object_ID comes from, where hardware allocation breaks fixed-shape O(1) and software naming reopens R11(b) unprivileged. R1 does not reduce any of the resource costs that motivate slabs — the child entry, the local id and the ODT write all remain — so it buys authority scoping only. I recommend CARVE-v2 (entry-resident carve authority, arithmetically derived child id from an arena id-run, CAndPerm-shaped Custom-2 encoding at funct7 0b0011000) plus a scoped ODA, and note R1 is blocked behind Rev-B, not merely co-designed with DESIGN_01.",
        "findings": [
          {
            "claim": "The crux premise is false at source: per-element Populate/Destroy is ALREADY unprivileged, ALREADY one instruction, and this repo ALREADY ships an object-per-allocation malloc built on it. Option (a) does not cost a syscall per malloc.",
            "evidence": "/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_ocl_insts.sail:632-633 — `function veda_oda_authorized() -> bool = veda_oda_tag & not(isSealedCap(veda_oda)) & permBit(veda_oda.Perms, PERM_ACCESS_SYSTEM_REGISTERS)` — no privilege term at all. The gate at :636, :753, :850 is `if not(cur_privilege == Machine | veda_oda_authorized()) then Illegal_Instruction()`, an OR. /home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt_asm.S:26-28 — `veda_odt_populate_fast_asm: veda.odt.populate.fast a0, a0, a1 / ret`; :32-34 — `veda_odt_destroy_asm: veda.odt.destroy a0, a0 / ret`. /home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt.c:69-95 — `veda_malloc` calls `veda_odt_populate_fast_asm(object_id, base)` and `veda_free` calls `veda_odt_destroy_asm(obj)`, one instruction each, no ecall anywhere in the file. /home/prabhu/veda-core-sindhu/veda-core/runtime/veda_rt.h:3-8 — \"A malloc/free-equivalent wrapper around real ODT-Populate(-Fast)/Bind/ODT-Destroy ... Object IDs and backing-memory slots are 1:1\".",
            "bearing": "The cost argument that motivates making carve unprivileged collapses. R1's case must be rebuilt on authority scope, not on instruction count — carve and Populate cost the same one ODT write. Any version of the R1 write-up that keeps the 'every malloc is a syscall' sentence is asserting something the repo's own runtime refutes."
          },
          {
            "claim": "What is actually wrong with option (a) is that the ODA is unscoped, globally ambient, and unsheddable — a delegation-granularity problem, not a privilege-transition cost. Delegating it to an allocator makes the allocator the kernel and leaks that authority to everything it calls.",
            "evidence": "`veda_oda` occurs in exactly three files: veda_regs.sail (11, declaration/reset/comment), veda_cap_insts.sail (4, OSpecialRW), veda_ocl_insts.sail (11, the gate and its consumers). ZERO occurrences in veda_bind_insts.sail, veda_types.sail, veda_atomic_insts.sail, and zero elsewhere in model/ — so it is never saved, cleared or narrowed on any trap, xret, OCInvoke or OCRETURN path. DESIGN_07 R34 (/home/prabhu/veda-core/design/DESIGN_07_ROBUSTNESS_AND_SECURITY_HARDENING.md:~868) — \"`OSpecialRW` is Machine-gated on both layers, so a U-mode compartment would *inherit* the root ODA through OCInvoke and **never be able to shed it**.\" /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:5228 — \"six authority gates take $veda_oda_authorized with no privilege term at all\". DESIGN_00 (/home/prabhu/veda-core/design/DESIGN_00_SASOS_REFRAME.md, \"What this buys immediately\") — \"Kernel vs user becomes 'holds the ODA authority vs does not' (DESIGN_05), not a hardware ring.\"",
            "bearing": "This is the true statement of R1's problem and it should replace the cost framing. It also means the correct comparison is not carve-vs-syscall but carve-vs-scoped-delegation."
          },
          {
            "claim": "Option (b) is refuted by the dereference check itself — and the refutation PROVES the fresh child Object_ID is forced rather than chosen. A generation is per-Object_ID by construction.",
            "evidence": "/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_ocl_insts.sail:83 and :423 — `else if not(entry.valid) | entry.generation != cap.generation then Err(veda_trap(..., VEDA_CAUSE_TAG_VIOLATION))`, where `entry = odt_lookup(cap.Object_ID)` (:73). RTL mirror /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv:3416 — `$veda_gen_stale = (!$veda_check_odt_valid) || ($veda_check_odt_gen != $veda_rs1cap_reserved);`. veda_cap_insts.sail:231-240 — veda_setbounds writes `Object_ID = cap.Object_ID` and `generation = cap.generation`. So: a CSetBounds that mints a fresh generation into the CAPABILITY alone produces a capability that fails its very next dereference; a fresh generation written into the ENTRY kills every capability naming that Object_ID, including the arena's own. There is no third place for a generation to live that the check consults.",
            "bearing": "R1's 'mint a fresh child Object_ID + own ODT entry' is not a design preference, it is the only shape the existing check admits. Attack the authority model, not the mechanism. Option (b) should be recorded as refuted-by-construction, not merely rejected."
          },
          {
            "claim": "The per-dereference transaction never reads entry.Base or entry.Length — only valid, generation, resident and cow. A child ODT entry is, on the hot path, a 32-byte container for a 24-bit counter.",
            "evidence": "Sail veda_ocl_insts.sail:71-72 — `let real_loc64 : bits(64) = zero_extend(cap.Base) + zero_extend(offset);` and :181 — `else if unsigned(offset) + width > unsigned(cap.Length) then Err(... BOUNDS_VIOLATION)` — both from the capability's cached copy. RTL veda_core.tlv:3479 — `$veda_bounds_ok = (({1'b0, $rs2_data} + 65'd8) <= {25'b0, $veda_rs1cap_length});`. The entry-derived terms in the same RTL expression (:3481-3482) are exactly `$veda_gen_stale`, `$veda_cow_write`, `$veda_deref_nonresident`. entry.Base/entry.Length are consumed only at Bind/Rebind (veda_bind_insts.sail:313-314, :344-345).",
            "bearing": "This is the honest place to attack R1's cost: the per-element entry's Base/Length exist only so the child can be re-Bound by name. It also bounds any 'cheaper carve' idea — the saving available is at most the fields Bind needs, and DESIGN_02's \"exactly one indirection structure\" forbids moving the counter to a second table reached by a dependent read."
          },
          {
            "claim": "CARVE-v1 cannot be encoded as bind mode 0b11 as sketched: the operand budget is short by roughly 100 bits, and the veda_attr staging route used by Populate-Fast is Machine-only, so unprivileged code cannot reach it.",
            "evidence": "veda_bind_insts.sail:184-186 — `mapping clause encdec = VEDA_BINDINST(mode, rs1, rd) <-> 0b0000000000 @ mode @ encdec_reg(rs1) @ 0b101 @ 0b0 @ encdec_vcap(rd) @ 0b0001011`. That is ONE GPR, ONE capability register, and 10 reserved-zero immediate bits. CARVE-v1 needs parent capability + child Object_ID(44) + offset(40) + length(40) + destination capability. DESIGN_01's own decision 6 already hit this exact wall for Populate: \"Base56+Length40+Perms16 = 112 bits no longer fits the single-GPR packed descriptor\". The escape Populate-Fast used — the veda_attr CSR — is closed to unprivileged code: 0x7C4 has bits[9:8] = 0b11 = Machine by the RISC-V convention, and DESIGN_07 R35 records \"Sail refuses the write: `csrPriv(0x7C4)` is bits [9:8] = `0b11`, Machine-only ... enforced by the generic `check_CSR_priv`\", with R35 adding the missing RTL privilege term.",
            "bearing": "CONSTRUCTIVE: the CAndPerm R-type shape already carries exactly {GPR, source capability, destination capability} — veda_cap_insts.sail:963-965, `0b0010111 @ encdec_reg(rs2) @ 0b0 @ encdec_vcap(rs1) @ 0b001 @ 0b0 @ encdec_vcap(rd) @ 0b1011011`. funct7 0b0011000 is the next free Custom-2/funct3=001 slot (highest allocated is CAndPerm's 0b0010111; full enumeration confirmed by grep of every encdec in the file). If the arena's ODT entry carries an element stride, the GPR needs to hold only a 24-bit element index and the budget closes. The 'free encoding slot' at bind mode 0b11 is free in DECODE, not in operand budget."
          },
          {
            "claim": "PERM_CARVE as a capability permission bit is advisory and therefore cannot express 'carve-only parent, not directly dereferenceable'. Carve authority must live in the ODT entry, as `cow` does.",
            "evidence": "veda_regs.sail:982-983 — `function veda_bind_perms(e : odt_entry) -> bits(16) = if e.cow then e.Perms & 0xFFF7 else e.Perms` — Bind re-derives Perms from the ENTRY, masking only for cow. The codebase's own recorded correction, quoted at veda_ocl_insts.sail:157-160: \"a capability handed out with store stripped is only ADVISORY, because the holder can re-Bind the name and get a fresh, fully-permissioned one. So the attenuation has to happen HERE, where the entry is the authority and no re-derivation can escape it.\" And the bind gate is open by default: veda_regs.sail:985-987 `veda_bind_domain_ok` returns true for `VEDA_DOMAIN_ANY`, which every Populate writes (veda_ocl_insts.sail:~726, `owner_domain = VEDA_DOMAIN_ANY`) and which veda_types.sail:404 calls \"how every object is created\".",
            "bearing": "Anyone who learns the arena's Object_ID re-Binds it and recovers PERM_CARVE, and equally recovers LOAD/STORE if the entry has them. The verified fact that Perms bits 13/14/15 are free (PERM_SET_CID=11, PERM_NMC_COMPUTE=12, veda_types.sail:229-230) is true but is not the binding constraint. Put carve in the entry and gate it with `owner_domain`, the one authority a re-Bind cannot escape."
          },
          {
            "claim": "CARVE-v1 never says where the child Object_ID comes from, and both obvious answers are fatal: hardware allocation breaks the fixed-shape lookup, software naming reopens the R11(b) eviction class without even the M-mode/ODA gate.",
            "evidence": "Hardware side: DESIGN_08 §6 commits to \"a fixed-shape O(1) operation: **one read intra-domain, a worst-case-bounded two reads cross-domain**, data-independent in depth\" — a free-slot search over a per-region ODT that is demand-paged in 128-entry granules is neither fixed-shape nor bounded. Software side: Populate deliberately does NOT refuse a valid slot — it repopulates and bumps. veda_ocl_insts.sail:683-685 — `let new_generation : bits(24) = if old_entry.valid then (if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1) else old_entry.generation;`. RTL veda_core.tlv:2610-2612 — `$veda_odt_populate_violation = (...) && (!($priv || $veda_oda_authorized) || $veda_odt_retired || $veda_object_is_executing);` — no `!valid` term. The only pin is on code being executed: veda_regs.sail:990-992, `veda_object_is_executing(object_id) = (object_id == veda_pcc_object) | (veda_trap_depth != zeros() & object_id == veda_mepcc_object)` — no protection for any data object.",
            "bearing": "An unprivileged carve that names its own child id and reuses Populate's write semantics is an unprivileged revoke-and-repoint primitive against every object in the region — precisely the bypass R11(b) closed. CONSTRUCTIVE FIX: derive the child id arithmetically (arena id_base + index) with the id-run length and stride held in the arena's ODT entry. Then 'child window inside parent window' and 'child id inside the parent's reserved run' become one check, there is no search, and no victim outside the arena can be named."
          },
          {
            "claim": "DESIGN_08 already names R1's authority hole as an open question and classifies the bug as a compartment escape; it also makes Rev-B a hard prerequisite for R1, not a co-design partner.",
            "evidence": "/home/prabhu/veda-core/design/DESIGN_08_OBJECT_NAMESPACE_SCALE.md §9 — \"**region-grant authority** -- which domain may mint into which region -- is a new authority question, and a bug letting domain A mint into region B is a compartment-escape of the Milestone-19 class\". Same section — \"**Reclamation is now a hard prerequisite, not a someday.** Under R1 SLAB-CARVE the 24-bit local space and the 24-bit generation fill under churn; without Cornucopia-style sweeping revocation (DESIGN_06, DESIGN_07 Rev-B, unstarted) a hot domain exhausts its local namespace.\" The runtime already lives this limit: veda_rt.h:13-19 — \"once a slot's generation counter is exhausted ... that slot's backing memory is permanently unusable too, not just its Object_ID\".",
            "bearing": "A PERM_CARVE capability to an arena in region B, held by a domain in region A, IS domain A minting into region B. R1 as sketched does not answer DESIGN_08's open question, it instantiates it. And DESIGN_07 R1's \"Attaches to: Phase 1-2\" understates the dependency: without Rev-B, a carving allocator burns 24-bit local ids and per-slot generations at malloc rate."
          },
          {
            "claim": "Rev-A does not subsume R1 — the two partition on orthogonal axes, and no re-indexing of Rev-A turns it into R1 without hitting the entry-space wall.",
            "evidence": "DESIGN_07 Rev-A (design/DESIGN_07_...md:76-101) indexes the epoch vector \"by a delegation/compartment ID\" and its own verifier correction bounds it to \"a **bounded fan-out of k delegatees per object** (not arbitrary)\". Intra-slab UAF is a stale pointer to element 3 and a live pointer to element 7 held by the SAME principal — same delegation ID, same epoch bit, both pass. Re-indexing the vector by element instead of delegatee is a different mechanism, and it needs entry space the entry does not have: RTL veda_core.tlv:108 `ODT_ENTRY_BYTES = 32` with the last allocated field at :160 `ODT_OFF_COW = 29`, leaving 2 spare bytes. A per-arena generation vector reached through the entry is a second dependent read, which DESIGN_02 forbids: \"there is exactly one indirection structure in the machine, and it is object-indexed.\"",
            "bearing": "Rev-A should be recorded as complementary (per-holder revocation of a live shared object) and explicitly NOT as a substitute for R1. The in-entry sub-generation variant is worth naming as the honest third option, and honestly killed on arity: real kmem_caches hold dozens to hundreds of elements per slab, and 2 spare bytes will not hold their counters."
          },
          {
            "claim": "Option (d) 'do nothing in hardware' is not vacuous on this machine: the class is ALREADY closed by the shipped allocator's shape, and CARVE-v1 buys back none of the resource costs that would make an allocator choose the vulnerable shape instead.",
            "evidence": "veda_rt.h:6-13 — \"Deliberately a SINGLE-SLAB-SIZE pool allocator ... Object IDs and backing-memory slots are 1:1 (slot i always backs Object_ID VEDA_RT_FIRST_OBJECT_ID+i)\" — one Object_ID per allocation, so there is no intra-slab window to exploit. The reasons to prefer slab-in-one-object are exactly the costs DESIGN_07 R1 itself concedes: \"one ODT entry per live slab element -> pressures the Object_ID ceiling and ODT footprint ... carve/free each pay a DRAM-tier ODT access unless the child lands in TCM\". CARVE-v1 still allocates one child entry per element, still consumes one 24-bit local per element, and still performs one ODT write per malloc and per free.",
            "bearing": "State plainly in the R1 write-up that carve reduces ZERO of the footprint, namespace and DRAM-traffic costs versus a per-element Populate; it changes only who is allowed to issue the write. That is a defensible thing to buy, but it must be the stated thing, or the design will be judged against a saving it does not deliver."
          },
          {
            "claim": "There is a genuinely different answer that adds no instruction: SCOPE the ODA. The gate currently ignores every field of the ODA capability except one permission bit, and both the Object_ID range and the Base range need to be scoped — the Base half is an open hole today even on the privileged path.",
            "evidence": "Sail veda_ocl_insts.sail:632-633 and RTL veda_core.tlv:4184 — `$veda_oda_authorized = $veda_oda_tag && !$veda_oda_sealed && $veda_oda_perms[7];` — the ODA capability's own Object_ID(44), Base(56) and Length(40) are read by nothing. DESIGN_07 Rev-E — \"**ODT-Populate writes a software-supplied raw Base with zero range validation**. So an ODA-authorized Populate can seat an object's Base outside the legal backing region\". DESIGN_07 R34's own test for whether a gate is real — \"an authority gate whose authority no reachable context lacks denies nothing\" — is passed by scoping and failed by the refuted root-ODA proposal.",
            "bearing": "Extending `veda_oda_authorized()` to `veda_oda_authorized(object_id, base, length)` — target id inside a run the ODA names AND target Base/Length inside the ODA's own window — is one predicate, in one already-factored Sail helper and one RTL signal, with no new instruction, no new permission bit, and zero dereference-path cost. It answers DESIGN_08 §9(b) and Rev-E together. Honest weakness: the ODA is ONE register, so a domain needing two arenas must swap it, and OSpecialRW is Machine-gated (veda_cap_insts.sail:894), so swapping costs a trap. That is exactly where a per-arena capability OPERAND — i.e. carve — wins, and it is the real argument for R1."
          },
          {
            "claim": "VERDICT: not 'CARVE-v1 is simply right'. Adopt CARVE-v2 — mechanism unchanged (forced), authority rebuilt in three places — and keep it behind Rev-B. Also, one RTL comment that any R1 entry-field work will read is stale.",
            "evidence": "CARVE-v2 = (1) carve authority in the ODT ENTRY, not a Perms bit, gated by `owner_domain` (per finding on veda_bind_perms/veda_bind_insts.sail:303-307); (2) child Object_ID derived as arena_id_base + index from a stride/count field in the entry, so no free-slot search and no arbitrary victim (per the Populate-repopulates-valid finding, veda_ocl_insts.sail:683-685 / veda_core.tlv:2610-2612); (3) encoded in the CAndPerm R-type shape at Custom-2 funct7 0b0011000 with the GPR carrying a 24-bit element index (veda_cap_insts.sail:963-965; 0b0010111 is the highest allocated). Arena CREATION stays privileged/ODA and supplies the carve-only Perms and the id-run — one privileged act per arena, not per element. STALE COMMENT: veda_core.tlv:106 says the entry is \"(26 B used, 6 spare after RTL-6)\", but RTL-17 put owner_domain at +26..+28 (:148) and RTL-18 put cow at +29 (:160), so with ODT_ENTRY_BYTES = 32 (:108) only bytes 30-31 remain — 2 spare, not 6.",
            "bearing": "CARVE-v2 keeps the one property CARVE-v1 gets uniquely right — the parent capability bounding the child's memory window, which closes Rev-E's raw-Base hole by construction for the carve path — while removing the three failure modes. The stale spare-byte comment matters concretely: a stride/id-run field, a sub-generation vector, and `backing` are all competing for those 2 bytes, and the file currently advertises three times the room it has."
          }
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Ground"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Refute"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Brief"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "ground:id-space",
      "phaseIndex": 1,
      "phaseTitle": "Ground",
      "agentId": "a226f2e27a1bae5cf",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040115153,
      "queuedAt": 1787040110322,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "R1/CARVE-v1 sweep on \"where do child Object_IDs come from?\"…",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787040898049,
      "tokens": 196191,
      "toolCalls": 49,
      "durationMs": 782896,
      "resultPreview": "{\"summary\":\"R1/CARVE-v1 sweep on \\\"where do child Object_IDs come from?\\\", from a complete read of DESIGN_08 plus the ODT indexing on both layers. Headline: **no free-ID allocator exists anywhere in this machine, in hardware or in architecture.** Every ODT-mutating instruction on both layers takes the 44-bit Object_ID verbatim from a GPR; the only allocator in the tree is an 8-slot linear scan ove…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "ground:authority",
      "phaseIndex": 1,
      "phaseTitle": "Ground",
      "agentId": "afa71e4548da1ead3",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040116129,
      "queuedAt": 1787040110322,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SWEEP RESULT. Sail: every ODT write goes through one chokep…",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787040821887,
      "tokens": 187413,
      "toolCalls": 67,
      "durationMs": 704741,
      "resultPreview": "{\"findings\":[{\"claim\":\"COMPLETE ODT WRITE CENSUS, SAIL. Every ODT write on the Sail layer funnels through one chokepoint, `odt_write` (veda_regs.sail:1091-1094), over `register veda_odt : vector(8388608, odt_entry)` (veda_regs.sail:802). It has exactly nine call sites in instruction code, belonging to eight instructions. SEVEN ARE PRIVILEGED, all carrying the identical gate `if not(cur_privilege =…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "ground:attack-surface",
      "phaseIndex": 1,
      "phaseTitle": "Ground",
      "agentId": "a8e40dbdcf0e66ef4",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040115626,
      "queuedAt": 1787040110323,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CARVE-v1 as specified does not close the intra-slab use-aft…",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787040677701,
      "tokens": 194099,
      "toolCalls": 47,
      "durationMs": 560556,
      "resultPreview": "{\"findings\":[{\"claim\":\"CARVE-v1 does not close intra-slab use-after-free -- it converts it into arena-wide use-after-free. A child's ODT entry is a SEPARATE entry with its own Base, Length, generation and valid bit. Destroying the parent touches only the parent's entry, so every child capability stays tagged, generation-current, valid, in-bounds and resident, and keeps read/write access to the fre…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "ground:alternatives",
      "phaseIndex": 1,
      "phaseTitle": "Ground",
      "agentId": "ae05b50586f4482d3",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040115487,
      "queuedAt": 1787040110323,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CARVE-v1 is half right and its stated motivation is factual…",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787040792859,
      "tokens": 183774,
      "toolCalls": 48,
      "durationMs": 675714,
      "resultPreview": "{\"summary\":\"CARVE-v1 is half right and its stated motivation is factually wrong. The MECHANISM half is not merely right, it is forced: the dereference recheck compares `entry.generation != cap.generation` against `odt_lookup(cap.Object_ID)`, so a generation is per-Object_ID by construction, and per-element temporal safety therefore requires a per-element ODT index, i.e. a fresh child Object_ID. Op…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "refute:unprivileged-odt-write",
      "phaseIndex": 2,
      "phaseTitle": "Refute",
      "agentId": "a45d5f71e891c7d91",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040903582,
      "queuedAt": 1787040900061,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787041651011,
      "tokens": 127596,
      "toolCalls": 23,
      "durationMs": 746245,
      "resultPreview": "{\"verdict\":\"REFUTED\",\"strongest_attack\":\"CARVE-v1 SPLITS THE MINT FROM THE POLICY. It delegates the half that widens and keeps the half that narrows, and the kept half is privileged, un-pre-stageable, and irrevocable. This survives every repair the other sweeps proposed (derived child IDs, entry-held carve right, wide containment arithmetic), because it is forced by two pre-existing decisions that…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "refute:exhaustion",
      "phaseIndex": 2,
      "phaseTitle": "Refute",
      "agentId": "a2142bb843244ad54",
      "model": "claude-opus-5",
      "fallbackModel": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1787040902690,
      "queuedAt": 1787040900061,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787041327238,
      "tokens": 94749,
      "toolCalls": 14,
      "durationMs": 424547,
      "resultPreview": "{\"verdict\":\"REFUTED\",\"strongest_attack\":\"HOT-SLAB-SLOT GENERATION EXHAUSTION -> PERMANENT, UNPRIVILEGED SLOT DEATH.\\n\\nCARVE-v1 wires free() = ODT-Destroy of the child (DESIGN_07:57-59). Destroy ALWAYS bumps the child's 24-bit generation: veda_ocl_insts.sail:784 \\\"let new_generation : bits(24) = if old_entry.generation == 0xffffff then 0xffffff else old_entry.generation + 1;\\\". On the cycle it lea…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "refute:does-it-close-the-class",
      "phaseIndex": 2,
      "phaseTitle": "Refute",
      "agentId": "ab7edc656c2b37cea",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040904135,
      "queuedAt": 1787040900062,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787041382130,
      "tokens": 115155,
      "toolCalls": 14,
      "durationMs": 477364,
      "resultPreview": "{\"verdict\":\"REFUTED\",\"strongest_attack\":\"RE-CARVE THE WINDOW. The attacker never touches a stale capability, so the generation mechanism is irrelevant by construction.\\n\\nCARVE-v1's crux is that the parent capability IS the authority -- holding a PERM_CARVE capability to an arena authorises minting children inside it, unprivileged. Nothing stops minting a child over a window someone else is alread…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "refute:composition",
      "phaseIndex": 2,
      "phaseTitle": "Refute",
      "agentId": "ad2e586cc11a3a410",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040902865,
      "queuedAt": 1787040900067,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787041534293,
      "tokens": 170365,
      "toolCalls": 31,
      "durationMs": 629528,
      "resultPreview": "{\"verdict\":\"REFUTED\",\"strongest_attack\":\"THE PAIR THAT CONTRADICTS: R11(b)'s executing-object pin vs CARVE-v1 clause 2.\\n\\nThe pin's PREDICATE is an identity comparison. Sail: `veda_object_is_executing(object_id) = (object_id == veda_pcc_object) | (veda_trap_depth != zeros() & object_id == veda_mepcc_object)` (/home/prabhu/veda-core-sail-riscv/model/extensions/Veda/veda_regs.sail:990-992). RTL: a …"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "refute:minimality",
      "phaseIndex": 2,
      "phaseTitle": "Refute",
      "agentId": "a1d6521d9846b6d36",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787040903226,
      "queuedAt": 1787040900067,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REFUTED",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787041291807,
      "tokens": 116348,
      "toolCalls": 13,
      "durationMs": 387042,
      "resultPreview": "{\"verdict\":\"REFUTED\",\"strongest_attack\":\"PERM_CARVE OVER A RANGE IS A STRICT SUPERSET OF LOAD|STORE OVER THAT RANGE, SO CLAUSE 6 -- R1's OWN SELF-DECLARED LOAD-BEARING PART -- IS AN AMPLIFICATION, AND IT RECONSTRUCTS THE EXACT UAF IT EXISTS TO CLOSE.\\n\\nDESIGN_07_ROBUSTNESS_AND_SECURITY_HARDENING.md:63-65 states R1's whole security claim and pins it on clause 6: \\\"Closes the class? Yes, by constru…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "brief",
      "phaseIndex": 3,
      "phaseTitle": "Brief",
      "agentId": "acf3f744bdb3fd8b5",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1787041656149,
      "queuedAt": 1787041653193,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd /home/prabhu/veda-core-sail-riscv/model/extensions/Veda …",
      "promptPreview": "READ-ONLY. Do not modify, build, or run anything that writes. Never touch\n/home/prabhu/makerchip/rva23-core -- frozen, owned by another session. Cite file:line\nfor every claim; quote the actual source line.\n\n  Sail model : /home/prabhu/veda-core-sail-riscv/model/extensions/Veda/*.sail\n  RTL        : /home/prabhu/veda-core-sindhu/veda-core/rtl/veda_core.tlv\n  Tests      : /home/prabhu/veda-core-sin…",
      "lastProgressAt": 1787042227672,
      "tokens": 175253,
      "toolCalls": 41,
      "durationMs": 571522,
      "resultPreview": "Verification complete. Every claim below was re-read at source in this pass; nothing was built or run; rva23-core was never touched.\n\n---\n\n# R1 / CARVE-v1 — SYNTHESIS PASS\n\n## 0. Method note\n\nI re-verified every claim I repeat, including the four the architect asked for and the prior sweeps' headline findings. Where a prior sweep's line number was off by one I give the corrected one. Two claims in…"
    }
  ],
  "totalTokens": 1560943,
  "totalToolCalls": 347
}
